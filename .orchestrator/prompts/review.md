{{packet}}
You did not write this diff. Review it adversarially against the packet's acceptance criteria.
The diff is complete. Read a file only when a hunk depends on code outside it; at most 8 tool calls.
Return ONLY JSON: {"verdict":"approve|request_changes","comments":[{"path":"","line":0,"issue":"","severity":"low|med|high"}]}
Your entire final message must be that one JSON object: no prose before or after it, no markdown code fences. Put any explanation inside a "summary" string field of the same object. A message with text outside the JSON object is not parseable and fails the review.
