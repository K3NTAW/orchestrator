# plan.md — Planner checkpoint (updated 2026-09-19 01:10, end of session 2; written for a fresh session)

## Read first
1. `bus_read(status_not="done")` then skill `resume`. Codex cools until 2026-09-19 ~14:00.
2. RESTART FIRST, then let the daemon work. This session's MCP server still runs the pre-4aaaa03 daemon (no fallback dispatch, sonnet reviews of sonnet work, no orphan reconcile). goal/T-0073 now carries every fix: free_slots fallback capacity, review_tier, reconcile_dead (orphaned executors re-gated), merge.py keeps the checked-out main worktree in sync, run_worker keeps worker-posted verdicts, merge_reviewed sweeps done tasks. A fresh `f orch` session loads them: the daemon then dispatches ready tasks on the Claude fallback, spec-reviews c≥6 (D1 raised the threshold), reviews on the other model, merges on approve. Check the first dispatch (`pipeline.dispatched_at` on a queued ready task) before doing anything by hand. The hand procedures below stay valid if the daemon misbehaves.
3. Main checkout is on goal/T-0073, in sync with the branch (merge.py sync is live only in the new server; if the checkout goes stale after a merge: `git diff --name-only HEAD -- . ':!.orchestrator'` then `git checkout HEAD -- <those paths>`, never `-- .`).
4. Planner commits from the main checkout: `git status --short` first; stage nothing outside .orchestrator/ that nobody edited; commit with `git commit -F <scratchpad file>` (the co-author line's `>` trips planner-mode.sh); record.sh facts must avoid backticks and the words the hook treats as writes.
5. Tool contracts learned today: `spawn_spec_review(<execute id>)` RUNS AN EXECUTE (it wants the spec_review task id); `spawn_scout(<execute id>)` runs the fallback executor; a fix round whose branch carries unreviewed c≥4 work must be pre-stamped `pipeline.gated_at` or the daemon auto-merges it at c≤3; specs, scope and depends_on are immutable on the bus (recreate the task; mark the superseded one done+merged_into when the replacement merges so bus.ready sees the chain).
6. Review policy in force (D1, merged on goal/T-0109): 1–3 hooks only; 4–6 one review on the non-executing model; 7–10 two reviews (cross-model when Codex executed, both on the non-executing Claude tier otherwise) + security checklist; spec review from complexity 6 on sonnet.
7. Restart rule: hand over at ~150k context and only when no worker is running (reconcile_dead now recovers orphans, but a clean stop is still cheaper).
8. goal/T-0065 (PR 6) is pushed and awaits the human.

## Open goals and tasks (all execute tasks are sonnet fallback unless Codex is back)
Phase C — goal T-0073 (orchestrator repo, branch goal/T-0073). Merged: T-0079 C-O2a auth/secrets · T-0078+T-0087+T-0090 C-O1 notify webhook · T-0083+T-0102 C-O6 daemon fallback slots and review tier · config commit 523f438 (spec_review in role_affinity).
- T-0115 C-O3 v4 goal runner (c6): done, commit abc3f56 on task/T-0115, gate green, 127 tests. Executor's worker thread was orphaned by the restart; Planner reclaimed and posted the result by hand. Daemon's sonnet review T-0127 failed by hand (same model); opus review T-0128 running on B (15:00). Opus review T-0128 (1.24 USD, 212 s) requested changes: 1 high (scaffold commit sweeps the target's index), 5 med (porcelain quoting, install before prechecks, target pool.toml secrets run through bash -c, planner budget hardcoded, untested prechecks), 3 low. T-0115 held (review request_changes).
- T-0129 C-O3 fix round 1 (c5, deps T-0115, wt from task/T-0115): executor finished (2.64 USD, 710 s) but did not commit; Planner committed the worktree as found = 6e12f51; daemon's sonnet review T-0133 killed+failed; opus review T-0134 died at the 1.5 USD review cap; retry T-0137 approved (1.53 USD) with four low notes (backlog below). T-0129 merged into goal/T-0073 as 88f7396 (T-0115 rebased as eca9354) at 16:30; T-0115 marked done+merged by hand; ROOT synced; review cap 3.0 re-applied on the merged pool.toml line (decision recorded).
- T-0130 C-O9 daemon re-gates orphaned executors (c4): done f3d1f10; opus review T-0132 request_changes (3 med: upstream fallback never resolves, orphaned results could auto-merge at c≤3, requeue leaves dispatched_at stamped so the task is never retried = pre-existing dead end; 3 low). Held.
- T-0135 C-O9 fix round 1 (c4, deps T-0130): done e712553, 124 tests; daemon gated and spawned sonnet review T-0136 → killed+failed; opus review T-0139 approved 16:32 but run_worker overwrote its posted verdict with a parse_error result (gotcha recorded), so the daemon never merged; Planner merged T-0135 by hand 16:50 = acc0894; T-0130 marked done+merged by hand; ROOT synced.
- T-0131 C-O8 merge.py syncs the checked-out main worktree (c3): merged 58a6ec4 into goal/T-0073 at 15:28; ROOT synced by hand. Live only in a server started after the merge.
- Backlog from the approving serve review T-0161 (low): `git clone` argv needs a `--` before git_url (a git_url starting with '-' or an ext:: URL would be parsed as an option); missing repos.toml should be 503 not 404; get_goal/cancel_goal/create_goal duplicate check call goals.* unguarded (only the fan-out is isolated); _safe_reason redacts only URL credentials; long flock holds inside the shared threadpool. Fold into a C-O4 polish task with the C-O3 polish items below.
- Backlog from the approving review T-0137 (low, not blocking): goals.py _preview_cfg should catch TOMLDecodeError on the target pool.toml and refuse; Popen of claude should catch FileNotFoundError after the scaffold commit; single-goal guard should run under the goals lock; cli _format_goal_line should print the note field. Fold into a C-O3 polish task after C-O4/C-O5, or into the C-O5 runbook task if it touches goals.py anyway.
- T-0138 C-O10 run_claude/run_worker failure reason + partial output (c2): done 5aff342, clean; daemon gates and auto-merges (c≤3). Then sync ROOT.
- T-0116 C-O4 serve (c6, deps T-0115 merged): spec review spawned by hand 16:55 (old daemon cannot dispatch while Codex cools). On approve: `spawn_scout("T-0116")` by hand, or let the daemon do it after a session restart (the new server loads free_slots fallback, review_tier, reconcile_dead, merge sync).
- T-0141 C-O11 verdict preservation: merged 674b390 into goal/T-0073 (17:25, auto-merge c3). ROOT synced.
- goal/T-0109 fast-forwarded to 674b390 (goal/T-0073 head) at 17:33 before any D work; D worktrees cut from it carry today's C fixes.
- Phase D: T-0121 D2 scout budget merged 1b94384 into goal/T-0109 (17:50). T-0120 D1 review policy done cf7ad84 (18:10), gate green; daemon's sonnet review T-0149 killed+failed; opus review T-0150 (1.05 USD) request_changes: orphaned+two-review deadlock, failed sibling waits forever, second review could land on the executor's model (only two Claude tiers), CLAUDE.md still says spec review from 5, merged_at on the wrong task. T-0120 held.
- T-0151 D1 fix round 1 (c4, deps T-0120): executor finished 19:00 but did not commit; Planner committed the worktree as found. Daemon gate red: test_planner_md_matches_claude_md_lists (CLAUDE.md threshold line changed, prompts/planner.md copies it and was out of scope). T-0154 fix round 2 (planner.md sync) done 4501d91; the daemon gated and tried the c2 direct merge before the Planner could stamp it → rebase_conflict on README.md Pipeline (T-0121 scouts line + old sentence vs T-0120 paragraph), T-0154 failed. T-0157 fix round 3 done 20:05: rebased cleanly onto goal/T-0109 (2e32566 T-0120, 425775e T-0151, 8a82703 T-0154), README keeps the scouts line + threshold paragraph, gate green, pre-stamped. Opus review T-0159 (1.48 USD) request_changes: the failed-sibling escape only runs from a done review, so all-failed reviews still stall silently (high); avoid_account is always None and unconsumed (med); CLAUDE.md step 7 still claims cross-model for Claude-executed 7-10 (med); test asserts an inert key; README misses the orphaned exception. D1 fix round 4 T-0160 created 20:20 (c4, deps T-0157, wt from task/T-0157, six files incl. CLAUDE.md + planner.md, pre-stamped). T-0160 done 2f37982 (20:55), gate green; opus review T-0164 (1.69 USD) request_changes: an off-vocabulary review verdict makes merge_reviewed IndexError and stops merging for every task each tick (high); review count read live at merge time vs frozen at gate (med); src.review_verdict fallback unsafe with two reviews; sweep skips ungated tasks; inert second_review field. D1 fix round 5 T-0169 created 21:35 (c4, deps T-0160, wt from task/T-0160, daemon.py+tests only, pre-stamped). T-0169 done c593794, gate green; opus review T-0172 (1.46 USD) APPROVED with low notes (fewer reviews than needed + no rejections can still yield an empty hold_reason; [review] values untyped; bus.read(role=review) inside the per-task loop; vacuous avoid_account assertion). Merged c593794 into goal/T-0109 at 23:00; T-0120, T-0151, T-0154, T-0157, T-0160 marked done+merged_into goal/T-0109. D1 total: 5 execute rounds + 4 opus reviews (1.05+1.48+1.69+1.46 = 5.68 USD). Backlog: fold the T-0172 notes into D3 T-0122 (daemon.py) or a small D-polish task. D1 review spend so far: T-0150 1.05 + T-0159 1.48 + T-0164 1.69 = 4.22 USD; D1 is the costliest task of the day. If round 5 is rejected again: escalate to the human (CLAUDE.md: executor fails 3 rounds). 22:02: T-0169's executor hit a rate limit on A (B had one at 22:00 for T-0170); status() showed cooling 0 minutes later (subscription window reset), so both were re-spawned 22:12; T-0169's worktree kept the partial edits. Lesson: a fix round whose branch carries unreviewed higher-complexity work must not be complexity ≤3, or the daemon merges it unreviewed; give it the parent's complexity. Note: .claude/skills/orchestrate is a symlink to skills/planner/orchestrate, so the 'two copies' test in T-0121 is trivially true.
- Fix candidate not yet specced: dispatch loop `break` on zero slots skips later spec reviews (gotchas.md 17:30); fold into D3 T-0122 or a c2 task after D1 merges (both touch daemon.py dispatch()).
- T-0117 C-O5 superseded 21:05 by T-0165 C-O5 v2 (c5, deps T-0079, T-0116 marked merged): spec review T-0162 risks folded in (UID/GID build args + host chown, version pins, objective secret grep, protected-paths.txt gains the five credential mount paths; 7 files). Spec review waived after one round (Planner). T-0165 done c1155c4 (22:05), gate green; opus security review T-0171 (1.04 USD) request_changes with 10 findings: ${ORCH_CREDS} inert in protected-paths (guardrails expands ~ only), no external network so kgpt cannot reach the service, repos.toml/pool.toml baked into the image (high); unguarded ${ORCH_CREDS}, runbook step order, arm64 vs x86_64 asset, no checksums/digest pin, ./work ownership, .dockerignore leaks .git and operator state (med); hardening (low). C-O5 fix round 1 T-0173 created 22:35 (c5, deps T-0165, wt from task/T-0165, pre-stamped, spec review waived as a fix round). First run (923 s, 1.36 USD) edited all five files but exited waiting on a background docker build, nothing committed; requeued and re-run 23:40; the second run did the same (emulated amd64 build outlives the executor). Planner committed the worktree as found = 94d5e32 (23:55) and runs `docker build --platform linux/amd64` itself in the background as the external gate (log in the scratchpad); opus security review of c1155c4..94d5e32 by hand. The Planner's amd64 build FAILED on this Mac (Bun installer segfaults under emulation, no AVX; gotcha recorded); arm64 native build worked for T-0165. Opus review T-0176 (2.07 USD) request_changes: .dockerignore `.mcp*.json` strips the four tracked role configs so goals in the container start with no MCP servers (high); work/ not excluded and work/ config/ not gitignored; `export GID` no-op and UID read-only in bash; host curl to container DNS wrong; platform advice redundant; Compose v2.24+ prerequisite. C-O5 fix round 2 T-0178 created 00:25 (c5, deps T-0173, wt from task/T-0173, pre-stamped, spec review waived): all six plus multi-arch (no platform pin, codex asset per arch), native build per host, compose .env with ORCH_UID/ORCH_GID. T-0178 done 156e120 (00:55), gate green, native arm64 build evidence recorded (image 1.61 GB disk, uv 0.12.16 / claude 2.1.276 / codex 0.155.0 / gh 2.101.0, .mcp.*.json present); opus review T-0181 request_changes: no git identity in the container so every goal's scaffold commit fails (high); /opt/orchestrator writable by orch and outside protected paths; executor.env template lacks GH_WRITE_TOKEN; installer hashes logged not enforced; ~/.config parent root-owned. C-O5 fix round 3 T-0183 done 88c0798 (01:45), gate green, native build + git-identity and read-only checks recorded; opus review by hand (fourth image review; if it rejects again, escalate to the human instead of a fifth round). On approve: `merge`, mark T-0117, T-0165, T-0173, T-0178 done+merged_into goal/T-0073, sync ROOT. The amd64 build stays a runbook step on kenta-server. Lesson: tasks whose acceptance includes a multi-minute docker build need budget_turns/timeout sized for it, or the build moved to a gate script.
- T-0118 C-O7a superseded 21:05 by T-0167 C-O7a v2 (c5, deps T-0083, T-0116 marked merged): spec review T-0163 risks folded in (encode_project_dir maps every non-alnum char to '-', planner counters reset on window/day rollover, pick tallies first, offsets pruned). Spec review waived after one round (Planner). T-0167 done 2cf1d45 (21:50), gate green; opus review T-0170 (1.59 USD, after a rate-limit retry) request_changes: out-of-window lines dropped from the day counter too (high); pick tracebacks on tally errors, prints an unexpanded ~ config_dir, missing-dir warning every tick, hold message on stdout, planner state in the raced pool_state blob, no end-to-end pick test. C-O7a fix round 1 T-0174 created 22:45 (c5, deps T-0167, wt from task/T-0167, pre-stamped, spec review waived as a fix round). T-0174 done 8c65a97, opus review T-0175 (1.58 USD) APPROVED with low notes (missing-dir continue skips the rollover save; tally still ends in pool.save(); now= only half honoured; null usage values raise; planner_usage.json lacks its day/window anchors). Merged f5d2a16 into goal/T-0073 at 23:25; T-0118 and T-0167 marked done+merged. ROOT synced. Backlog: fold the T-0175 notes into a C-O7 polish task. On approve: merge by hand, mark T-0118 done+merged_into goal/T-0073 (T-0119 C-O7b depends on it), sync ROOT.
- T-0119 C-O7b `handover` command (c4, deps T-0118 marked merged): done 41cf86a (00:10), gate green; opus review T-0177 (0.93 USD) request_changes: failed children vanish from the snapshot (high); non-atomic plan.md write; throttle timestamp in the raced pool_state; first-heading match can delete Planner prose; per-goal bus.read under the lock; worktree list includes _state and merged tasks. C-O7b fix round 1 T-0179 done 3657876; opus review T-0180 (1.51 USD) APPROVED with notes (daemon handover dirties the tracked plan.md in ROOT every 15 min; throttle check-then-act outside the flock; handover_state.json not gitignored; section-wide truncation branch untested). Merged fdf9d2f into goal/T-0073 at 01:05; T-0119 marked done+merged. Backlog: gitignore handover_state.json, throttle under the flock. then D3 T-0122 / D4 T-0123 become ready (D4 should add: handover refuses while an execute is running, per the orphan gotcha).
- Then: retrospective for T-0073 (record.sh draft T-0073), close the GOAL task (`orchestrator post T-0073 --summary ...`), PR goal/T-0073 → main.
Phase D — goal T-0109 (branch goal/T-0109 from goal/T-0073): D1 T-0120 review policy (deps T-0102, T-0115) · D2 T-0121 scout budget (deps T-0115) · D3 T-0122 planner_runs decision points (deps T-0115, T-0119, T-0102) · D4 T-0123 session rules incl. 150k restart (deps T-0115, T-0119) · D5 T-0124 scorecard by task/goal (deps T-0116→T-0142, T-0118). Merge target goal/T-0109; when a D task and a C task both touch daemon.py, the C one goes first.
kgpt — its own bus at /Users/k3ntaw/code/kgpt/.orchestrator (ORCH_ROOT=/Users/k3ntaw/code/kgpt). GOAL T-0001; T-0002 ruff exclude merged; Phase B T-0003..T-0011 (B0b-api, B0b-web, B0, B1, B2a, B2b, B3, B4, B5) queued with depends_on; backlog goals T-0012 computer use, T-0013 Higgsfield-style studio. Branch goal/T-0001 pushed to origin. Plan there: kgpt/.orchestrator/plan.md (Phase B facts and specs, B0b, Phase C kgpt side C-K1..C-K4, backlog). A kgpt Planner session starts from the kgpt root with ORCH_ROOT set; its daemon autostarts with the MCP server.

## Human items outstanding
- Merge PR 6 (orchestrator install) when reviewed: https://github.com/K3NTAW/orchestrator/pull/6
- GitHub App: add permission "Pull requests: read and write"; private key + App ID via `make secrets-edit` on the Hetzner box (handbook 15-github step 2); no webhook, no callback URL; client secret unused.
- Executor host decided: own container on kenta-server; C-O5 writes the runbook; logins (`claude setup-token` ×2, `codex login --device-auth`, `gh auth login`) are human steps, tokens only in the gitignored executor.env on the server.
- Launcher (protected config dir): start the Planner on the account with headroom; `orchestrator pick planner` arrives with C-O7a. Account A's config dir now holds the second subscription after the /login on 2026-09-18.

## Phase C architecture (decided)
Two processes. (1) kgpt `modules/orchestrator`, thin MCP module on the shared image, runs_on home, owns per-user `orchestrator_connections` (endpoint_url, label, encrypted bearer), tools list_goals/goal_status READ and start_goal/cancel_goal WRITE_EXTERNAL (proposals), forwards to the user's endpoint with X-KGPT-User-Id applied by UserContextMiddleware. (2) `orchestrator serve` in its own container on kenta-server (ubuntu:24.04 image from this repo: claude native installer, codex release binary, uv, git, gh), one ORCH_ROOT per repo under /work, `goal start` = install+commit scaffold + GOAL task + headless Planner (`claude -p --mcp-config .mcp.planner.json --strict-mcp-config --append-system-prompt <planner.md>`), only ever opens PRs. Kernel reaches modules by URL only (kernel/kgpt_kernel/mcp/client.py:88-144); home modules gated by KGPT_HOME_AVAILABLE; ingress mcp-<name>.kentawaibel.com with an Access service-token policy.

## Measurements that drive Phase D (2026-09-18)
Planner session: 411 turns, 104M cache-read, 777k output, avg context 257k, max 446k, 112 monitor notifications, ≈11.2M by the pool metric (in + out + cache_read/10). Workers: 25 runs, 16.78 USD: scouts 4.59 (8 × sonnet, ~130 s), execute 4.56 (6 × sonnet, ~213 s), reviews opus 4.61 (4) + sonnet 1.43 (4), spec reviews 1.59. Every fallback task got two reviews. Three spec-review rounds on one task (C-O3) cost ≈3 USD and an hour; write specs against the CLI's real flags (`claude --help`: --append-system-prompt exists, -file does not) and real module boundaries.

## Gotchas recorded today (gotchas.md)
daemon never fired the Claude fallback (free_slots) → fixed C-O6/C-O6b · gate spawned same-model reviews → fixed C-O6 review_tier · spec_review missing from role_affinity → fixed in both pool.toml · rebased fix round hides the original from already_merged → manual bus.update, fix candidate open · planner-mode.sh flags any command text containing `>` or words like cp/rm/install/mv, and guardrails.sh any mention of the ssh or system config dirs, even inside ssh remote strings or record.sh facts.

## Prior art
- bus:T-0066..T-0069 kgpt scouts (projects model, github module, web client, tests/gate); T-0074..T-0077 Phase C scouts (home-module wiring, portability, connection pattern, headless auth).
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt (compose project), home `ssh k3ntaw@192.168.1.167` (fish shell, pipe scripts via `bash -s`), kgpt home side /opt/kgpt-home. Both healthy 2026-09-18 11:00; gateway saw zero chat requests in the prior 7 days. Spec review round 2: the old daemon never reached T-0142 (its dispatch loop breaks at the first ready low-complexity task when slots are 0; gotcha recorded), so Planner created spec_review T-0143 by hand (17:32) → request_changes (clone race, module-attribute import, slug on entries, requester type). T-0142 → T-0144 v3 (17:52) → spec review round 3 T-0146 (daemon duplicate T-0145 killed) still request_changes: status() list shape + missing single-goal tests, credential leak via clone stderr, no clone timeout, 'path missing' ambiguity, 409 check outside the lock. All folded into T-0147 C-O4 v4 (18:05, 18 tests); spec review waived by Planner decision after three rounds (decisions.md), verdict set approve, dispatched by hand. T-0147 done be42500 (18:45), gate green, stamped by Planner before the daemon; opus security review T-0152 (1.39 USD) request_changes: non-ASCII Authorization header → 500 (med); body/goal unbounded, dependency message, requester scoping undocumented, weak lock test, untested branches (low). T-0147 held. Fix round T-0153 done 38db9bb (19:15), gate green; opus review T-0155 (1.05 USD) request_changes: start() refusal reasons reach the body unredacted (med), body bound checked after buffering (med), repos.toml parse unguarded → 500, cancel not idempotent (low). T-0153 held. Fix round T-0158 done 6f8018d (20:30), 191 tests, gate green; opus review T-0161 by hand (third serve review; approve unless production-relevant). T-0161 approved (0.88 USD) with backlog notes; merged 6f8018d into goal/T-0073 at 20:45. Marked done+merged_into goal/T-0073 by hand: T-0116, T-0142, T-0144, T-0147, T-0153 (via T-0158). T-0117 and T-0118 are now ready; the old daemon spawns their spec reviews (c5) itself; dispatch by hand on approve. Earlier note kept for history: mark T-0153, T-0147 and T-0116 done+merged_into goal/T-0073 (merged_via T-0147) so T-0117/T-0118/T-0124 become ready, and sync ROOT.
- T-0115 C-O3 v4 goal runner (c6): done, commit abc3f56 on task/T-0115, gate green, 127 tests. Executor's worker thread was orphaned by the restart; Planner reclaimed and posted the result by hand. Daemon's sonnet review T-0127 failed by hand (same model); opus review T-0128 running on B (15:00). Opus review T-0128 (1.24 USD, 212 s) requested changes: 1 high (scaffold commit sweeps the target's index), 5 med (porcelain quoting, install before prechecks, target pool.toml secrets run through bash -c, planner budget hardcoded, untested prechecks), 3 low. T-0115 held (review request_changes).
- T-0129 C-O3 fix round 1 (c5, deps T-0115, wt from task/T-0115): executor finished (2.64 USD, 710 s) but did not commit; Planner committed the worktree as found = 6e12f51; daemon's sonnet review T-0133 killed+failed; opus review T-0134 died at the 1.5 USD review cap; retry T-0137 approved (1.53 USD) with four low notes (backlog below). T-0129 merged into goal/T-0073 as 88f7396 (T-0115 rebased as eca9354) at 16:30; T-0115 marked done+merged by hand; ROOT synced; review cap 3.0 re-applied on the merged pool.toml line (decision recorded).
- T-0130 C-O9 daemon re-gates orphaned executors (c4): done f3d1f10; opus review T-0132 request_changes (3 med: upstream fallback never resolves, orphaned results could auto-merge at c≤3, requeue leaves dispatched_at stamped so the task is never retried = pre-existing dead end; 3 low). Held.
- T-0135 C-O9 fix round 1 (c4, deps T-0130): done e712553, 124 tests; daemon gated and spawned sonnet review T-0136 → killed+failed; opus review T-0139 approved 16:32 but run_worker overwrote its posted verdict with a parse_error result (gotcha recorded), so the daemon never merged; Planner merged T-0135 by hand 16:50 = acc0894; T-0130 marked done+merged by hand; ROOT synced.
- T-0131 C-O8 merge.py syncs the checked-out main worktree (c3): merged 58a6ec4 into goal/T-0073 at 15:28; ROOT synced by hand. Live only in a server started after the merge.
- Backlog from the approving serve review T-0161 (low): `git clone` argv needs a `--` before git_url (a git_url starting with '-' or an ext:: URL would be parsed as an option); missing repos.toml should be 503 not 404; get_goal/cancel_goal/create_goal duplicate check call goals.* unguarded (only the fan-out is isolated); _safe_reason redacts only URL credentials; long flock holds inside the shared threadpool. Fold into a C-O4 polish task with the C-O3 polish items below.
- Backlog from the approving review T-0137 (low, not blocking): goals.py _preview_cfg should catch TOMLDecodeError on the target pool.toml and refuse; Popen of claude should catch FileNotFoundError after the scaffold commit; single-goal guard should run under the goals lock; cli _format_goal_line should print the note field. Fold into a C-O3 polish task after C-O4/C-O5, or into the C-O5 runbook task if it touches goals.py anyway.
- T-0138 C-O10 run_claude/run_worker failure reason + partial output (c2): done 5aff342, clean; daemon gates and auto-merges (c≤3). Then sync ROOT.
- T-0116 C-O4 serve (c6, deps T-0115 merged): spec review spawned by hand 16:55 (old daemon cannot dispatch while Codex cools). On approve: `spawn_scout("T-0116")` by hand, or let the daemon do it after a session restart (the new server loads free_slots fallback, review_tier, reconcile_dead, merge sync).
- T-0141 C-O11 verdict preservation: merged 674b390 into goal/T-0073 (17:25, auto-merge c3). ROOT synced.
- goal/T-0109 fast-forwarded to 674b390 (goal/T-0073 head) at 17:33 before any D work; D worktrees cut from it carry today's C fixes.
- Phase D: T-0121 D2 scout budget merged 1b94384 into goal/T-0109 (17:50). T-0120 D1 review policy done cf7ad84 (18:10), gate green; daemon's sonnet review T-0149 killed+failed; opus review T-0150 (1.05 USD) request_changes: orphaned+two-review deadlock, failed sibling waits forever, second review could land on the executor's model (only two Claude tiers), CLAUDE.md still says spec review from 5, merged_at on the wrong task. T-0120 held.
- T-0151 D1 fix round 1 (c4, deps T-0120): executor finished 19:00 but did not commit; Planner committed the worktree as found. Daemon gate red: test_planner_md_matches_claude_md_lists (CLAUDE.md threshold line changed, prompts/planner.md copies it and was out of scope). T-0154 fix round 2 (planner.md sync) done 4501d91; the daemon gated and tried the c2 direct merge before the Planner could stamp it → rebase_conflict on README.md Pipeline (T-0121 scouts line + old sentence vs T-0120 paragraph), T-0154 failed. T-0157 fix round 3 done 20:05: rebased cleanly onto goal/T-0109 (2e32566 T-0120, 425775e T-0151, 8a82703 T-0154), README keeps the scouts line + threshold paragraph, gate green, pre-stamped. Opus review T-0159 (1.48 USD) request_changes: the failed-sibling escape only runs from a done review, so all-failed reviews still stall silently (high); avoid_account is always None and unconsumed (med); CLAUDE.md step 7 still claims cross-model for Claude-executed 7-10 (med); test asserts an inert key; README misses the orphaned exception. D1 fix round 4 T-0160 created 20:20 (c4, deps T-0157, wt from task/T-0157, six files incl. CLAUDE.md + planner.md, pre-stamped). T-0160 done 2f37982 (20:55), gate green; opus review T-0164 (1.69 USD) request_changes: an off-vocabulary review verdict makes merge_reviewed IndexError and stops merging for every task each tick (high); review count read live at merge time vs frozen at gate (med); src.review_verdict fallback unsafe with two reviews; sweep skips ungated tasks; inert second_review field. D1 fix round 5 T-0169 created 21:35 (c4, deps T-0160, wt from task/T-0160, daemon.py+tests only, pre-stamped). T-0169 done c593794, gate green; opus review T-0172 (1.46 USD) APPROVED with low notes (fewer reviews than needed + no rejections can still yield an empty hold_reason; [review] values untyped; bus.read(role=review) inside the per-task loop; vacuous avoid_account assertion). Merged c593794 into goal/T-0109 at 23:00; T-0120, T-0151, T-0154, T-0157, T-0160 marked done+merged_into goal/T-0109. D1 total: 5 execute rounds + 4 opus reviews (1.05+1.48+1.69+1.46 = 5.68 USD). Backlog: fold the T-0172 notes into D3 T-0122 (daemon.py) or a small D-polish task. D1 review spend so far: T-0150 1.05 + T-0159 1.48 + T-0164 1.69 = 4.22 USD; D1 is the costliest task of the day. If round 5 is rejected again: escalate to the human (CLAUDE.md: executor fails 3 rounds). 22:02: T-0169's executor hit a rate limit on A (B had one at 22:00 for T-0170); status() showed cooling 0 minutes later (subscription window reset), so both were re-spawned 22:12; T-0169's worktree kept the partial edits. Lesson: a fix round whose branch carries unreviewed higher-complexity work must not be complexity ≤3, or the daemon merges it unreviewed; give it the parent's complexity. Note: .claude/skills/orchestrate is a symlink to skills/planner/orchestrate, so the 'two copies' test in T-0121 is trivially true.
- Fix candidate not yet specced: dispatch loop `break` on zero slots skips later spec reviews (gotchas.md 17:30); fold into D3 T-0122 or a c2 task after D1 merges (both touch daemon.py dispatch()).
- T-0117 C-O5 superseded 21:05 by T-0165 C-O5 v2 (c5, deps T-0079, T-0116 marked merged): spec review T-0162 risks folded in (UID/GID build args + host chown, version pins, objective secret grep, protected-paths.txt gains the five credential mount paths; 7 files). Spec review waived after one round (Planner). T-0165 done c1155c4 (22:05), gate green; opus security review T-0171 (1.04 USD) request_changes with 10 findings: ${ORCH_CREDS} inert in protected-paths (guardrails expands ~ only), no external network so kgpt cannot reach the service, repos.toml/pool.toml baked into the image (high); unguarded ${ORCH_CREDS}, runbook step order, arm64 vs x86_64 asset, no checksums/digest pin, ./work ownership, .dockerignore leaks .git and operator state (med); hardening (low). C-O5 fix round 1 T-0173 created 22:35 (c5, deps T-0165, wt from task/T-0165, pre-stamped, spec review waived as a fix round). First run (923 s, 1.36 USD) edited all five files but exited waiting on a background docker build, nothing committed; requeued and re-run 23:40; the second run did the same (emulated amd64 build outlives the executor). Planner committed the worktree as found = 94d5e32 (23:55) and runs `docker build --platform linux/amd64` itself in the background as the external gate (log in the scratchpad); opus security review of c1155c4..94d5e32 by hand. The Planner's amd64 build FAILED on this Mac (Bun installer segfaults under emulation, no AVX; gotcha recorded); arm64 native build worked for T-0165. Opus review T-0176 (2.07 USD) request_changes: .dockerignore `.mcp*.json` strips the four tracked role configs so goals in the container start with no MCP servers (high); work/ not excluded and work/ config/ not gitignored; `export GID` no-op and UID read-only in bash; host curl to container DNS wrong; platform advice redundant; Compose v2.24+ prerequisite. C-O5 fix round 2 T-0178 created 00:25 (c5, deps T-0173, wt from task/T-0173, pre-stamped, spec review waived): all six plus multi-arch (no platform pin, codex asset per arch), native build per host, compose .env with ORCH_UID/ORCH_GID. T-0178 done 156e120 (00:55), gate green, native arm64 build evidence recorded (image 1.61 GB disk, uv 0.12.16 / claude 2.1.276 / codex 0.155.0 / gh 2.101.0, .mcp.*.json present); opus review T-0181 request_changes: no git identity in the container so every goal's scaffold commit fails (high); /opt/orchestrator writable by orch and outside protected paths; executor.env template lacks GH_WRITE_TOKEN; installer hashes logged not enforced; ~/.config parent root-owned. C-O5 fix round 3 T-0183 done 88c0798 (01:45), gate green, native build + git-identity and read-only checks recorded; opus review by hand (fourth image review; if it rejects again, escalate to the human instead of a fifth round). On approve: `merge`, mark T-0117, T-0165, T-0173, T-0178 done+merged_into goal/T-0073, sync ROOT. The amd64 build stays a runbook step on kenta-server. Lesson: tasks whose acceptance includes a multi-minute docker build need budget_turns/timeout sized for it, or the build moved to a gate script.
- T-0118 C-O7a superseded 21:05 by T-0167 C-O7a v2 (c5, deps T-0083, T-0116 marked merged): spec review T-0163 risks folded in (encode_project_dir maps every non-alnum char to '-', planner counters reset on window/day rollover, pick tallies first, offsets pruned). Spec review waived after one round (Planner). T-0167 done 2cf1d45 (21:50), gate green; opus review T-0170 (1.59 USD, after a rate-limit retry) request_changes: out-of-window lines dropped from the day counter too (high); pick tracebacks on tally errors, prints an unexpanded ~ config_dir, missing-dir warning every tick, hold message on stdout, planner state in the raced pool_state blob, no end-to-end pick test. C-O7a fix round 1 T-0174 created 22:45 (c5, deps T-0167, wt from task/T-0167, pre-stamped, spec review waived as a fix round). T-0174 done 8c65a97, opus review T-0175 (1.58 USD) APPROVED with low notes (missing-dir continue skips the rollover save; tally still ends in pool.save(); now= only half honoured; null usage values raise; planner_usage.json lacks its day/window anchors). Merged f5d2a16 into goal/T-0073 at 23:25; T-0118 and T-0167 marked done+merged. ROOT synced. Backlog: fold the T-0175 notes into a C-O7 polish task. On approve: merge by hand, mark T-0118 done+merged_into goal/T-0073 (T-0119 C-O7b depends on it), sync ROOT.
- T-0119 C-O7b `handover` command (c4, deps T-0118 marked merged): done 41cf86a (00:10), gate green; opus review T-0177 (0.93 USD) request_changes: failed children vanish from the snapshot (high); non-atomic plan.md write; throttle timestamp in the raced pool_state; first-heading match can delete Planner prose; per-goal bus.read under the lock; worktree list includes _state and merged tasks. C-O7b fix round 1 T-0179 done 3657876; opus review T-0180 (1.51 USD) APPROVED with notes (daemon handover dirties the tracked plan.md in ROOT every 15 min; throttle check-then-act outside the flock; handover_state.json not gitignored; section-wide truncation branch untested). Merged fdf9d2f into goal/T-0073 at 01:05; T-0119 marked done+merged. Backlog: gitignore handover_state.json, throttle under the flock. then D3 T-0122 / D4 T-0123 become ready (D4 should add: handover refuses while an execute is running, per the orphan gotcha).
- Then: retrospective for T-0073 (record.sh draft T-0073), close the GOAL task (`orchestrator post T-0073 --summary ...`), PR goal/T-0073 → main.
Phase D — goal T-0109 (branch goal/T-0109 from goal/T-0073): D1 T-0120 review policy (deps T-0102, T-0115) · D2 T-0121 scout budget (deps T-0115) · D3 T-0122 planner_runs decision points (deps T-0115, T-0119, T-0102) · D4 T-0123 session rules incl. 150k restart (deps T-0115, T-0119) · D5 T-0124 scorecard by task/goal (deps T-0116→T-0142, T-0118). Merge target goal/T-0109; when a D task and a C task both touch daemon.py, the C one goes first.
kgpt — its own bus at /Users/k3ntaw/code/kgpt/.orchestrator (ORCH_ROOT=/Users/k3ntaw/code/kgpt). GOAL T-0001; T-0002 ruff exclude merged; Phase B T-0003..T-0011 (B0b-api, B0b-web, B0, B1, B2a, B2b, B3, B4, B5) queued with depends_on; backlog goals T-0012 computer use, T-0013 Higgsfield-style studio. Branch goal/T-0001 pushed to origin. Plan there: kgpt/.orchestrator/plan.md (Phase B facts and specs, B0b, Phase C kgpt side C-K1..C-K4, backlog). A kgpt Planner session starts from the kgpt root with ORCH_ROOT set; its daemon autostarts with the MCP server.

## Next session: Phase D and closing Phase C
- Phase C is code-complete (02:00). Close-out done in this session if you see it below: retrospective entry in decisions.md, GOAL task T-0073 posted with the PR url, PR goal/T-0073 → main opened for the human. If any of those is missing, do it first. Then Phase C is code-complete: retrospective (record.sh draft T-0073), close the GOAL task (`orchestrator post T-0073 --summary ...`), open PR goal/T-0073 → main (human merges).
- Backlog tasks to spec (small, c2-3, on goal/T-0073 before the PR or after): C-O3/C-O4 polish (T-0137, T-0161 notes: TOMLDecodeError guards, git clone `--`, missing repos.toml as 503, per-route guards), C-O7 polish (T-0175 notes), handover polish (T-0180 notes: gitignore handover_state.json, throttle under the flock), dispatch loop `break` → `continue` (gotcha 17:30), spawn tools refuse ids of the wrong role, D1 T-0172 notes.
- Phase D: goal/T-0109 has D1+D2 (6 commits) and lags goal/T-0073 by 10 commits (serve, pool tally, handover). Before D3–D5: a c2 execute task rebases goal/T-0109 onto goal/T-0073 (like T-0157 did for a task branch; conflicts expected only in README/pool.toml comments), or the human decides to merge goal/T-0109 into goal/T-0073 and continue D on one branch. D3 T-0122 (c6), D4 T-0123 (c3), D5 T-0124 (c4) are ready by depends_on but their specs predate C-O7a/C-O7b: re-read them against handover.py, pool.tally_planner and goals.py before dispatch; D4 must add "handover refuses while an execute is running" and the 150k/no-worker restart rule; D5 must read planner usage from planner_usage.json.
- Session 2 cost picture (2026-09-18 14:40 → 01:10): about 14 sonnet executes and 22 opus reviews; reviews again cost as much as execution, and every non-trivial task needed one to five fix rounds. Consider raising the executor tier to opus for daemon.py/serve.py work, or waiting for Codex (back ~14:00) for the D tasks.
