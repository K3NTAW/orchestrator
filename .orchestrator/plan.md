# plan.md — Planner checkpoint (updated 2026-09-20 12:50, session 5; open goal: T-0353 Phase H efficiency handover)

## OWNER 2026-09-20 16:55 — session 6410 (the resumed transcript) drives T-0353; the launcher session (planner_session.json pid 7181, process 7155) ceded at 16:49 by superseding its own T-0437 as a duplicate of T-0435 and must not create tasks or mark chains
Both sessions yielded in the same minute (16:49 to 16:50): the launcher session failed T-0437 and T-0438 as duplicates; this session failed T-0435 in favour of T-0437. Tie-break: the session whose spec actually received the round-3 review (T-0436 on T-0435) continues; the other stays idle. H3b v4 (complexity 5, spec review waived after rounds T-0430, T-0434, T-0436 with every risk folded in) is the live task. State: nine of ten Phase H tasks merged on goal/T-0353 (head df2ae89, 42 commits over 2ca1517): H1, H2, H3a, H4, H5a, H5b, H6, H7, H8, every lineage marked. Open: H3b v3 = T-0437 (written by the registered session; T-0365, T-0433 and T-0435 are superseded predecessors, mark all three done+merged_into when T-0437 merges so H9 T-0366 becomes ready), spec review T-0438 pending (round 3; if it rejects, v4 at complexity 5 with the review waived per the Phase G rule); T-0436 (spec review of the superseded T-0435) can be ignored. Then H9 T-0366 docs, retrospective (record.sh, cost from scorecard --by goal T-0353 and --planner), PR 12 goal/T-0353 to main listing PR 11 as base, GOAL result with the PR url. After Phase H: the user's request of 15:23, a self-contained HTML page for the docs.kentawaibel.com orchestrator entry with diagrams of every system and dependency, deployed to the docs repo (goal start there) and published as a shareable artifact. Escalation note for the PR: the H4 reservation lineage took nine rounds plus two rebases (every review a distinct defect); Codex shipped code without its named tests nine times this goal (H6 makes that a gate_red once the server runs Phase H code).

## GOAL T-0445 Phase I: efficiency and Jev decision layer (opened 2026-09-20 17:35, complexity 9, owner = launcher session 7155; session 6410 finishes Phase H and PR 12 only, then stays idle)
Brief: .orchestrator/phase-i-program.md (user text). Priority order P0 telemetry -> P1 packets -> P2 fix-round economics -> P3 Jev shadow routing -> P4 evaluation -> P5 active (evidence only) -> P6 review packets -> P7 independent decision-point experiments. Base: goal/T-0353 head (0d7bf09, H9 merged); goal branch goal/T-0445 to be cut after PR 12 opens. Three sonnet scouts on account B against the read-only checkout wt/goal-T-0353 (three because the brief spans three independent subsystems): T-0446 telemetry inventory and P0 gap table, T-0447 packets and fix-round paths (P1, P2), T-0448 routing and Jev seams (P3-P5, safety points). Next: read the three results once into a matrix here, write the P0 specs (attribution fields, metrics, amplification, baseline snapshot), dispatch through the daemon, measure, then P1.
Docs goal (done 17:30): docs repo goal T-0007 merged on goal/T-0007 at e9dace9, PR 2 https://github.com/K3NTAW/docs-kentawaibel/pull/2 (base PR 1), artifact https://claude.ai/artifact/WVgUMFJ1eWxFGTqVTxdUtt, Vercel preview via the GitHub integration. Lessons: the one-shot headless goal Planner exits when it waits (2.44 USD lost); a fresh worktree has no node_modules so the first gate is environment-red; the Planner may not edit another repo's .orchestrator/pool.toml (autonomous stays false there).

## State in one paragraph
Open goal: T-0353 Phase H, complexity 8: implement /Users/k3ntaw/Documents/orchestrator-efficiency-handover.md on top of goal/T-0260 (Phase G, PR 11 unmerged). Goal branch goal/T-0353 cut from goal/T-0260 head 2ca1517 at 12:48; a read-only checkout of that head sits at wt/goal-T-0260 for scouts (the running server is still Phase F code on goal/T-0240, so scout worktrees are stale and every spec says READ wt/goal-T-0260). Two sonnet scouts on account B are running the gap matrices: T-0354 (handover sections 2-5) and T-0355 (sections 6-11). Next: summarize both once into the matrix below, decide the core changes, write atomic specs with parent T-0353 (the daemon dispatches them into goal/T-0353), then PR 12 goal/T-0353 to main. Phase G closed 2026-09-19 23:56 (PR 11). docs.kentawaibel.com is live (see below).

## Phase H: premium usage evidence (measured, 2026-09-20 12:45)
- Interactive Planner (Fable, account A): planner_day_tokens 35.2M on 2026-09-19 (status() at 23:24), over the 30M daily budget; this session 5 alone ran Phase G's tail, the docs goal and now Phase H. Zero headless decision runs recorded in this root (no .orchestrator/planner_runs.json). One headless goal Planner (docs repo, Fable, account B): 6.17 USD, 1.0M cache-read tokens, 11 min, 652 s API time.
- Workers, 3 days (runs/2026-09-18..20, usd is API-equivalent): execute 128 rows 78.19 USD (in 45.8M, out 2.2M, cache_read 230M; mostly Codex), review 83 rows 67.98 USD (cache_read 52M), spec_review 21 rows 13.61 USD, scout 11 rows 6.12 USD.
- Reading: the premium bottleneck is the interactive Fable session doing bookkeeping the merged Phase G code already automates (marking chains merged, posting Codex results, re-spawning reviews, resetting slot counters, writing fix rounds by hand). The single largest lever is running goal/T-0260 code in the server; Phase H must not rebuild what G7 v4, G6 v4 and G10 already merged.
- Known gaps from session 5 gotchas (all on goal/T-0260): executor running counters persisted and leaking; codex/codex_reply MCP tools do not post results; requeued reviews never re-dispatched; gate does not check acceptance-named tests exist (Codex skipped them 5 times); plan.md rewritten by the daemon every 15 min (competing writer).

## Phase H gap matrix (from scouts T-0354 and T-0355, read once; code = goal/T-0260 at 2ca1517)
Existing and verified: token bucket normalisation incl. cached subset vs separate and no reasoning double count (bus.py:206-229, pool.py:232-313); decision identity per held event and atomic claim under the bus lock (planner_runs.py:189-196, 271-284); stage stamps claim/complete/clear under lock and already_merged idempotency (daemon.py:248-286, 397-428); single-daemon lock so a second account cannot double dispatch (daemon.py:1063-1074); premium headroom via reserve_for_planner (pool.py:209); packet expansion handles and visible truncation markers, empty distinct from truncated (spawn.py); role prompts 94 lines over 9 files, no duplicated contract text; recall shows provenance and age; automatic repair only in scope with lineage and merge-verified reconciliation (daemon.auto_fix_round, report_merge); retry budgets configurable ([daemon].auto_fix_rounds, executor MAX_ROUNDS); review approval bound to reviewed sha and re-assessed after rebase, semantic triggers (G8); Jev sample mode 10 percent (pool.toml:216), 608 gate rows / 190 KB so far.
Partial: run rows lack attempt, decision kind/key, client version, policy version, route reason (bus.py:232-245) -> H1; spend vs API-equivalent vs quota not labelled -> H1 docs; premium invocations only count+usd (scorecard.py:622-632), launch record lacks reason/evidence/cheaper steps (planner_runs.py:271-317) -> H2; bus.events cursor exists but no role filter and no caller persists a cursor -> H8, H3b; infra failures share the 2-strike gave_up with real failures -> H3b; notify fires every tick while a condition holds (daemon.py:1052-1059) -> H4; acceptance summarised to 5 bullets when the packet overflows -> H8; failure kinds only gate_red vs review -> H7; hold dedupe on held_at timestamp not a content signature -> H7; codex_reply resume has no worktree compatibility check -> H7.
Missing: routine/investigate/escalate routing (no decision.py; daemon.py:1044-1049 launches unconditionally) -> H3a, H3b; coalescing of held tasks into one packet -> H3b; pinned config/prompt version per run (goals.py:322-329 re-reads pool.toml fresh by design) -> H2; token-amount budget reservations across parallel dispatch (daemon.free_slots counts slots only) -> H4; tokens per accepted goal returns 0 at zero accepted (scorecard.py:598-603) and no median/tail -> H1; packet hash/version/provenance -> H8; gate does not check acceptance-named tests exist (5 Phase G cases) -> H6; executor running counters leak (persisted increments) and codex tools do not post results -> H5a; requeued reviews never re-dispatched, stray task branch blocks spawn, handover rewrites plan.md on unchanged state (6 times on 2026-09-19) -> H5b.
Unnecessary or deferred with reason: Jev changes (judge cost vs savings unmeasured; sample mode already in force; G5 diagnose exists to build the offline sample first); read-only result cache and test-result cache (no measured duplicate-read or duplicate-test cost; worktree and post-rebase gates cover different states); memory expiry/superseded marks (compact-memory skill exists, volume small); Codex sandbox-boundary scope enforcement (no observed violation; post-run scope diff exists); learned routing (G9 rules exist, sample too small); expansion-read metrics (needs worker tool logs; Jev gate rows could serve later); interactive session exclusion is a pid-liveness check already (planner_runs.py:243-263), functionally a lease.
Regression cases: covered already: idle ticks zero launches (test_tick_launches_nothing_when_autonomous_false), concurrent claim once (test_run_claims_before_launch_so_concurrent_callers_launch_once), usage accounting (test_normalize_usage_*, test_null_usage_counts_zero, test_tally_*), crash reconciliation (test_keyboard_interrupt_releases_claim_and_reraises, reconcile_dead tests), lineage reconciliation and review re-assessment (G7, G8 tests). New in Phase H: reservations (H4), routes and soft budget (H3a/H3b), same-state no relaunch (H3b), unchanged repeated failure stops (H7), truncated results expandable and cursor filters (H8), missing acceptance tests cannot self-certify (H6).

## Phase H task map (all parent T-0353, goal branch goal/T-0353 from goal/T-0260; state 13:32)
- Merged on goal/T-0353: H3a T-0357 (approve, first try); H1 T-0356 via fix round T-0370 (review T-0367: cache-read discount bypassed, legacy detection); H5a T-0358 via T-0371 and T-0378 (reviews T-0368, T-0376: reply rounds dropped, unlabeled sha parse, claim on a done task); H6 T-0359 via T-0373 and T-0380 (gate_red: test isolation leak; review T-0377: no-worktree TypeError); H2 T-0361 at 68ff495 by Planner decision over review T-0384 (its finding is the route wiring H3b delivers). Every review found a real defect; Codex claimed green gates twice while red (T-0374, T-0379, T-0382 all reported the same failing daemon test and committed anyway).
- H8 T-0360 merged 14:11 at 6b773d8 via rebase task T-0388 after a six-task chain: T-0374 (review T-0372 non-atomic double query) -> T-0379 (its two tests failed) -> T-0382 (same daemon test) -> T-0385 (Codex bisected in scope, no commit: the leak is tests.test_cli's autostart thread, not the bus tests) -> T-0386 (stop_event threaded into tick; approved; rebase conflict in tests/test_bus.py against H1) -> T-0388 rebase. Lesson recorded in gotchas: full-suite failures that pass alone need an ordered-module run before a fix round.
- H4 T-0362 chain (15:05): T-0362 (no tests, codex_reply added them at 530c2ab) -> T-0392 (review T-0390: role_cap read from the wrong table, reservation leak on exhausted hold) -> T-0395 (gate_red: per-run cap misused as goal cap refused every second parallel dispatch) -> T-0398 (review T-0397: reservation owner mismatch, zero estimate, stamp-failure leak, Pool per dead task) -> T-0400 (gate_red: worker signature broke an existing mock) -> T-0404 running (review T-0402: finally released the reservation on the fallback hand-off). Every review found a new defect; the reservation ledger is the hardest Phase H piece.
- H5b T-0363 chain (15:05): T-0363 (no tests, codex_reply added them at 3164bed) -> T-0393 (review T-0391: guard equivalent to if pipeline) -> T-0396 (review T-0394: global event page for a per-task question, double update) -> T-0401 (review T-0399: duplicated Claude slot counting) -> T-0405 queued (review T-0403: no re-read under lock, no stale check).
- H5b merged 15:18 at 9b058ab via T-0405 (chain marked). H4 lineage continued: T-0404 (review T-0402: finally released the reservation on the fallback hand-off) -> T-0408 (review T-0406: fallback thread never joined, intermittent gate) -> T-0411 running 15:29 (review T-0409: placeholder account ids codex/claude break the exhausted path). ESCALATION NOTE for the human: the H4 lineage failed the gate criterion three times and is at seven rounds, every one a distinct defect in the dispatch-to-worker reservation hand-off; the Planner decided in T-0411 to remove that hand-off (the worker that starts a run reserves for exactly its executor, dispatch only checks slots), the simpler variant the T-0398 spec already allowed. If T-0411's review still rejects, hold H4 and ship Phase H without reservations behind [limits].reservations = false.
- H4 continued (16:35): T-0411 (review T-0413: Pool.save overwrote the locked ledger) -> T-0415 approved, rebase conflict vs H5b -> T-0419 rebase (review T-0421: Codex rows have no usd, reconcile_dead drops cost) -> T-0423 (tests via codex_reply at 2b1d941, ninth instance) -> T-0427 confirm round, approved, rebase conflict vs H7 -> T-0431 rebase 2 (external gate green), review T-0432 running. When it merges: mark T-0427, T-0423, T-0419, T-0415, T-0411, T-0408, T-0404, T-0400, T-0398, T-0395, T-0392, T-0362.
- H7 merged 16:27 at ccaa601 via T-0425 (chain marked: T-0364, T-0412, T-0414, T-0418, T-0422). Lineage lessons: rerun timeout, runner probed in the worktree with its own timeout, validated rerun ids (argument injection), rerun cap honoured.
- H4 merged 16:44 at df2ae89 via rebase 2 T-0431 (twelve-task lineage marked). Nine of ten merged; goal/T-0353 = 42 commits over 2ca1517.
- Session restart 16:36 (the Claude Code process ended; the in-process daemon and its posting threads died): review T-0432 worker dead, requeued by reconcile_dead and re-spawned by hand; spec review T-0434's worker survived but its posting thread was gone, the daemon requeued and re-spawned it; leaked slot counters reset (codex 5, astra 5, terra 2, sol 2 with nothing running). Two orchestrator.mcp processes now run (16:36:14 and 16:36:24); the daemon lock keeps one daemon.
- H3b: v1 T-0365 superseded after spec review T-0430 (ctx fields never populated, unstamped side effects, two dedup schemes, cursor timing); v2 T-0433 superseded after spec review T-0434 (daemon import cycle, coalesced held packets need per-task sections, close gate must run off-tick after the dedup check, gh pr create failure handling, cursor location); v3 T-0435 (failures.py module, off-tick gate) rejected by spec review T-0436 and its duplicate T-0437 superseded; v4 T-0439 (c5, review waived, written by session 6410) running on Codex since 16:50; the launcher session's duplicate v4 T-0440 failed as superseded. When v4 merges: mark T-0365, T-0433, T-0435, T-0437 done+merged_into so H9 T-0366 becomes ready.
- H7 T-0364 (16:12): tests added by codex_reply at fb696ce (eighth instance) -> T-0412 (review T-0410: rerun without timeout) -> T-0414 (gate_red: assertion on the old runner command) -> T-0418 (review T-0416: min(1, rerun_max) ignored the knob) -> T-0422 running (review T-0420: unvalidated failure text as runner argv, runner probed in the daemon interpreter instead of the worktree). When it merges: mark T-0418, T-0414, T-0412, T-0364; then H3b T-0365 (c6, spec review first), then H9 T-0366.
- Queued user request (2026-09-20 15:23): after Phase H, rebuild the docs.kentawaibel.com orchestrator entry as a self-contained HTML page with diagrams of every system and their dependencies, deploy it, and publish it as a shareable artifact. Runs as a goal in the docs repo.
- Then: H7 T-0364 (after T-0363), H3b T-0365 (c6, spec review; after T-0357, T-0361, T-0364), H9 T-0366 docs (after all), retrospective, PR 12.
- Measurement plan: baseline = Phase G (22.38 USD, 82 tasks, 5.96M tokens per accepted goal over 9 goals, interactive Fable 35.2M tokens on 2026-09-19). Phase H runs on the Phase F server, so after-results for the new code come from tests and policy replay only (label: estimate); the measured after-result is Phase H's own scorecard row. Report both in PR 12.

## Phase G (T-0260) closed 2026-09-19 23:56
All eleven sub-goals merged on goal/T-0260 (head 2ca1517, 37 commits over goal/T-0240), tests-green exit 0 externally, PR 11 https://github.com/K3NTAW/orchestrator/pull/11 open for the human after PR 10, retrospective in decisions.md. Cost 22.38 USD, 82 tasks, review 62.8 percent.

## docs.kentawaibel.com (LIVE 2026-09-20 00:30)
Repo /Users/k3ntaw/code/docs-kentawaibel (private GitHub K3NTAW/docs-kentawaibel, PR 1 goal/T-0001 to main open). Headless Planner on account B: 11 min, 6.17 USD, five Codex tasks, zero fix rounds. Vercel project docs-kentawaibel (linked to the GitHub repo, production branch main), domain docs.kentawaibel.com, production deploy from goal/T-0001 at ebf4a11. Merging PR 1 triggers the first automatic deploy.

## Read first
1. `bus_read(status_not="done")` then skill `resume`. Live: T-0353 and its children; the rest are superseded/failed leftovers.
2. Planner commits from the main checkout (goal/T-0240): stage only .orchestrator/ paths; commit with `git commit -F <scratchpad file>`. planner-mode.sh scans the whole command text: keep backticks, `>`, `<`, and the words it treats as writes (cp, rm, mv, install, tail with a pipe, git rebase/checkout/reset/revert, .env) out of Bash text. Use absolute paths. macOS has no `timeout`; use run_in_background.
3. Tool contracts: `spawn_scout(<scout id>)`; `spawn_review(<review task id>)`; `spawn_spec_review(<spec_review task id>)`; `merge(task_id, target)` skips the review policy. `codex(task_id, prompt)` and `codex_reply(task_id, delta)` return the result dict and do NOT post it: bus.post_result by hand. Specs are immutable: a fix round is a new task with constraints.fix_round_for; mark the chain merged by hand (bus.update(original, status='done', merged_into=..., merged_via='fix round <id> <sha>', hold_reason=None)) until G7 v4 runs in the server.
4. Before trusting a Codex result whose acceptance names test ids: `git diff --stat HEAD~1` in the worktree must touch tests/; if not, one codex_reply listing the missing ids fixes it.
5. Slot counters: pool_state.json running fields leak when a restart kills a Codex process; reset only when pgrep shows no codex exec and no claude -p worker.
6. A review that shows status queued with a "process died; requeued" event never runs again by itself: spawn_review(<id>) by hand.
7. Restart rule: hand over at ~150k context or when an account's daily budget is reached, only when no worker is alive. Keep Planner turns short; this goal's own objective is fewer Fable tokens.
8. The daemon appends an "Auto-handover" section to this file every 15 min while a goal is open; drop it when rewriting.
9. Goal branch for T-0353 is goal/T-0353 (from goal/T-0260); execute tasks with parent T-0353 base on it. When Phase H closes: PR 12 goal/T-0353 to main, listing PR 11 as its base.

## Human items outstanding
- Merge PRs 6, 7, 8, 9, 10, 11 in order; PR 1 in the docs repo. PR 12 (Phase H) follows.
- Decisions: latency target, security_paths width, daemon as a process (Phase F review); review budget (Phase G: 22.38 USD, review 63 percent, every review found a defect).

## Backlog (c2-c3, after PR 11) — candidates for Phase H specs
- Pool load derives executor running counters from bus tasks; drop the legacy codex mirror.
- codex and codex_reply MCP tools post the result to the bus.
- dispatch re-spawns queued review and spec_review tasks whose pipeline is empty.
- Gate checks that every tests/...::name in the acceptance resolves to a defined test.
- Consistent total_tokens for pre-G1a scorecard rows; recall ranking relevance.
- kgpt side of the orchestrator module (C-K1..C-K4) on the kgpt bus.

## Phase C architecture (decided)
Two processes. (1) kgpt `modules/orchestrator`, thin MCP module on the shared image, runs_on home, owns per-user `orchestrator_connections`, tools list_goals/goal_status READ and start_goal/cancel_goal WRITE_EXTERNAL, forwards to the user's endpoint with X-KGPT-User-Id. (2) `orchestrator serve` in its own container on kenta-server, one ORCH_ROOT per repo under /work, `goal start` = install+commit scaffold + GOAL task + headless Planner launch; bearer per endpoint; per-slug locks.

## Phase D architecture (decided)
The daemon owns the loop; the Planner becomes a function of decision points (planner_runs.py): task held, scout fan-out done, goal closable. Each decision is a fresh headless `claude -p` launched by goals.launch_planner with an ids-only prompt (G10: compact decision packet), recorded in planner_runs.json, reconciled when dead, terminal gave_up after two failures. Never while an interactive session is registered in planner_session.json.

## Prior art (ids for recall.sh get)
- Phase C, D, F, G retrospectives: decisions.md 2026-09-18/19 entries (goals T-0073, T-0109, T-0240, T-0260). Gotchas of 2026-09-19 in gotchas.md.
- bus:T-0261..T-0263 Phase G scouts; T-0354, T-0355 Phase H scouts.
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt, home `ssh k3ntaw@192.168.1.167` (fish shell), kgpt home side /opt/kgpt-home.

## Auto-handover 2026-09-20T17:33:25+02:00 — daemon tick

### T-0353 GOAL Phase H: efficiency handover — gap assessment against goal/T-0260, normalized accounting completeness, premium (Fable) launch attribution and escalation routing, no-work/no-duplicate decision guarantees, budget reservations where parallel spend exists, packet and result hygiene, bounded repair stopping rules, Jev and memory conditionals, reviewable PR
- failed: T-0438 spec review: H3b v3: wire decision routes into the daemon (spec reviews T-0430 and T-0434 folded in) — build_ctx lives in daemon.py (no import cycle), coalesced held packets carry one section per non-routine held child, dedup before any gate evidence and no suite run inside tick, gh pr create failures retried then held, cursor per goal with cursor_at_launch per record (reason=not run: spec review of superseded duplicate T-0437 (T-0436 reviews T-0435), resume_hint_keys=[])
- done (not merged): T-0354 Scout H-S1 (gap matrix, handover sections 2-5): accounting completeness, premium launch attribution and escalation routes, decision identity/claims/coalescing/backoff, budget reservations and quota handling, T-0355 Scout H-S2 (gap matrix, handover sections 6-11): packets, bounded results and bus deltas, repair stopping rules and failure signatures, resume compatibility, review binding, Jev cost, memory and snapshot hygiene, scope enforcement at the sandbox boundary, T-0367 review: H1: run rows carry attempt, decision kind and key, client version, policy version and route reason; tokens per accepted goal is undefined at zero accepted; by_goal shows median and max per goal with counts, T-0368 review: H5a: executor running counters derived from bus state at Pool load (no persisted increments, legacy codex mirror dropped); codex and codex_reply MCP tools post their result to the bus, T-0369 review: H3a: orchestrator/decision.py — deterministic routine / investigate / escalate routing for decision points with persisted launch reasons, evidence and cheaper steps tried; pool.toml [planner.routes], T-0372 review: H8: worker packets carry a version hash, base sha and source provenance and never drop an acceptance criterion; bus_events takes role and task filters, T-0375 review: H1 fix round 1: _tokens_of keeps the discounted cache-read convention for normalised rows, and the legacy total-only detection matches only rows without normalised buckets (review T-0367), T-0376 review: H5a fix round 1: codex_reply rounds replace the previous round's posted result instead of being dropped, and the commit sha is parsed only from an explicit commit label or a full 40-char sha (review T-0368), … and 33 more
- merged: T-0356 H1: run rows carry attempt, decision kind and key, client version, policy version and route reason; tokens per accepted goal is undefined at zero accepted; by_goal shows median and max per goal with counts (sha=?), T-0357 H3a: orchestrator/decision.py — deterministic routine / investigate / escalate routing for decision points with persisted launch reasons, evidence and cheaper steps tried; pool.toml [planner.routes] (sha=e3b2b368), T-0358 H5a: executor running counters derived from bus state at Pool load (no persisted increments, legacy codex mirror dropped); codex and codex_reply MCP tools post their result to the bus (sha=?), T-0359 H6: the gate checks that every acceptance-named test id exists in the worktree before running the suite; missing ids are a gate_red with the ids in resume_hint.failures so the automatic fix round handles them (sha=?), T-0360 H8: worker packets carry a version hash, base sha and source provenance and never drop an acceptance criterion; bus_events takes role and task filters (sha=?), T-0361 H2: premium launch attribution — every headless Planner launch persists route, reason, decision requested, evidence and cheaper steps tried plus the pinned policy version; scorecard --planner explains premium usage (invocations, context, output, cache, cost, top reasons) for headless and interactive sessions (sha=68ff4950), T-0362 H4: pre-dispatch budget reservations with leases — parallel workers cannot start beyond an account's daily token budget or a goal's role budget; expired leases never hide a live worker's spend; quota holds produce no repair task and one notification per transition (sha=?), T-0363 H5b: ensure_worktree reuses an existing task branch and spawn_scout reports spawn errors; dispatch re-spawns queued review and spec_review tasks whose worker died; handover snapshot rewritten only when goal state changed (sha=?), … and 41 more

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 184 more

Last events:
- 2026-09-20T17:32:10+02:00 T-0444 update {"status": "done", "result": {"summary": "", "verdict": "approve", "comments": [
- 2026-09-20T17:32:10+02:00 T-0444 update {"review_verdict": "approve"}
- 2026-09-20T17:32:10+02:00 T-0366 update {"review_verdict": "approve"}
- 2026-09-20T17:32:17+02:00 T-0366 update {"pipeline": {"dispatched_at": 1789917833.026064, "gated_at": 1789918118.037653,
- 2026-09-20T17:33:24+02:00 T-0366 update {"status": "done", "merged_into": "goal/T-0353", "sha": "0d7bf091113568273ebcb33

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.
