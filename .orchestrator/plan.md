# plan.md — Planner checkpoint (updated 2026-09-21 03:30; GOAL T-0503 Adaptive Parallelism S1 CLOSED, PR 15 open for the human; no open goal on this bus)

## CLOSED 2026-09-21 03:27 — GOAL T-0503 Adaptive Parallelism S1 (brief priorities 0-4)
goal/T-0503 head 8227caf (15 commits over main 8fd67c8, 25 files, +2041), external gate 822 tests green (main 754), PR 15 https://github.com/K3NTAW/orchestrator/pull/15 -> main, GOAL result posted, retrospective in decisions.md 2026-09-21 (four gotchas the same day). Brief: .orchestrator/adaptive-parallelism-program.md. Merged: S0a T-0504 dispatch-skip telemetry + schedlog, S0b T-0505 scorecard --parallelism + baseline block, S1 T-0506 interference.py (fix round after review T-0513), S4a T-0508 graph signal, S2 v4 T-0534 waves behind [scheduler].mode=shadow (three Codex rounds), S3 v4 T-0541 critical_path.py, S4b v6 T-0550 stale checks behind stale_rebase=false, hotfixes T-0514 reviewed_sha, T-0517 acceptance collection + write-spec rule, T-0552 already_merged before head-moved + gate message, T-0553 graph empty pathspec, T-0559 critical path partial. Superseded without code: T-0507/T-0524/T-0531 (S2 rounds), T-0509/T-0526/T-0538 (S3), T-0510/T-0528/T-0543/T-0546/T-0548 (S4b), T-0520/T-0529 (ordering fix). Cost 132.9 USD API-equivalent, 35.9M tokens, 67 rows; 22 reviews, 10 spec reviews.
Measurements: baselines sched-pre (before) and sched-s1-code (after, this goal's own execution on the old scheduler, so not the effect of the change): tokens per accepted task -5.4 percent, fix_round_rate 0.317 -> 0.291, review request_changes 0.580 -> 0.527, usd per accepted task 1.50 -> 2.24 (Codex multi-round threads). Parallelism for T-0503 from stamps: max 3 concurrent executors (avg 1.29), Claude workers max 2 (avg 1.19), median queue wait 23 s, execution 421 s, review 150 s; zero rebase conflicts; 9 of 12 merged tasks green and approved first try; wall clock about 2 h 45 min. Undefined: critical_path_s and merge wait for this goal (hand merges stamp no merged_at; follow-up), waves rows 0 (server ran old code).

## NEXT SESSION (read first)
1. Human: review and merge PR 15 (orchestrator), then PR 1 and PR 2 in docs-kentawaibel. After PR 15 merges: switch the main checkout to the merged head and restart with f orch so orchestrator.mcp loads spawn.py (reviewed_sha fix), acceptance.py, daemon.py (waves, stale checks, already_merged ordering). Until then, approved tasks never merge on their own (gotcha 2026-09-21) and every hand merge that rebases opens one extra review.
2. `bus_read(status_not="done", role="execute")` then skill `resume`; no goal is open. Leftovers are superseded or failed tasks.
3. First goal on the new code: run one ordinary goal in mode=shadow, then `scorecard --parallelism`, read runs/sched/waves.jsonl (wave vs baseline_order, predicted pairs) and dispatch.jsonl skip reasons, `baseline save sched-shadow-1` and compare with sched-s1-code. Flip [scheduler].mode = active only if the shadow waves would have avoided a real conflict or reordered a critical-path task; otherwise keep shadow and open brief priorities 5 to 14 as separate goals.
4. Spec-writing rules now enforced by the gate: acceptance names tests as tests/test_<module>.py::test_name, tests are unittest.TestCase methods. Create a dependent task only after its parent's spec review approves (depends_on is immutable). Read every finished review, including ones on already-merged heads.
5. Planner commits from the main checkout (branch main): stage only .orchestrator/ paths; `git commit -F <scratchpad file>`; guardrails block pushing main, the goal branch carries Planner commits. planner-mode.sh scans the whole command text: keep backticks, `>`, `<`, cp/rm/mv/install/touch, tail with a pipe, git rebase/checkout/reset/revert and .env out of Bash text; absolute paths; macOS has no `timeout` and bash 3.2 has no associative arrays (Monitor scripts).
6. Tool contracts: `codex(task_id, prompt)` dispatches directly (used after three spec-review rounds; the daemon may still create one stray spec review in the 30 s gap); `codex_reply(task_id, delta)` resumes the thread and posts the result; `merge(task_id)` skips the review policy, use it only after the daemon gate and a review approve; `bus_post_result` returns the full task (large); recall is `uv run python .claude/skills/memory/scripts/recall.py`.
7. Session restart kills the in-process daemon and its posting threads; check pgrep for codex exec and claude -p before restarting.

## Human items outstanding
- Merge PR 15 here; PR 1 then PR 2 in docs-kentawaibel; then restart the session as in item 1.
- Decisions: when to flip [scheduler].mode to active (after one shadow goal); latency target, security_paths width, daemon as a process; review budget; [limits].reservations after the first production goal; [planner.routes].auto_open_pr stays false.

## Backlog (c2-c3)
- Hand merges via merge() stamp no merged_at, so merge wait stays undefined for Planner-merged tasks; critical_path_s undefined for T-0503 despite T-0559 (investigate with real rows).
- compact-memory: decisions.md 304 lines (budget 300), gotchas.md 314.
- cache_max_entries = 0 guard in jev_route; --economics/--efficiency flag conflict message in cli; consistent total_tokens for pre-G1a rows; recall ranking relevance; recall.sh python shim (system python lacks tomllib).
- Brief priorities 5-14 as later goals: duration/capacity-aware scheduling, scout objective reuse, review-overlap follow-up, merge pressure, adaptive concurrency, scheduling scorecard, Jev shadow signal, critical-path model allocation, workflow learning, speculative execution.
- kgpt side of the orchestrator module (C-K1..C-K4) on the kgpt bus.

## Architecture (decided)
Phase C: two processes, kgpt `modules/orchestrator` thin MCP module forwarding to `orchestrator serve` per repo. Phase D: the daemon owns the loop; the Planner is a function of decision points (planner_runs.py). Phase H/I: routes, reservations, acceptance-test gate, failure kinds, packets, Jev shadow routing, review telemetry, live on main since 2026-09-21. S1 (this goal): [scheduler] table, schedlog rows under runs/sched, interference.py, critical_path.py, waves in dispatch, stale checks in gate; memory/architecture.md holds the repo map at be0e82b.

## Prior art (ids for recall)
- Retrospectives: decisions.md 2026-09-18 to 2026-09-21 (goals T-0073, T-0109, T-0240, T-0260, T-0353, T-0445, T-0489, release T-0499, T-0503). Gotchas 2026-09-18 to 2026-09-21 in gotchas.md.
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt, home `ssh k3ntaw@192.168.1.167` (fish shell), kgpt home side /opt/kgpt-home.
- docs.kentawaibel.com: repo /Users/k3ntaw/code/docs-kentawaibel, Vercel project docs-kentawaibel, PRs 1 and 2 open.
