# plan.md — Planner checkpoint (updated 2026-09-21 08:40)

## OPEN GOAL T-0561 — Complete both optimization roadmaps (brief .orchestrator/roadmap-completion-program.md)
Complexity 9, route: high-risk/architectural → audit scouts, detailed plan with interface contracts, atomic specs (spec review ≥6), Codex execution in waves, gate + security review on daemon/pool/executor paths, serial merge into goal/T-0561, human PR. Experimental modes stay shadow/off. Base: main 8c6e2ae (S1 merged code live).

### Audit (P0) — done 08:55 (scouts T-0562 done, T-0563 failed on budget after 199 s, T-0573 done; Planner grep filled the rest)
Implementation matrix (source + tests are truth):
- Phase I P0 measurement: implemented_tested (scorecard.efficiency/by_goal/baseline; tests test_efficiency_*, test_tokens_per_accepted_goal). P1 packets: implemented_tested (executor/fix-delta/review/spec-review/scout/planner-decision packets, packet_run_meta measures size, hash+base sha prevent stale reuse). P2 fix-round economics: implemented_tested (executor_economics, expected_cost, class_success, resume_plan). P3 shadow routing: implemented_tested (12 jev_route tests incl. off, single eligible, batching, budget, timeout/malformed, cache, privacy; test_shadow_dispatch_never_changes_chosen_executor). P4 routing evaluation: implemented_tested (scorecard.routing_eval + `scorecard --routing`, 7 tests) — scout T-0573 wrongly reported it missing. P5 active ranking: MISSING (notify stub jev_route.py:167-171) → R14a T-0585. P6 review roles + overlap: implemented_off (complementary=false, evidence-gated; review_quality overlap tests; reviews spawn in parallel via spawn_async). P7 Jev decision points: MISSING (jev_triage covers generic kinds only) → R14c T-0586, experimental, shadow.
- AP P0 telemetry: implemented_tested (scorecard.parallelism, dispatch skip reasons stale|dependency|fallback_no_tier|account_capacity|cooldown|executor_capacity|predicted_interference|budget|spec_review_*|other; gap: merged_at not stamped by Planner merge() calls, review timings from created→done only) → stamps in R15a. P1 interference: implemented_tested (hard dependency/same_file/glob_covers/same_glob; soft dir_contains/same_dir/test_adjacent/sibling_globs/graph_link; no scope-size or module signal beyond same_dir; no caching for large ready sets → acceptable, pairwise only over ready set). P2 graph: implemented_tested (graph_signal, freshness, graph_hard_links). P3 waves: implemented_shadow (select_wave; capacity = global slots only → R2 capacity). P4 critical path: implemented_tested (rank/explain; duration inputs cold-start only → R1). P5 duration: MISSING → R1 T-0564. P6 capacity: MISSING → R2 v2 T-0582. P7/P8 scouts: MISSING → R6 T-0574. P9/P10 reviews: implemented_tested/implemented_off (see Phase I P6). P11 merge pressure: MISSING → R3 T-0567. P12 stale: partial (paths only; no interface/tests/dependency/graph/overlap signals) → R16 T-0575. P13 adaptive concurrency: MISSING → R4 v2 T-0584. P14 scheduling scorecard: MISSING → R5 T-0572. P15 Jev scheduling: MISSING → R8 T-0577. P16 allocation: MISSING → R9 T-0578. P17 speculation: MISSING → R11 T-0581 (off). P18 strategy: MISSING → R10 T-0580 (shadow). Brief P14 decision log → R12 T-0570; P15 promotion → R13 T-0571; P16 roadmap-status → R17 (after integration).
- Gotchas this goal: scout results lose structured lines (summary truncated to 1500-2000 chars, findings lists dropped when posted outside the result dict); a 6-complexity audit scout exhausted 1 USD in one turn; superseded T-0565/T-0568 after spec reviews (mutating Account.utilization, whole-wave coupled rule).
Interventions 09:00-09:25: prompts execute.md/fix-delta.md now state unittest.TestCase (Codex wrote pytest functions: T-0570/T-0571/T-0574 gate_red; fixed via codex_reply, T-0571 also merged via daemon fix round T-0591 before the supersede landed); review.md forbids prose/fences (reviews T-0583/T-0587 approved in prose around fenced JSON and were parsed as no verdict; Planner transcribed their verdicts; parser fix R19 T-0601); codex_reply leaves pipeline.gated_at set so the gate skips the task — Planner cleared it on T-0570/T-0574 (follow-up: post_tool_result with replace_result should clear gate stamps); spec reviews bounced R2/R4/R16 twice and R14a once → v3 specs T-0598/T-0599/T-0600 created with the third spec review waived by Planner decision (all risks folded), R14a v2 T-0602 reviewed again. Superseded: T-0565, T-0568, T-0575, T-0579, T-0582, T-0584, T-0585, T-0591, T-0592, T-0593, T-0596.
Wave A+B complete 10:44: 17 module tasks merged into goal/T-0561 (head 5458968, 27 commits, gate 894-895 tests): R1 T-0564 duration, R3 T-0567 merge_pressure, R12 T-0570 decision_log, R13 T-0571 promotion, R5 T-0572 sched_scorecard, R6 T-0574 scout_evidence, R11 T-0581 speculation, R14c T-0586 jev_points, R2 v3 T-0598 capacity, R4 v3 T-0599 concurrency, R16 v3 T-0600 stale, R19 T-0601 extract_json, R14a v3 T-0606 active routing, R9 v3 T-0634 allocation, R8 v3 T-0636 jev_sched, R10 v3 T-0637 strategy (+ fix rounds T-0591, T-0641). Wave C filed 10:46: R15a T-0646 dispatch integration (c7, spec review), R15b T-0647 gate/merge/executor/mcp integration (c7, held by Planner behind T-0646: both edit daemon.py), R18 T-0648 CLI wiring (c3). Then R17 roadmap-status (after R15a/b/R18), pool.toml documented keys (Planner, gate first), retrospective, PR.
Task ids (history): T-0564 R1 duration (done, gate pending) · T-0567 R3 merge_pressure (done) · T-0570 R12 decision_log (gate_red: tests not defined; auto fix round T-0579) · T-0571 R13 promotion (running) · T-0572 R5 sched_scorecard (dep R1) · T-0574 R6 scout_evidence (running) · T-0575 R16 stale (spec review T-0576) · T-0577 R8 jev_sched (dep R12) · T-0578 R9 allocation (dep R12,R1) · T-0580 R10 strategy (dep R12) · T-0581 R11 speculation (dep R12,R1) · T-0582 R2 v2 capacity · T-0584 R4 v2 concurrency · T-0585 R14a active routing · T-0586 R14c jev_points (dep R12). Pending creation after their dependencies' spec reviews approve: R15a/R15b daemon+executor+merge integration (serialized), R18 cli wiring (scorecard --scheduling/--strategies/--promotion, explain <task>, roadmap-status), R17 roadmap.py + roadmap-status.json, pool.toml config tables (Planner edit, gate first).
- Planner grep facts (provenance repo): no code exists for duration estimation beyond critical_path.COLD_START_S priors (est_duration_s accepts a durations dict keyed by task id or band), capacity beyond free_slots()/eligible_executors(), merge pressure, adaptive concurrency, scheduling scorecard, Jev scheduling, allocation, strategy learning, speculation, unified decision log, promotion framework, roadmap-status. Waves: daemon.dispatch 660-860 (shadow logs wave_row to runs/sched/waves.jsonl; active applies select_wave). Stale: daemon 1050-1123 (signals: scoped/changed paths moved on goal branch; risk none/low/high; rebase behind stale_rebase). Reviews: _open_reviews spawns each review via spawn_async in one loop (parallel), reviewer_role acceptance/adversarial behind [review].complementary=false. Jev routing: shadow only; hypothetical() heuristic; record_shadow logs role=jev_route rows. pick_executor: cheapest proven-safe by class economics else weight ranking.

### Decomposition (dependency-ordered; ids assigned at creation) — draft, finalize after audit
Wave A, independent pure modules (parallel, no daemon.py):
- R1 orchestrator/duration.py (AP P5): empirical estimates from bus stamps (dispatched_at→first_green_at / done) by task id history, task_class+executor, band+class, band; trimmed median, min_samples (config [scheduler.duration] min_samples=5, mode="empirical"|"cold_start" default empirical since deterministic); returns durations dict consumable by critical_path.rank; explain() with source and n. Tests: cold start, sufficient, sparse, outlier, telemetry.
- R2 orchestrator/capacity.py (AP P6): snapshot(pool) {executors: free/cooling/day budget, claude workers free, account headroom, window caps}, admit(ranked, snapshot, durations, priorities) with reasons; premium reservation only for a ready critical-path task, never speculative. Tests as brief.
- R3 orchestrator/merge_pressure.py (AP P11): metrics from bus + sched rows; pressure none|elevated|saturated with reason; throttle() defers only coupled tasks under saturated. Tests.
- R4 orchestrator/concurrency.py (AP P13): select_limit(hard_max, ready, pairwise, priorities, snapshot, pressure, durations) → limit ≤ hard_max, reasons, expected_benefit. Tests.
- R5 orchestrator/sched_scorecard.py + cli `scorecard --scheduling` (AP P14): predicted vs actual conflicts (waves.predicted vs rebase_conflict/stale rows), duration error, queue duration, precision/serialization metrics, malformed tolerance. Tests.
- R6 orchestrator/scout_evidence.py + prompts/scout.md + mcp spawn_scout check (AP P7/P8): objectives vocabulary, reuse_check (recall + bus scout results + graph, freshness by age and base sha), structured finding schema, telemetry rows. Tests.
- R12 orchestrator/decision_log.py + cli `explain <task>` (brief P14): unified structured decision rows runs/sched/decisions.jsonl. Contract below. Tests.
- R13 orchestrator/promotion.py + cli `promotion` (brief P15): feature registry, evidence vs criteria, recommendation stay/promote/demote. Tests.
- R16 orchestrator/stale.py (brief P7): extend signals (interface changed via graph symbols, relevant tests changed, dependency changed, graph relationships, overlapping earlier merge); daemon.stale_check delegates. Tests.
Wave B, evidence-consuming modules (parallel; depend on R12 for logging, R1 for durations):
- R8 orchestrator/jev_sched.py (AP P15): typed questions only for soft-level pairs, batched per tick, cache by pair+scope hash+goal head, mode [scheduler].jev_mode off|shadow|active default shadow; never overrides hard/dependency/capacity; fail open. Tests incl. no O(n^2).
- R9 orchestrator/allocation.py (AP P16): EV-based executor choice among eligible using economics + critical path + capacity; min evidence; [allocation].mode off|shadow|active default shadow; hooks pool.pick_executor via executor.start. Tests.
- R10 orchestrator/strategy.py + cli `scorecard --strategies` (AP P18): strategy id derivation from lineage, per-strategy outcomes, recommend() shadow, [strategy].mode off|shadow default shadow. Tests.
- R11 orchestrator/speculation.py (AP P17): eligibility, shadow estimator, deterministic result selection; [speculation].mode default off; active path deferred to a later task only if evidence. Tests.
- R14 Phase I gaps from audit (likely: jev_route active ranking with weight bound + tests for the 19 cases; challenge packet; routing_eval report; packet size by role).
Wave C, daemon/executor integration (serialized chain, complexity 7 each, security review):
- R15a dispatch: durations into _wave_order/wave_row, capacity.admit, concurrency limit → wave_limit, merge pressure, jev_sched shadow annotation, decision_log rows; stamps merged_at in merge.merge, review started/finished.
- R15b gate/merge: stale.py signals, sched telemetry outcome backfill, allocation hook in executor.start.
- R17 orchestrator/roadmap.py + .orchestrator/roadmap-requirements.json + cli `roadmap-status` writing .orchestrator/roadmap-status.json with evidence checks (final audit).

### Interface contracts (workers must follow)
- durations: dict {task_id: seconds} ∪ {"1-3"|"4-6"|"7-10": seconds}; duration.estimate(task, root=STATE, cfg=None) → {"est_s", "source": task_history|class_executor|band_class|band|cold_start, "n", "band", "task_class"}; duration.durations_for(tasks, root) → durations dict.
- decision_log.record(kind, subject, *, candidates, hard_constraints, deterministic, historical, jev=None, selected, rejected, reason, confidence=None, n=None) appends {ts, kind, subject(task/goal id), ...} to runs/sched/decisions.jsonl via schedlog.append("decisions", row); decision_log.explain(subject) returns rows newest first; outcome attached later by sched_scorecard.
- capacity.snapshot(pool) → {"executors": {id: {"free", "cooling_s", "day_tasks_left", "roles", "complexity": [min,max]}}, "claude_workers_free", "accounts": {id: {"utilization", "cooling_s"}}}; capacity.admit(ranked_ids, tasks, snapshot, priorities, durations) → {"admit": [...], "deferred": {tid: reason}} reasons ⊂ {executor_capacity, cooldown, account_capacity, budget, reserved_for_critical}.
- merge_pressure.assess(root=STATE) → {"waiting", "queue_depth", "median_wait_s", "recent_conflicts", "stale_events", "pressure": none|elevated|saturated, "reason"}.
- concurrency.select_limit(hard_max, ready_ids, tasks, pairwise_rows, priorities, snapshot, pressure, durations) → {"hard_max", "limit", "reasons": [...], "tasks_considered", "expected_benefit"}.
- Every mode key read through pool.cfg with a safe default; unknown value → default + one notify (pattern daemon._load_scheduler_cfg).
- Tests: unittest.TestCase methods in tests/test_<module>.py; acceptance names tests/test_<module>.py::test_name.

### Needs the human
- Review and merge the goal PR when it opens. Decisions after merge: none of the new modes are flipped by this goal.

## CROSS-REPO GOAL 2026-09-21 08:05 — kgpt-ios service-token auth (user "do recommended"): headless Planner in /Users/k3ntaw/code/kgpt-ios (GOAL T-0001 there, pid 54670, account B, scaffold 74f8414). Background wait in this session reports when it exits. Human: mint Cloudflare Access service token kgpt-ios, add Service Auth policy on the gpt.kentawaibel.com application, paste into the app, merge its PR.

## RELEASED 03:45: PR 15 merged at 58ee5d1; docs PRs 1 and 2 merged. Resumed 07:45 on the merged code (servers restarted 07:41 from 8c6e2ae).

## Session rules (carry forward)
- Spec-writing: acceptance names tests as tests/test_<module>.py::test_name, unittest.TestCase methods; create a dependent task only after its parent's spec review approves (depends_on immutable); read every finished review.
- Planner commits from main checkout: stage only .orchestrator/ paths; `git commit -F <scratchpad file>`; guardrails block pushing main; planner-mode scans Bash text (no backticks, `<`, `>`, cp/rm/mv/install/touch, tail pipes, git rebase/checkout/reset/revert, .env).
- Tool contracts: codex(task_id, prompt) dispatches directly; codex_reply(task_id, delta) resumes; merge(task_id) after gate + approve only; recall via `uv run python .claude/skills/memory/scripts/recall.py`.
- Session restart kills the in-process daemon; check pgrep for codex exec and claude -p first.

## Backlog (c2-c3)
- compact-memory: decisions.md 318 lines (budget 300), gotchas.md 314.
- cache_max_entries = 0 guard in jev_route; --economics/--efficiency flag conflict message; consistent total_tokens for pre-G1a rows; recall ranking; recall.sh python shim.
- kgpt side of the orchestrator module (C-K1..C-K4) on the kgpt bus.

## Prior art (ids for recall)
- Retrospectives: decisions.md 2026-09-18 to 2026-09-21 (goals T-0073, T-0109, T-0240, T-0260, T-0353, T-0445, T-0489, release T-0499, T-0503). Gotchas in gotchas.md.
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt, home `ssh k3ntaw@192.168.1.167` (fish), kgpt home side /opt/kgpt-home.
- docs.kentawaibel.com: repo /Users/k3ntaw/code/docs-kentawaibel, Vercel project docs-kentawaibel.

## Auto-handover 2026-09-21T10:42:00+02:00 — daemon tick

[planner].handover_context_tokens is the configured handover threshold.
### T-0561 GOAL: Complete both optimization roadmaps (Phase I P0-P7, Adaptive Parallelism P0-P18) to implementation completeness; experimental features stay shadow/off; roadmap-status.json
- failed: T-0563 Scout (Adaptive Parallelism audit): matrix of adaptive-parallelism-program.md P0-P4 and P12 against source and tests, plus scheduler telemetry row shapes (reason=budget or error exit (rc=1): , resume_hint_keys=[]), T-0565 R2: orchestrator/capacity.py — global capacity snapshot and deterministic admission with critical-path reservation only against present demand (AP P6) (reason=?, resume_hint_keys=[]), T-0568 R4: orchestrator/concurrency.py — adaptive concurrency limit 0..hard_max from independence, critical path, capacity and merge pressure, with logged reasons (AP P13) (reason=?, resume_hint_keys=[]), T-0575 R16: orchestrator/stale.py — complete stale-work evidence: interface, tests, dependency, graph and overlapping-merge signals with graded risk and injectable git (AP P12) (reason=?, resume_hint_keys=[]), T-0577 R8: orchestrator/jev_sched.py — Jev as a shadow scheduling signal for ambiguous soft-interference pairs only, batched, cached, fail-open, never overriding deterministic constraints (AP P15) (reason=?, resume_hint_keys=[]), T-0578 R9: orchestrator/allocation.py — critical-path-aware executor allocation by expected value over accepted cost, first-pass success and latency, with hard eligibility and evidence thresholds (AP P16) (reason=?, resume_hint_keys=[]), T-0579 fix round 1: R12: orchestrator/decision_log.py — unified structured decision evidence rows and explain() for routing, scheduling and workflow decisions (brief P14) (reason=?, resume_hint_keys=['failures', 'flaky_runs', 'missing_tests', 'rejected_ids']), T-0580 R10: orchestrator/strategy.py — workflow-strategy identifiers derived from task lineage, per-strategy outcome scorecard and shadow recommendation with evidence thresholds (AP P18) (reason=?, resume_hint_keys=[]), … and 12 more
- done (not merged): T-0562 Scout (Phase I audit): requirement-by-requirement matrix of phase-i-program.md P0-P7 against the current source and tests, T-0566 spec review: R2: orchestrator/capacity.py — global capacity snapshot and deterministic admission with critical-path reservation only against present demand (AP P6), T-0569 spec review: R4: orchestrator/concurrency.py — adaptive concurrency limit 0..hard_max from independence, critical path, capacity and merge pressure, with logged reasons (AP P13), T-0573 Scout (Phase I audit, part 2): matrix of phase-i-program.md P1-P7 against source and tests; findings as structured entries, T-0576 spec review: R16: orchestrator/stale.py — complete stale-work evidence: interface, tests, dependency, graph and overlapping-merge signals with graded risk and injectable git (AP P12), T-0583 review: R1: orchestrator/duration.py — empirical task-duration estimates with robust statistics and deterministic cold-start priors (AP P5), T-0587 review: R3: orchestrator/merge_pressure.py — measure integration contention and defer only coupled work when the merge queue is demonstrably saturated (AP P11), T-0588 spec review: R2 v2: orchestrator/capacity.py — read-only capacity snapshot (pure utilization math, day rollover by date comparison) and deterministic admission with critical-path reservation against present demand (AP P6), … and 32 more
- merged: T-0564 R1: orchestrator/duration.py — empirical task-duration estimates with robust statistics and deterministic cold-start priors (AP P5) (sha=e5590c08), T-0567 R3: orchestrator/merge_pressure.py — measure integration contention and defer only coupled work when the merge queue is demonstrably saturated (AP P11) (sha=8515a18c), T-0570 R12: orchestrator/decision_log.py — unified structured decision evidence rows and explain() for routing, scheduling and workflow decisions (brief P14) (sha=981b3ba8), T-0571 R13: orchestrator/promotion.py — shadow-first promotion framework: feature registry, evidence collection and evidence-based recommendation off/shadow/active (brief P15) (sha=?), T-0572 R5: orchestrator/sched_scorecard.py — scheduling scorecard: predicted vs actual interference, stale outcomes, duration error, queue time and heuristic validation (AP P14) (sha=0ecc2393), T-0574 R6: orchestrator/scout_evidence.py — scout objectives, reusable-evidence check before launching a scout, structured finding validation and scout telemetry (AP P7, P8) (sha=1ffdb173), T-0581 R11: orchestrator/speculation.py — selective speculative execution: strict eligibility, shadow estimator of economic benefit, deterministic single-result selection, mode off by default (AP P17) (sha=a944b854), T-0586 R14c: orchestrator/jev_points.py — experimental Jev decision points (scout necessity, context escalation, review escalation, planner relaunch), shadow by default, escalate-only in active, fail-open (Phase I P7) (sha=52d3f097), … and 10 more

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 292 more

Last events:
- 2026-09-21T10:27:51+02:00 T-0637 update {"review_verdict": "approve"}
- 2026-09-21T10:29:31+02:00 T-0634 update {"status": "done", "merged_into": "goal/T-0561", "sha": "5458968c206b6b0b7facaa8
- 2026-09-21T10:29:32+02:00 T-0561 update {"pipeline": {"last_merge": {"status": "merged", "target": "goal/T-0561", "sha":
- 2026-09-21T10:29:32+02:00 T-0634 update {"pipeline": {"first_ready_at": 1789977173.336173, "dispatched_at": 1789977173.3
- 2026-09-21T10:29:32+02:00 T-0634 update {"pipeline": {"first_ready_at": 1789977173.336173, "dispatched_at": 1789977173.3

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.
