# plan.md — Planner checkpoint (updated 2026-09-19 22:40, session 4 handover mid Phase G; written for a fresh session)

## State in one paragraph
Open goal: T-0260 Phase G token economy (complexity 8), branch goal/T-0260 from goal/T-0240 head 4c92cac. Session 4 handed over because account A reached its daily Planner budget (30M tokens) and the pre-F3 MCP server's executor counters leaked (status() says Codex exhausted while no codex exec runs, so new dispatches fall back to Claude sonnet). The main checkout is goal/T-0240: the next server loads Phase F code (F3 posts Codex results itself, F5 codex_reply, F6 red-merge holds) but not Phase G. Merged on goal/T-0260 so far (see git log goal/T-0260): G4 docs+fix, G1a+fix, G6 v4 leases, G1b+fix, G2 chain (packet, two fixes), G0 scout base, G3 bounded outputs, G9 chain (routing, two fixes; verify T-0314 merged and T-0308/T-0275 marked), G2b chain (repomap, two fixes; verify T-0307 merged and T-0302/T-0267 marked). In flight at handover: T-0276 G10 decision packets (work uncommitted by Codex, a sonnet fallback worker was committing it), T-0320 G7 v3 automatic fix rounds (c6, needs its spec review; if the daemon stamps spec_review_at without creating the child, create it by hand in the daemon shape and spawn_spec_review), T-0324 G11 prompts say commit (c2), T-0273 G5 sampled Jev (depends T-0265 and T-0267 marked merged), T-0277 G8 semantic review triggers (depends T-0272 and T-0267; mark T-0272 and T-0296 merged when T-0320 merges). First steps in the new session: bus_read(status_not="done") for T-0260 children; for each done-but-unmerged fix round check merged_into and mark its originals (pattern: bus.update(original, status='done', merged_into='goal/T-0260', merged_via='fix round <id> <sha>')); then let the daemon run. With F3 live the daemon posts Codex results itself; verify on the first Codex task that it reaches done without a Planner post (this also closes T-0240 acceptance 3).

## Phase G measurements and cost (22:30)
- scorecard --by goal T-0260: 9.60 USD at 21:55 (44.9 percent review, 39.1 percent spec review, 16 percent scouts), above the 3.0 USD review budget; every one of the ten code reviews and five spec reviews found a real defect (unguarded lookup in the run logger, packet ranking and truncation, repomap trim and a gate skip, routing dead code and floor fallback, lease reconciliation gaps, fix-round coordination and lineage cap). Decision for the human: keep spec review from 6 and security_paths as is (it paid), or narrow.
- tokens per accepted goal (G1b, on this repo): 5,963,247 over 9 goals (usd 20.27); T-0240 row shows cache_read above total_tokens because legacy rows mix with normalised ones (polish: make total_tokens consistent for pre-G1a rows).
- G2 packet for T-0256: 3,335 chars, 21 symbols, 15 relevant tests; a 100-bullet acceptance list stays under the cap without losing discovery sections.
- repomap --stdout: 3,966 chars, 4 functions each for daemon.py, bus.py, spawn.py; architecture.md refreshes from the merged sha after each merge that touches orchestrator/*.py.

## Phase G task map (22:55, handover)
- Merged on goal/T-0260: G0 T-0270; G1a T-0264+T-0283; G1b T-0265+T-0291; G2 T-0266+T-0284+T-0298; G3 T-0271; G4 T-0274+T-0280; G6 v4 T-0290 (T-0268, T-0281, T-0286 superseded, marked).
- Posted done, awaiting review or merge (mark the chain merged by hand when the last link merges): G2b chain T-0267 (held), T-0302 (held), T-0307 (held by T-0313), T-0329 (posted, aab6312); G9 chain T-0275, T-0308, T-0314 (held by T-0325), T-0327 (posted, d0ba3a7); G11 T-0324 (failed gate) + T-0328 (posted, b12190e).
- Held for a fix round already queued: G10 T-0276 (sonnet, 875da13; review T-0330 eight findings) -> fix round T-0331 queued (constraints.fix_round_for T-0276).
- Uncommitted Codex work: G7 v4 T-0326 (spec review waived; wt/T-0326 has daemon.py, planner_runs.py, pool.toml, README.md modified and prompts/fix-round.md untracked, no commit). Next session: run the gate on wt/T-0326; if green, a fresh codex(T-0326, ...) thread that only commits (or a sonnet fallback), then post; if red, read the diff and re-spec.
- Queued: G5 T-0273 (needs T-0267 chain marked merged), G8 T-0277 (needs T-0272/T-0296/T-0320 marked merged when T-0326 merges, and T-0267).
- Backlog after Phase G (c2 each): consistent total_tokens for legacy rows; recall ranking relevance; review-budget decision (goal at about 12 USD, every review found a defect).

## Read first
1. `bus_read(status_not="done")` then skill `resume`. Live: T-0260 and its children listed above; the rest are superseded/failed leftovers.
2. Planner commits from the main checkout (goal/T-0240): `git status --short` first; stage only .orchestrator/ paths; commit with `git commit -F <scratchpad file>`. planner-mode.sh scans the whole command text: keep backticks, `>`, and the words it treats as writes (cp, rm, mv, install, tail with a pipe, git rebase/checkout/reset/revert even inside quoted prose) out of Bash text; put long text in a scratchpad file with the Write tool and read it back with cat. Use absolute paths: a `cd wt/...` inside a command moves the session cwd.
3. Tool contracts: `spawn_spec_review(<spec_review task id>)` (given an execute id it RUNS AN EXECUTE); `spawn_scout(<execute id>)` runs the fallback executor; `spawn_review(<review task id>)`; `merge(task_id, target)` rebases, tests and fast-forwards but skips the review policy. `codex(task_id, prompt)` works on a running task; when Codex is exhausted it silently falls back to a sonnet worker. Specs, scope and depends_on are immutable on the bus: a fix round is a new task with constraints.fix_round_for (its worktree is cut from the held task's branch); mark the chain merged by hand until G7 lands. A rebase is a Codex task (T-0258 pattern).
4. Review policy in force: code review only when the merged diff touches pool.toml [review] security_paths (orchestrator/*.py matches almost everything); spec review from complexity 6 on sonnet, waived by the Planner after three rounds (set complexity 5 on the replacement); tests-green is the merge bar otherwise.
5. Restart rule: hand over at ~150k context or when an account's daily budget is reached, and only when no codex exec or claude -p worker is alive (pgrep).
6. The daemon appends an "Auto-handover" section to this file every 15 min while a goal is open; drop it when rewriting.

## Human items outstanding
- Restart the Planner session (checkout goal/T-0240) so F3, F5 and F6 are live; account A is at its daily budget, `orchestrator pick planner` names the account.
- Merge PRs 6, 7, 8, 9, 10 in order: https://github.com/K3NTAW/orchestrator/pull/10 is Phase F. PR 11 (Phase G) comes after Phase G closes.
- Decisions from the Phase F review (latency target, security_paths width, daemon as a process) and the Phase G review-budget question above.

## Next work after Phase G
1. Verify goal T-0240 acceptance 3 and codex_reply on the restarted server.
2. Switch the checkout to goal/T-0260 after PR 11 opens so the daemon runs the lease sweep, auto fix rounds and packets on itself; measure tokens per accepted goal on the next goal against 5.96M.
3. kgpt side of the orchestrator module (C-K1..C-K4) on the kgpt bus.

## Phase C architecture (decided)
Two processes. (1) kgpt `modules/orchestrator`, thin MCP module on the shared image, runs_on home, owns per-user `orchestrator_connections` (endpoint_url, label, encrypted bearer), tools list_goals/goal_status READ and start_goal/cancel_goal WRITE_EXTERNAL (proposals), forwards to the user's endpoint with X-KGPT-User-Id applied by UserContextMiddleware. (2) `orchestrator serve` in its own container on kenta-server (ubuntu:24.04 image from this repo: claude native installer, codex release binary, uv, git, gh), one ORCH_ROOT per repo under /work, `goal start` = install+commit scaffold + GOAL task + headless Planner launch; bearer per endpoint; per-slug locks.

## Phase D architecture (decided)
The daemon owns the loop; the Planner becomes a function of decision points (planner_runs.py): task held, scout fan-out done, goal closable. Each decision is a fresh headless `claude -p` launched by goals.launch_planner with an ids-only prompt, recorded in planner_runs.json, reconciled when dead, terminal gave_up after two failures. Never while an interactive session is registered in planner_session.json. Review cost is bounded by pool.toml [review]; scout cost by pool.toml scout limits; Planner session cost by the 150k handover rule.

## Prior art (ids for recall.sh get)
- Phase C retrospective and D3/Phase D retrospective: decisions.md 2026-09-18/19 entries (goal T-0073, T-0109). Gotchas of 2026-09-18/19 in gotchas.md (stale checkout, spawn_spec_review contract, dispatch break, pre-stamp rule, docker builds outlive executors, amd64 emulation impossible here, claude CLI vanished).
- bus:T-0066..T-0069 kgpt scouts; T-0074..T-0077 Phase C scouts.
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt (compose project), home `ssh k3ntaw@192.168.1.167` (fish shell, pipe scripts via `bash -s`), kgpt home side /opt/kgpt-home.

## Auto-handover 2026-09-19T22:47:40+02:00 — daemon tick

### T-0260 GOAL Phase G: token economy — tokens per accepted goal, straightforward-goal route with zero scouts, compact worker packets and bounded tool outputs, sampled Jev, short Planner decision sessions, automatic routine fix tasks, stage-stamp crash safety, semantic review triggers
- queued: T-0273 G5: Jev gate in sampled mode by default — deterministic sampling, local exact-repeat detection without a network call, tool target and input hash in gate rows, and an offline diagnosis command with a labelling export (depends_on=['T-0265', 'T-0267']), T-0277 G8: semantic review triggers (authorization, migrations, public interfaces, dependencies) next to security_paths, and review approval bound to the reviewed commit and re-assessed after a rebase that changes the diff (depends_on=['T-0272', 'T-0267'])
- running: T-0324 G11: execute and fix-round prompts tell the worker to commit on its branch and name the real gate script (.claude/hooks/tests-green.sh), so Codex runs stop ending with uncommitted work (executor=luna, started=2026-09-19T22:33:07+02:00), T-0326 G7 v4: automatic routine fix rounds (final; spec reviews T-0293, T-0312, T-0321 folded in, further spec review waived after three rounds) — parent carried, reconciliation in report_merge for all merge paths, defined failure-id extraction, every rejected review checked, hold key from planner_runs._held_at, goal-closed guard (executor=luna, started=2026-09-19T22:38:43+02:00), T-0327 G9 fix round 3: pool.codex_available takes the task too, so availability and dispatch routing agree (review T-0325) (executor=luna, started=2026-09-19T22:38:43+02:00), T-0328 G11 fix round 1: test_fallback_claude_when_codex_cooling asserts the old gate string in the execute prompt (gate red on T-0324) (executor=luna, started=2026-09-19T22:41:36+02:00), T-0329 G2b fix round 3: tests/test_repomap.py imports the test harness first so ROOT binds to the sandbox; drop the inert ORCH_TESTS_GREEN marker (review T-0313) (executor=luna, started=2026-09-19T22:41:36+02:00)
- held: T-0267 G2b: repo map generator — orchestrator repomap writes memory/architecture.md (module, public symbols, one-line purpose, commit sha) under about 1,000 tokens, refreshed after every merge (hold_reason=review request_changes: T-0295 (request_changes), resume_hint_keys=[]), T-0271 G3: bounded tool outputs with an expansion path — tests-green prints one line on pass and keeps the log on fail, review and spec-review prompts get a summary-first bounded diff, recall shows provenance and freshness (hold_reason=review request_changes: T-0309 (request_changes), resume_hint_keys=[]), T-0272 G7: the daemon writes routine fix rounds itself (gate red with failing test ids, review request_changes with concrete comments) and reconciles the original task's merged_into when the fix round merges; design or scope changes still escalate to the Planner (hold_reason=spec_review request_changes, resume_hint_keys=[]), T-0275 G9: executors routed by expected completion cost per task class with a success floor, falling back to weight times score below the sample size (hold_reason=review request_changes: T-0304 (request_changes), resume_hint_keys=[]), T-0276 G10: short Planner decision sessions get a compact decision packet, record their own tokens, and the handover threshold moves to pool.toml (hold_reason=review request_changes: T-0330 (request_changes), resume_hint_keys=[]), T-0296 G7 v2: the daemon writes routine fix rounds itself and reconciles merged_into (spec review T-0293 folded in: coordinated with the autonomous held decision point, escalation notifies once) (hold_reason=spec_review request_changes, resume_hint_keys=[]), T-0302 G2b fix round 1: repo-map trimming spreads cuts across modules so daemon.py and spawn.py keep their public functions; the merge gate never skips on an ambient env var (review T-0295) (hold_reason=review request_changes: T-0305 (request_changes), resume_hint_keys=[]), T-0307 G2b fix round 2: build the repo map from the merged commit, not ROOT's checked-out tree; drop the fake nested-gate guard; record a failed refresh on the merge result (review T-0305) (hold_reason=review request_changes: T-0313 (request_changes), resume_hint_keys=[]), … and 3 more
- failed: T-0274 G4: straightforward-goal route, zero scouts by default, five-file limit as a warning, read scope and interface contracts in specs, one-line Planner reminder (reason=?, resume_hint_keys=[]), T-0299 spec review: G7 v2: the daemon writes routine fix rounds itself and reconciles merged_into (spec review T-0293 folded in: coordinated with the autonomous held decision point, escalation notifies once) (reason=review returned no parseable verdict, resume_hint_keys=['raw']), T-0311 spec review: G7 v2 automatic fix rounds (T-0296) (reason=superseded by T-0312 (daemon-shaped spec review task), resume_hint_keys=[])
- done (not merged): T-0261 Scout G-S1 (token accounting inventory): every place tokens or usd are recorded per role, what scorecard by_goal/by_task aggregate, and what tokens-per-accepted-goal would still miss, T-0262 Scout G-S2 (worker packet and tool-output surfaces): what a worker receives at start, what it fetches in its first turns, where outputs are capped, and what a compact packet generator can build from, T-0263 Scout G-S3 (daemon stage stamps, automatic fix rounds, review triggers): crash windows between stamp and action, what an automatic fix task needs and already exists, and where semantic review triggers and commit-bound approval would go, T-0269 spec review: G6: stage stamps become claimed/completed pairs with a lease — a crash between a stamp and its action is re-run on a later tick, reconciled against process and git state, T-0278 review: G1a: every run row carries goal_id and normalised token buckets (uncached input, cache read, cache write, output, reasoning) through one bus.normalize_usage(); Jev usage rows carry task and goal ids, T-0279 review: G2: compact worker packet — spawn.packet(task) rendered into execute and fix-round prompts at dispatch (objective, acceptance, base commit, write and read scope, symbols and tests per scope file, matching gotchas, verification command, evidence pointers), capped at about 1,200 tokens, T-0282 spec review: G6 v2: stage stamps get a lease and a done marker with an explicit field mapping; sweep_leases only reconciles tasks in queued, running or done, never a held task; merge retries counted (spec review T-0269 folded in), T-0285 review: G4 fix round 1: mirror the rewritten CLAUDE.md Never/Always lists into .orchestrator/prompts/planner.md (gate red: test_planner_md_matches_claude_md_lists), … and 18 more
- merged: T-0264 G1a: every run row carries goal_id and normalised token buckets (uncached input, cache read, cache write, output, reasoning) through one bus.normalize_usage(); Jev usage rows carry task and goal ids (sha=?), T-0265 G1b: scorecard reports tokens per accepted goal — by-role and cached/uncached splits per goal, Jev and Planner buckets, failed and superseded tasks included, accepted goals counted by PR (sha=?), T-0266 G2: compact worker packet — spawn.packet(task) rendered into execute and fix-round prompts at dispatch (objective, acceptance, base commit, write and read scope, symbols and tests per scope file, matching gotchas, verification command, evidence pointers), capped at about 1,200 tokens (sha=?), T-0268 G6: stage stamps become claimed/completed pairs with a lease — a crash between a stamp and its action is re-run on a later tick, reconciled against process and git state (sha=?), T-0270 G0: scout and triage worktrees base on the goal branch, not origin/main (Phase G scouts T-0262/T-0263 read code from PR 5 and reported stale findings) (sha=443feaab), T-0280 G4 fix round 1: mirror the rewritten CLAUDE.md Never/Always lists into .orchestrator/prompts/planner.md (gate red: test_planner_md_matches_claude_md_lists) (sha=391ff9a0), T-0281 G6 v2: stage stamps get a lease and a done marker with an explicit field mapping; sweep_leases only reconciles tasks in queued, running or done, never a held task; merge retries counted (spec review T-0269 folded in) (sha=?), T-0283 G1a fix round 1: log_run's goal lookup fails open, and the spawn/executor run rows are proven by subprocess-mocked tests (review T-0278) (sha=f88041de), … and 5 more

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 142 more

Last events:
- 2026-09-19T22:42:55+02:00 T-0276 update {"review_verdict": "request_changes"}
- 2026-09-19T22:43:07+02:00 T-0276 update {"pipeline": {"dispatched_at": 1789848260.82575, "gated_at": 1789850404.275442, 
- 2026-09-19T22:43:51+02:00 T-0326 update {"codex_thread": "01a0bb64-8b7e-7c21-b702-2b7e7639a4bb"}
- 2026-09-19T22:43:54+02:00 T-0329 update {"codex_thread": "01a0bb67-2ee9-7880-823a-753d2ede2052"}
- 2026-09-19T22:44:45+02:00 T-0328 update {"codex_thread": "01a0bb67-2ee7-7771-8bff-0536a13b7360"}

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.
