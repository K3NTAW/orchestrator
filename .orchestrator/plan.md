# plan.md — Planner checkpoint

## Goal T-0065 (opened 2026-09-18 11:20, orchestrator side closed 14:30): kgpt cowork with GitHub repos
Phase A merged: `orchestrator install <target>` (d410503, PR 6 goal/T-0065 → main, awaiting the human). kgpt scaffolded on kgpt goal/T-0001 (97094ac + ruff exclude 33c1afb + memory f9b2900), not pushed (needs approval). Phase B plan and specs live in kgpt/.orchestrator/plan.md; tasks are created in the kgpt bus (see that file). The kgpt Planner starts with `ORCH_ROOT=/Users/k3ntaw/code/kgpt` from the kgpt root.
Gotchas recorded: daemon.free_slots never fires the Claude fallback while every Codex row cools (daemon.py:84); gate() spawns the review at the default tier (same-model review, daemon.py:168). Both are fix candidates → folded into Phase C spec C-O6.
Phase B addition found by scout C3 (2026-09-18 15:40): no gateway route or Settings UI ever calls kernel github.connect(); the handbook's "connect under Settings" step is unbuilt, so the GitHub module is unusable live. Added as B0b in kgpt plan.md.

## Goal T-0073 (opened 2026-09-18 15:05): Phase C — kgpt orchestrator module (per user, home-side)
Complexity 8. User decisions 2026-09-18: per user; same Claude A/B and Codex accounts as this laptop; module in kgpt. Scouts T-0074..T-0077 done 15:45.
Facts (≥0.85 unless noted):
- Kernel reaches a module by manifest.mcp_url only, streamable-http, fresh session per call; nothing requires the shared kgpt image (kernel/kgpt_kernel/mcp/client.py:88-144). Auth = Cloudflare Access service token at the tunnel edge + `X-KGPT-Module-Secret` (env `<MODULE>_SHARED_SECRET`, hmac-checked in the module, refuses to boot unset in prod) + `X-KGPT-User-Id` header applied by UserContextMiddleware (kernel/kgpt_kernel/mcp/user_context.py) so RLS sees the caller. Home modules are dropped from the registry unless KGPT_HOME_AVAILABLE is truthy (manifest.py:149-177). Ingress: one hostname `mcp-<name>.kentawaibel.com` per module in infra/cloudflared/home.yml, Access policy allowing only the service token. Compose pattern: docker-compose.home.yml:20-45 (own healthcheck, no-new-privileges, read_only, tmpfs).
- Per-user connection pattern = github_connections (migration 0018: owned table, RLS own_rows, GRANT kgpt_app, OWNED_AFTER_0012, unique (user_id, key)); kernel module owns the row and raises NotConnected; Google has the complete gateway+Settings reference (kernel/kgpt_kernel/google.py stores an encrypted refresh token; routes /google/connection, /authorize, /callback; Settings card reads a status endpoint). GitHub has none of that (see B0b).
- Orchestrator host assumptions (T-0075): daemon.notify is osascript-only (daemon.py:16-23); hooks hardcode /private/tmp, /var/folders and ~/code (guardrails.sh:36, planner-mode.sh:17-19); [secrets.*] in pool.toml shell out to the personal `f tok get` (Keychain) tool; Phase 0 logins are interactive; `f orch` launcher is outside the repo; the Planner is launched as an interactive claude session, there is no headless "start a goal" entry point (spawn.run_claude is the worker path).
- Headless auth (T-0077, web): Claude Code: `claude setup-token` mints a one-year OAuth token for CLAUDE_CODE_OAUTH_TOKEN, documented for CI on Pro/Max/Team/Enterprise (code.claude.com/docs/en/authentication); not read in --bare mode; CLAUDE_CONFIG_DIR keeps separate credentials per profile. Ubuntu 20.04+ officially supported for the native install. Codex CLI: `codex login --device-auth` (flag present in codex-cli 0.154.0), credentials in CODEX_HOME/auth.json reused by `codex exec`. Open: Codex's Rust binary glibc floor is unconfirmed for Ubuntu 20.04 (glibc 2.31) → run the executor in its own container (24.04 base) and the question is moot.
Decision proposed to the human (not yet confirmed): executor = its own container on kenta-server (always on; own glibc; the sandbox the devcontainer intended), image built from the orchestrator repo, credentials mounted from host files the human creates (two CLAUDE_CONFIG_DIRs with tokens, CODEX_HOME, gh token), repos under a /work volume. Alternatives that lose: the MacBook (not always on), native on 20.04 (Codex glibc risk, credentials in a shared home).
Architecture: two processes. (1) kgpt `modules/orchestrator` (thin MCP module on the shared image, runs_on home): owns `orchestrator_connections` (per user: endpoint_url, label, encrypted bearer token), tools list_goals/goal_status (READ), start_goal/cancel_goal (WRITE_EXTERNAL → proposals; a bound project supplies the repo), forwards to the user's endpoint. (2) orchestrator `serve`: HTTP API on the home network (POST/GET /goals, /goals/{id}, cancel, /healthz; bearer), one ORCH_ROOT per repo under /work, `goal start` = ensure clone + scaffold + GOAL task + headless Planner (`claude -p` on account A with the orchestrate skill and .mcp.planner.json) + daemon; only ever opens PRs.
Order: orchestrator repo first (C-O1..C-O6, this bus, Codex cooling → manual sonnet dispatch), kgpt side after B1 and B3 (C-K1..C-K5, kgpt bus). Provisioning on kenta-server is a runbook the human follows for logins; I run the non-secret steps over SSH with approval.

### Orchestrator specs (parent T-0073)
C-O1 (c4) notify(): log line always, optional webhook POST to ORCH_NOTIFY_URL (ntfy-style plain text), osascript only on darwin. Files: orchestrator/daemon.py, tests/test_daemon.py, README.md.
C-O2 (c4) Host-portable auth and secrets: [[claude_accounts]] optional `oauth_token_env`; run_claude exports CLAUDE_CODE_OAUTH_TOKEN from it; [secrets.<role>] values may be `env:NAME` (read from the environment) besides a shell command; hooks accept Linux temp dirs via TMPDIR and drop the ~/code anchor in favour of ORCH_ROOT's parent. Files: orchestrator/spawn.py, orchestrator/pool.py, tests/test_spawn.py, .claude/hooks/guardrails.sh, .claude/hooks/planner-mode.sh (hooks are protected paths → this spec splits: C-O2a python+pool, C-O2b hooks by the human or with explicit approval).
C-O3 (c6) `orchestrator goal start <repo_path> "<goal>"` / `goal status <repo_path> [id]` / `goal list`: ensure scaffold (install), create the GOAL task, launch the Planner headlessly (claude -p, account A config dir, --mcp-config .mcp.planner.json, --append-system-prompt = planner mode text from CLAUDE.md, prompt = `/orchestrate <goal>`), record in .orchestrator/goals.json, status from the bus. Files: orchestrator/goals.py, orchestrator/cli.py, tests/test_goals.py, .orchestrator/prompts/planner.md, README.md.
C-O4 (c6, deps C-O3) `orchestrator serve`: starlette app (already a dependency of mcp) with bearer auth (ORCH_SERVICE_TOKEN), repos.toml registry (slug → path, git url; clone on demand), endpoints above, 404/409 semantics, GET /healthz unauthenticated. Files: orchestrator/serve.py, orchestrator/cli.py, tests/test_serve.py, .orchestrator/repos.toml.example, README.md.
C-O5 (c5, deps C-O2a, C-O4) Executor image and runbook: Dockerfile (ubuntu:24.04, native claude installer, codex, uv, git, gh), docker-compose.executor.yml (volumes /work, /root/.claude-a, /root/.claude-b, /root/.codex; env file for tokens; healthcheck), docs/executor-host.md (human steps: setup-token ×2, codex device auth, gh auth login, ntfy topic; nothing secret in the repo). Files: Dockerfile, docker-compose.executor.yml, .dockerignore, docs/executor-host.md, README.md.
C-O6 (c4) Daemon fixes from Phase A: free_slots counts Claude capacity when on_exhausted=fallback_claude and every Codex row cools; gate() picks the review tier from the executed task's executor (opus for a sonnet executor, sonnet for Codex). Files: orchestrator/daemon.py, tests/test_daemon.py (conflicts with C-O1 → depends_on C-O1).

### kgpt specs (parent = kgpt GOAL, created in the kgpt bus; details in kgpt/.orchestrator/plan.md)
C-K1 migration 0027 orchestrator_connections + kernel module · C-K2 modules/orchestrator (manifest, server, tests, handbook 08 + 17) · C-K3a gateway routes /orchestrator/connection + /projects/{id}/goals · C-K3b Settings card + Cowork GoalsPanel · C-K4 compose service + tunnel ingress + runbook.

## Prior art
- mem:gotchas.md kgpt hosts; bus:T-0035 tests-green under uv; both boxes healthy 2026-09-18 11:00.

## Handover if the session restarts
Read this file, `bus_read(status_not="done")`, skill `resume`. PR 6 pending human. Create missing C-O specs, dispatch by hand while Codex cools (spawn_scout on an execute task runs the sonnet fallback), add an opus review for every fallback-executed task. Then close T-0073 orchestrator side with a PR goal/T-0073 → main.

### C-O7 (added 2026-09-18 17:40, user decision): the Planner's own usage and the account switch
Why: the interactive Planner is one process on one account; the pool balances only spawned workers and counts only their JSON usage. Measured: this session's transcript ≈7.8M tokens by the pool metric vs 613k credited to account A → the subscription limit hit with no warning; the user switched with /login (account A's config dir now holds the other subscription).
C-O7a (c5, deps C-O6 T-0083, C-O4 T-0082): pool.tally_planner() reads assistant-line usage from <config_dir>/projects/<encoded ROOT>/*.jsonl with per-file offsets, counted into utilization and the daily budget; daemon tick calls it; status shows it; `orchestrator pick <role>` prints the account with headroom for the launcher (exit 3 = hold).
C-O7b (c4, deps C-O7a): `orchestrator handover [--reason]` writes an Auto-handover section into plan.md from bus state; the daemon refreshes it every 15 min so a Planner that dies at a limit leaves a fresh checkpoint.
Launcher change (human, protected dir): call `orchestrator pick planner` and start the Planner with that config dir.
