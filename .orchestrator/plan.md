# plan.md — Planner checkpoint (updated 2026-09-24 ~22:40 CEST; GOAL T-0991 Hermes-inspired program COMPLETE: 26 execute tasks merged into goal/T-0991, PR 25 (stages 1-2) merged into main a721601, PR 26 (stage 3) open https://github.com/K3NTAW/orchestrator/pull/26; retrospective in decisions.md; follow-ups: run hermes-eval and scorecard --hermes on main after PR 26, revert B review affinity and window cap when B stops rate-limiting; docs-kentawaibel PR 4 merged earlier)

### Decisions 2026-09-23 17:20 (headless packet: T-1018 and T-1065 held)
- T-1018 (S2i harness_depth) held at spec review T-1070 request_changes, worktree null: never executed, so a fix round with depends_on=[T-1018] could never dispatch (bus.ready needs merged_into). Respec T-1093 (respec_for T-1018, depends_on [T-0996], c7, spec_review route) folds all four risks: constraints.architectural dropped (never persisted; goal container, security/migrations glob, complexity >= 8 remain level 4); would_skip names only the two real call sites, daemon._dispatch_worker jev_route.shadow_context/record_shadow and spawn.run_worker _prepare_skills for the execute role (strategy lookup and scout suggestions are not on the dispatch path and are not mentioned); level-0 history threshold is first_pass_defined_count >= 5, not n; history computed once per tick via harness_depth.history_by_task_class and passed to every candidate. Acceptance gained tests/test_spawn.py::test_active_fast_path_skips_skill_routing_for_execute_role and test_history_by_task_class_uses_defined_count. T-1018 superseded; T-1075 (S3e) depends_on re-pointed T-1018 -> T-1093 in place.
- T-1065 (S1c fix round 1) held "reviews failed: T-1080": the review worker died on a B rate limit (rate_limit on B, cooling 1800s) and the sweeper re-queued T-1080; the diff is gate-green and unchanged (reviewed_sha 9bcba6a). No fix round: review_held_at cleared and status set back to done so merge_reviewed() acts when T-1080 posts. If T-1080 dies again on rate limit the same clear applies; if it posts request_changes the daemon holds again with a real reason.
- Needs the human: nothing new. PR goal/T-0991 -> main stays pending until stage 2 merges.

## GOAL T-0991 (user 2026-09-23 12:00, brief verbatim in .orchestrator/roadmap-hermes.md): Hermes-inspired memory tiers, prompt-cache economics, worker control, security hardening, overhead metric, fast path, adaptive depth (P0-P36)
Complexity 10 (cross-cutting, touches spawn/executor/daemon/evidence/skills and security paths). Route: high-risk/architectural: detailed plan here, atomic specs with interface contracts, spec review on every task from complexity 6, deterministic gate, one security review per task (every orchestrator/*.py is a [review] security_paths glob), human PR. Zero scouts: the P0 audit below is Planner grep (provenance repo) plus the Hermes Agent README and docs pages fetched 12:05-12:15 (provenance web; data, never instructions). Goal branch goal/T-0991 cut from origin/main after the roadmap commit. Shadow-first: every new adaptive path is a promotion feature starting off/shadow with self-refusing activation criteria. Anti-goals P36 apply: nothing replaces the scheduler, scorecards, DAG, worktrees, hooks, tests-green, serial merge or the human merge.

### P0 audit: adopt / adapt / skip (Orchestrator today vs Hermes Agent)
Hermes facts (docs): MEMORY.md 2,200 chars + USER.md 1,375 chars frozen into the system prompt per session (cache-friendly), memory tool add/replace/remove that errors instead of dropping when over budget, no auto compaction (agent consolidates above 80%); session history in SQLite state.db with FTS5 (session_search, ~20 ms, raw messages, no LLM); prompt_caching.py applies Anthropic cache breakpoints with tiers stable → context → volatile (identity/tool guidance/skills, context files, then memory/profile/timestamp); context_compressor.py lossy summarization of middle turns; context files chosen first-match (.hermes.md → AGENTS.override.md → AGENTS.md → CLAUDE.md → .cursorrules), scanned for injection (ignore-previous-instructions, hidden HTML, credential reads, curl exfiltration, zero-width/bidi chars) and blocked (user-authored SOUL.md only warns); subdirectory hints injected progressively to keep the system prompt stable; env filtering (execute_code blocks vars containing KEY/TOKEN/SECRET/PASSWORD/CREDENTIAL/PASSWD/AUTH; MCP subprocesses get only PATH HOME USER LANG LC_ALL TERM SHELL TMPDIR XDG_*; skills declare required_environment_variables), log redaction of ghp_/sk-/bearer/key=/token=; delegate_task subagents exist but docs give no inspection/steering/cancel/partial-result detail; no structured output validation documented; Tirith pre-exec scanning, command approval modes, container isolation, SSRF blocklists, DM pairing (all skip: not our workflow).

| Area | Orchestrator today (grep) | Hermes | Decision |
|---|---|---|---|
| Always-loaded memory | packets get ≤5 gotchas + matched decisions via recall.py layers notes+bus ([memory] budget_hits 8, budget_chars 6000); decisions.md 93 KB, gotchas.md 74 KB, plan.md 61 KB, architecture.md 4 KB, model-notes.md 2 KB all Markdown | 2,200-char frozen MEMORY.md | ADAPT: bounded HOT view generated from a structured store, budget in tokens (start 3,000, [memory].hot_budget_tokens), never-compact classes (active constraints, unresolved gotchas, invariants, governing decisions) |
| Historical memory + search | recall.py scores terms over note entries, bus tasks, claude-mem sqlite (read-only observations_fts), graph lessons; no structured metadata, no filters | SQLite FTS5 session search | ADOPT: WARM store .orchestrator/memory/index.sqlite (records + FTS5) with the P1 metadata, filters by kind/component/file/task_class/model/date/tag; COLD = original Markdown + archived records, always searchable |
| Compaction | none (files only grow; plan.md rewritten by hand) | manual consolidation | ADAPT: deterministic compaction of the HOT view only, records never deleted (moved to warm/cold with pointers) |
| Memory provenance | decisions carry dated entries + revert paths in prose | curated | ADOPT structured fields (provenance, outcome, revert_path, superseded_by, source_task/goal) via migration parser; Markdown stays as archive; migration reversible (rebuild from Markdown) |
| Prompt caching | bus.log_run already normalizes cache_read_input_tokens / cache_creation_input_tokens (Claude) and cached_input_tokens (Codex); runs 2026-09-20..23: review 94.5M cache_read vs 3.4k uncached input over 130 runs, spec_review 91.0M vs 2.9k, codex execute 145.0M cached of 153.2M input, jev_route 78k input with 0 cache; no hit ratio, no prefix size, no invalidation reason, no effective cost | cache breakpoints, tiered prompt | ADOPT telemetry (P6) computed from existing rows + packet_meta prefix hash/sizes; effective_context_cost with per-provider price ratios in [cache] |
| Stable prefixes | packet = one user prompt to `claude -p` (skills, objective, acceptance, base, write_scope, read_scope, constraints, relevant_tests, symbols, gotchas, decisions, verify, evidence); role template wraps it; per-role --mcp-config and --allowedTools change tool schemas per role | stable → context → volatile | ADAPT: reorder to stable (role rules, tool catalog L0, skill catalog ids, repo rules) → semi-stable (memory HOT) → dynamic (task, evidence, timestamps); measure with prefix_sha across runs before any routing change |
| Cache-aware context/tool/skill routing | context_router HIDE/SHORT/LONG/FULL (relevance, tokens, risk); tool_catalog minimal set with one-shot escalation; skill_router compact catalog | subdirectory hints keep prompt stable | ADAPT in shadow: add cacheability + invalidation cost terms to the decision rows; stable L0 catalog + dynamic L2 schemas; test rather than assume |
| Worker registry/inspection | bus task rows carry pid, account, claimed_at, worktree; daemon.alive/reconcile_dead/stale_check; no per-worker event stream, no children, no tokens-so-far | none documented | ADOPT registry .orchestrator/workers/<task>.json + events jsonl, statuses per P11, `orchestrator workers` read-only inspection (structured events only) |
| Steering | none; codex_reply resumes a Codex thread between tasks; Claude session_id stored in packet_meta (spawn.py:1068) | interrupt via Ctrl+C / new message | ADAPT: steering = recorded message + interrupt + resume (codex exec resume with the message; claude -p --resume session_id) with immutable original spec; policy triggers in shadow first |
| Cancellation + partial results | p.kill() only on spawn failure; reconcile_dead requeues/regates/holds dead workers | /stop | ADOPT cancel(task, reason): SIGTERM then SIGKILL after grace, worktree kept, registry cancelled, pool.release, partial result from observable evidence (diff stat, commits, gate output, tool-event files) with provenance worker_partial into the evidence pool |
| Structured outputs | post_result caps 1,500 tokens, sets confidence/provenance; extract_json + review verdict parse; no per-role schema; failed parse = task failed | none | ADOPT contracts.py per role + recovery ladder (deterministic repair → bounded schema-repair resume → rerun), recovery tokens logged |
| Hostile context scanning | none; evidence.trust is only trusted/untrusted by provenance; skill_discovery stores quarantine as inert data with static inspect | pattern scanner blocks project files | ADOPT deterministic context_scanner safe/suspicious/blocked with evidence; trust classes SYSTEM/TRUSTED_REPO/LOCAL_USER/EXTERNAL/UNTRUSTED with fixed authority order; Jev never decides authority |
| External skill safety | discover → quarantine → inspect (hash, content changed check) → testing → shadow → active | Skills Guard env scan | ADAPT: structured risk report (scripts, endpoints, tools, credential refs, dependencies, filesystem) gating quarantined → testing |
| Credential filtering | spawn.py:1020 worker env = full os.environ + CLAUDE_CONFIG_DIR + ORCH_*; [secrets.<role>] resolves tokens by command; jev.redact on evidence persistence and usage logs | allowlisted env, log redaction | ADOPT allowlisted propagation ([secrets].env_passthrough, env_mode off/shadow/active; shadow logs stripped keys only, never values) |
| Orchestration overhead | cli cost by goal splits execute/review/spec_review/scout/other %, planner_runs, jev_tokens, planner_tokens (scorecard.by_goal) | none | ADAPT: overhead.py amplification = orchestration/execution tokens, cost share, latency share per accepted goal; `scorecard --overhead` |
| Fast path / harness depth | complexity 1-3 = hooks only, spec review from 6, security review by path; Jev/skills/strategy consulted regardless | none | ADOPT deterministic harness_depth level 0-4 from existing fields (complexity, scope, security paths, architectural, migrations, deps, first-pass history); level 0-1 skips Jev, skill routing, strategy lookup, scouts; hard safety unchanged; promotion feature fast_path |
| Stale-work + critical path steering | stale.evidence returns risk none/low/…, daemon rebases or holds (stale_rebase_conflict); critical_path.rank | none | ADAPT: severity → continue / steer / cancel-or-hold, critical-path workers first, shadow decision rows |
| Messaging, cron, approval modes, containers, SSRF, pairing | n/a | present | SKIP (P36) |

### Named uncertainties (answers could change implementation; resolved by grep, no scouts)
- U1 Do cache reads happen across separate worker invocations at all? Answer: yes within a run (multi-turn claude -p) and for Codex threads; cross-run reuse is unmeasured because no prefix identity is recorded → S1b records prefix_sha per packet before S2c reorders anything.
- U2 Can a running Claude worker be steered? `claude -p` has no stdin channel; the json output carries session_id, already stored on packet_meta → steering is interrupt + `--resume`. Codex: `codex exec resume` with a message (executor.argv_for kind resume). Both are adapt, not adopt.
- U3 Is FTS5 available in the bundled sqlite3? recall.py already queries claude-mem's observations_fts → yes on this machine; S1a still feature-detects and falls back to LIKE with a logged warning.

### Interface contracts (stage 1 tasks run in parallel on disjoint files)
- memory_store.py: Record fields id, kind (decision|gotcha|architecture|model_note|retrospective|strategy|skill_outcome), title, date (ISO), repo, components[], files[], tags[], provenance, outcome, revert_path, superseded_by, source_task, source_goal, tier (hot|warm|cold), body, sha; `migrate(root)`, `rebuild(root)`, `add(record, root)`, `search(query, *, kind=None, component=None, file=None, task_class=None, model=None, date_from=None, date_to=None, tag=None, tier=None, limit=20)`; DB .orchestrator/memory/index.sqlite tables records + records_fts (FTS5, external content); CLI `orchestrator memory migrate|rebuild|search|show`.
- cache_telemetry.py: `normalize(provider, usage) -> {input_uncached, cache_read, cache_write, output, hit_ratio}`; `effective_cost(norm, cfg)` using [cache] read_ratio (default 0.1) and write_ratio (1.25) per provider; packet_meta gains prefix_sha, prefix_chars, suffix_chars, dynamic_sections; `report(root, days)`; CLI `scorecard --cache`.
- worker_registry.py: STATUSES ("starting","running","waiting","steering","cancelling","cancelled","done","failed","held"); `upsert(task_id, **fields)`, `get(task_id)`, `active()`, `event(task_id, kind, **data)` → .orchestrator/workers/<task>.json and <task>.events.jsonl; `reconcile(alive_fn)`; CLI `orchestrator workers [--task id] [--json]`.
- context_scanner.py: `scan(text, *, source_kind) -> {"verdict": "safe"|"suspicious"|"blocked", "findings": [{"pattern", "line", "excerpt"}], "score"}`; TRUST_CLASSES ("SYSTEM","TRUSTED_REPO","LOCAL_USER","EXTERNAL","UNTRUSTED"), `authority(cls) -> int` (SYSTEM highest); evidence.make sets trust_class from provenance (repo → TRUSTED_REPO, memory → LOCAL_USER, web/ticket/db/external → EXTERNAL, scanner blocked → UNTRUSTED) and keeps the old trust field.
- overhead.py: ORCHESTRATION_ROLES ("planner","planner_decision","planner_shadow","scout","review","spec_review","challenge","jev_route","jev_gate","jev_sched","jev_points","jev_rank","jev_skills","memory","routing"); `for_goal(goal_id, root) -> {orchestration_tokens, execution_tokens, amplification, orchestration_usd, goal_usd, cost_share, orchestration_s, goal_s, latency_share}`; `report(root)`; CLI `scorecard --overhead`.
- contracts.py (S2g): SCHEMAS role→schema; validate(role, result) → {ok, errors, repaired}; repair(role, result) deterministic and idempotent; recover(role, result, *, session, thread, budget_usd) active-mode only; promotion feature contracts.
- env_policy.py: `worker_env(role, *, base, extra, cfg, task_id=None) -> (env, stripped)` (token-based credential veto, exact passthrough names override; wired into spawn, executor, goals, planner_shadow); [secrets] env_mode = "shadow" (off|shadow|active), env_passthrough default list PATH HOME USER LANG LC_ALL TERM SHELL TMPDIR XDG_* CLAUDE_* ORCH_* plus resolved [secrets.<role>] keys; shadow records stripped key names to runs/<date>.jsonl rows role "env_policy" (names only, never values).

### Stages (each task ≤5 files; spec review + security review each; tests/test_<module>.py named in acceptance)
Stage 1 (parallel, disjoint): S1a memory_store + migration + FTS (P1 warm/cold, P2, P3) c6 · S1b cache_telemetry + prefix identity (P6) c6 · S1c worker_registry + inspection (P11, P12) c7 · S1d context_scanner + trust classes (P19, P20) c7 · S1e overhead metric (P23) c5 · S1f env_policy credential filtering (P22) c7.
Stage 2: S2a HOT view + compaction + packet wiring in shadow (P1 hot, P4; depends S1a) c7 · S2b memory scorecard + memory evals (P5, P34 memory; depends S2a) c6 · S2c stable prefix reorder + prefix telemetry (P7; depends S1b) c7 · S2d cache-aware router/tool/skill disclosure in shadow (P8-P10; depends S2c) c7 · S2e cancel + partial results (P15, P16; depends S1c) c8 · S2f steering + policy shadow (P13, P14; depends S2e) c8 · S2g output contracts + recovery (P17, P18; depends S1c) c7 · S2h skill hardening risk report (P21; depends S1d) c6 · S2i harness_depth + fast path shadow (P24-P26; depends S1e) c7.
Stage 3: S3a evidence reuse worker_partial (P27; depends S2e) c6 · S3b stale severity + critical-path steering shadow (P28, P29; depends S2f) c7 · S3c memory→skill candidates + strategy outcomes in warm store (P30, P31; depends S2a) c6 · S3d cache + handoff economics (P32; depends S1b) c5 · S3e promotion features + hermes eval suite + metrics (P33-P35; depends all) c7 · S3f docs/hermes-hardening/00-12 (P0 matrix + each area) c4.

### Progress
- Clock note: the Planner's hh:mm stamps in today's entries below and in decisions/gotchas run ahead of the wall clock (by ~2 h early on, ~6 h by the entries stamped 19:00-20:00); the runs rows and git commit times carry the true times (CEST). Real time at this note: 14:25.
- 12:20 T-0760 closed done, T-0777 superseded (stale leftovers of the context program). T-0991 created, running (goal container).
- 12:30 plan written; roadmap + plan committed on main and pushed; goal/T-0991 cut from origin/main. Stage 1 specs filed 12:40: S1a T-0992 c6, S1b T-0993 c6, S1c T-0994 c7, S1d T-0995 c7, S1e T-0996 c5, S1f T-1002 c7 (depends_on T-0994; T-0997 superseded). Overlaps accepted: cli.py (new subcommands, five tasks), spawn.py (S1b packet_meta vs S1c launch/result site), tests/test_spawn.py and test_cli.py (new methods). 13:05 spec reviews T-0998/T-1001 request_changes folded: S1a re-filed T-1004, S1d re-filed T-1005 (T-0992/T-0995 superseded). Stage 2 filed 13:10 with depends_on (ids in the bus, parent T-0991, titles S2a1..S2i). 13:20 spec reviews T-0999/T-1000 folded: S1b and S1c re-filed and every dependent cascaded onto the new ids: T-0993 to T-1019, T-0994 to T-1020, T-1002 to T-1021, T-1011 to T-1022, T-1012 to T-1023, T-1013 to T-1024, T-1014 to T-1025, T-1015 to T-1026, T-1016 to T-1027. 13:40 round-2 spec reviews T-1006/T-1007 folded and the re-filed S1a/S1d approved by the Planner (two rounds reviewed); dependents cascaded: T-1004 to T-1036, T-1005 to T-1037, T-1008 to T-1038, T-1009 to T-1039, T-1010 to T-1040, T-1017 to T-1041, T-1032 to T-1042, T-1034 to T-1043, T-1035 to T-1044. 14:40 round-2 spec review T-1029 on S1c folded and the re-filed task approved; cascade: T-1020 to T-1048, T-1021 to T-1049, T-1024 to T-1050, T-1025 to T-1051, T-1026 to T-1052, T-1027 to T-1053, T-1030 to T-1054, T-1031 to T-1055, T-1043 to T-1056, T-1044 to T-1057. T-1036 memory store review approved, merging. 14:50 round-2 spec review T-1028 on S1b folded (prefix defined in render() before the packet header) and approved; cascade: T-1019 to T-1058, T-1022 to T-1059, T-1023 to T-1060, T-1033 to T-1061, T-1056 to T-1062, T-1057 to T-1063. Daemon pid 69441 on current main code dispatches; spec review from c6, one security review per task (orchestrator/*.py glob).
- 14:58 headless decision (packet: T-1038 held_task, spec_review T-1064 request_changes, expected output fix-round spec): RESPEC, not a fix round. T-1038 was never executed (worktree null), so a fix round with depends_on=[T-1038] could never dispatch (rule from T-0839/T-0847). All four T-1064 risks confirmed by Planner grep against memory_store.py on goal/T-0991 (KINDS has reference; source_tasks is a list; search() limit=20 default and LIMIT 0 yields nothing; tags never contain a literal constraint; no set_tier helper; bus.STATUSES lacks merged and superseded while 40 task files carry superseded). Filed T-1078 (S2a1 v2, c6, respec_for=T-1038, depends_on=[T-1036]) binding all six suggestions: open-status allowlist for pinning, memory_store.all_records unbounded accessor, reference weight 1, ANY-open over source_tasks, class heuristics not tag matches, set_tiers tier-only update with zero-write idempotency; scope grows to memory_store.py and tests/test_memory_store.py (8 files, accepted: two small store helpers). T-1038 superseded; T-1039 depends_on re-pointed in place to T-1078 (ids downstream unchanged).

## GOAL (user 18:25 "flip autonomous to true and run one goal"): R22 — spawn.render validates placeholders against the template, dispatch and run_worker hold on render errors, review respawn retries and caps
Complexity 5 (two files in orchestrator/, both `[review]` security paths → one security review each on security_review_tier; below spec_review_min 6, so no spec review). Route: clear localized change → two atomic specs, T2 depends_on T1 (both edit daemon.py and tests/test_daemon.py), deterministic gate, human PR. Zero scouts: all facts below are Planner grep, provenance repo.
[planner].autonomous flipped to true in .orchestrator/pool.toml at 18:30 (comment records the date; revert path: set it back to false). 18:40 user: "planner" added to account B role_affinity so pool.pick("planner") has headroom while A is over budget (`pick planner --model` now answers B, fable); revert path: remove the word from the list.

### Facts (grep 18:10-18:25)
- spawn.render (orchestrator/spawn.py:101-113) substitutes each kw sequentially with str.replace, then raises unfilled_placeholder on ANY remaining double-brace token in the rendered text. Consequences (gotchas 2026-09-21 ×3): a spec or diff that quotes a placeholder kills render; a value containing another key's token gets substituted twice.
- Call sites: daemon.dispatch execute path daemon.py:852-856 renders in the tick thread before spawn_async and before complete(dispatched_at), so the exception escapes dispatch and the task is re-stamped every lease with no event; daemon._dispatch_fresh_fix daemon.py:554-563 (called from 585, inside a worker thread); spawn.run_worker spawn.py:757-779 for review/challenge/spec_review/execute-fallback/scout (thread dies silently after the account was picked but before claim).
- Holding helper exists: daemon.hold_failed(tid, error_key, stage_label, exc) daemon.py:484-492 (status held, hold_reason "<stage> failed: <ExcType>", pipeline[error_key], notify). auto_fix_round (daemon.py:141-) iterates every held execute task and may create fix rounds; a render error is a spec defect the Planner must fix, never a fix round.
- Review respawn (daemon.py:922-962): a queued review/spec_review is respawned once (pipeline.respawned_at); `if respawned_at > requeued_at: continue` blocks any further retry forever, so a respawn whose worker died (render error, crash) leaves the task queued with no worker and no visible reason (gotcha 2026-09-21 T-0686/T-0712). respawn_after_s from [daemon] (default 120).
- Tests: tests/test_spawn.py (79 tests; test_render_flags_unfilled_placeholder:85 must stay green: missing packet key still raises "unfilled_placeholder: packet"), tests/test_daemon.py (204; test_dispatch_respawns_requeued_review_once:982 calls dispatch twice immediately and expects one spawn; must stay green). Harness: tests/_harness.py single ORCH_ROOT, unittest.TestCase.

### Evidence caveat for the autonomous run (why this goal may still produce only skip rows)
- planner_runs._session_attached() (planner_runs.py:212-232) is true while this interactive session lives (ORCH_DAEMON_HOST=mcp in the MCP server hosting the daemon; .orchestrator/planner_session.json pid 53524). run_group skips BEFORE routing and shadow launch with skip reason planner_session_attached (planner_runs.py:1279-1286). Headless launches, router decisions and Opus shadows only fire once this session ends and a standalone `uv run orchestrator daemon` runs the remaining decision points.
- pool.pick("planner") (pool.py:404-422) needs role planner in role_affinity: only account A has it, and A is at 67.1M of 30M daily tokens → skip no_account_headroom until the daily reset, or until the human adds "planner" to account B's role_affinity in pool.toml.
- Plan: file both specs now, let the in-process daemon execute, gate, security-review and merge into goal/T-0755; leave the closable point (retrospective + PR) and any held point to the headless Planner after the human exits this session and runs the standalone daemon. Skip rows in runs/sched/planner_skips.jsonl are themselves the first telemetry.

### Observed under autonomous = true (18:30-18:35)
- planner_skips.jsonl grows six rows per tick: planner_runs.decision_points (planner_runs.py:168-211) yields closable for six long-closed goals (T-0004, T-0065, T-0240, T-0260, T-0353, T-0489) because it never checks the goal task's own status; decision.route eliminates them (routine or unknown_gate) so no launch, but it is noise. Backlog: closable only for goals whose triage task is not done/failed.

### Decomposition
- T1 c5 execute: spawn.render validates against the template token set, single-pass substitution; dispatch, _dispatch_fresh_fix and run_worker hold with hold_reason render_error and notify; auto_fix_round skips render_error holds. Scope orchestrator/spawn.py, orchestrator/daemon.py, tests/test_spawn.py, tests/test_daemon.py.
- T2 c3 execute (depends_on T1): review respawn retries every respawn_after_s while queued and unassigned, counts pipeline.respawn_count, holds with hold_reason respawn_exhausted at [daemon].respawn_max (default 3). Scope orchestrator/daemon.py, tests/test_daemon.py, .orchestrator/pool.toml.
- Then: gate on goal/T-0755, retrospective (record.sh), PR goal/T-0755 → main. Read `scorecard --planner-routing`, `promotion`, and runs/sched/planner_skips.jsonl.

### Progress
- T1 T-0756 merged into goal/T-0755 at 77fb206 (18:12 UTC+2 stamp chain: gate green first attempt, gate_reds 0; security review T-0758 approve, review_reason security_paths:orchestrator/*.py). Diff 4 files +147/-38. Codex's own final full-gate run was red on one new test it then fixed; the external gate is the bar and passed.
- T2 T-0757 became ready on T1's merge and was dispatched to Codex (terra) at 18:13:21. The Planner session restarted at 18:14 (daemon.lock re-created), which killed the MCP server and its codex exec child: the task sat running with pid null, no process, clean worktree at 77fb206. reconcile_dead never fires for pid-null tasks (gotcha 2026-09-19, polish candidate still open). 18:17 Planner ran daemon.reconcile_dead(T-0757) by hand → requeued; in-process daemon redispatches. One background wait armed on merged/held/failed.
- Backlog from this incident (c3, next goal): reconcile_dead treats running tasks with pid null and claimed_at older than the daemon start as dead; codex()/codex_reply() record the codex exec pid on the task.

### Progress (closed)
- T-0757 merged into goal/T-0755 at 160f28c (18:35; gate green, 0 reds; security review T-0759 approve). Retrospective recorded in decisions.md 2026-09-21 (378 lines, compact-memory due). Branch pushed, PR opened 18:45.

### Next step
Human: review and merge the PR goal/T-0755 → main, then git pull the checkout and restart with f orch. No open goal. Backlog (next goal candidates, in order): (c3) reconcile_dead treats pid-null running tasks with claimed_at older than the daemon start as dead and the codex tools record the pid; (c2) planner_runs.decision_points yields closable for long-closed goals; (c2) tests/test_scorecard.py:858 live-bus leak; compact-memory.

## CROSS-REPO GOAL (user 18:50, CLOSED 19:39): highlight the chat that has the active proposal; make accepting proposals work
Diagnosis (Planner grep + Hetzner DB 18:40-18:55, provenance repo/db): the badge counts GET /proposals; the one pending proposal (ops health_watch "Health data has stopped arriving", created 2026-09-19 21:18, expires 2026-09-22 21:18) has turn_id null, so it belongs to no conversation and the iOS app has no UI at all to decide it (no Inbox; ApproveInline only renders inline for tool calls with proposal_id). list_pending() does not return conversation_id, so no client can highlight the chat. ApproveInline swallows errors, never refreshes the badge, cannot answer the 409 requires-reconfirm path.
Route: two repos, each via `orchestrator goal start --account B` with a full brief (scratchpad brief-kgpt.txt, brief-kgpt-ios.txt); zero scouts.
- kgpt T-0014 (c2, pid 52094, log kgpt/.orchestrator/runs/planner-T-0014.log): list_pending LEFT JOIN kernel.turns → conversation_id; tests in kernel/tests/test_agent_loop.py. Scaffold commit 3a5d6b3 on kgpt main (local; revert path git revert 3a5d6b3). kgpt cowork tasks T-0003..T-0011 parked as failed reason "parked" (18:55, reversible: status queued; old plan.md on branch goal/T-0001). kgpt main had no tracked .orchestrator on main, fresh install.
- kgpt-ios T-0006 (c5, pid 52256, log kgpt-ios/.orchestrator/runs/planner-T-0006.log): T1 model+APIClient.approve(reconfirmed)+AppState list/refreshProposals; T2 sidebar highlight + "Needs your decision" section + ApproveInline error/409/refresh. Scaffold commit 0321e34 on kgpt-ios main (local).
- 18:43-18:49 outcome: both headless Planners died with "You're out of usage credits" on account B (kgpt after 17 turns with no child task; kgpt-ios after filing T-0007/T-0008, T-0007 merged). Interactive Planner took over: kgpt-ios daemon --once passes gated and merged T-0008 (0fbc93a), retrospective in kgpt-ios decisions.md, GOAL T-0006 done, PR 17 https://github.com/K3NTAW/kgpt-ios/pull/17. kgpt: Planner filed T-0015 (c2) by bus.create_task; daemon --once cannot dispatch (the worker thread dies with the process; dispatched_at stamp had to be cleared by clear_stage), so a long-lived `orchestrator daemon` runs from this session (background Bash, 10 min cap, re-arm if it expires mid-task). T-0015 running on Codex astra since 18:53.
- Phone: Debug build of goal/T-0006 (0fbc93a) built for generic iOS at scratchpad kgpt-dd2; the iPhone is unavailable over the local network since ~18:50; a background wait installs and launches it as soon as devicectl lists it available.
- kgpt round 1 (T-0015, Codex astra, commits 3084303 d54a7a7): change correct, one assertion compared UUID to str; the worktree gate could not run database tests (.env.test untracked, 419 skips) or the web typecheck (no node_modules). Planner: tests.sh fixed and committed on kgpt main e200247, goal/T-0014 cut from it, T-0015 and the auto fix round T-0016 marked failed/superseded, T-0017 (round 2, worktree preset to wt/T-0015) created 19:10, dispatched to Codex luna 19:13 under a manual `orchestrator daemon` (background Bash, 10 min cap; re-arm if it expires mid-run). One wait armed on T-0017 terminal state.
- kgpt round 2 (T-0017, Codex luna, 203e954 after rebase onto goal/T-0014): assertion fixed; daemon held it as gate_red with "acceptance tests missing" because acceptance.py _TEST_ID matches `tests/...py::name` inside `kernel/tests/...`, resolving to wt/tests/ (BACKLOG c2 for this repo: honour a directory prefix before tests/). External gate red for a real reason: ruff lints the committed .claude scaffold (138 errors; main lacks the exclusion that goal/T-0001 33c1afb has). Round 3 T-0019 (19:30, same worktree) adds the ruff extend-exclude; acceptance phrased without path::name ids. Auto fix rounds T-0016/T-0018 retired as failed/superseded.
- kgpt round 3 (T-0019, Codex sol, e268a30): ruff extend-exclude for the scaffold; gate green, 1014 passed with database tests running; merged into goal/T-0014 19:37. Retrospective in kgpt decisions.md, GOAL T-0014 done, PR 38 https://github.com/K3NTAW/kgpt/pull/38 opened 19:39. CROSS-REPO GOAL CLOSED.
- Phone: user is away from home; the phone cannot be reached tonight. Install later from scratchpad kgpt-dd2 (or rebuild from the merged main) with devicectl when the phone is on the network.
- 19:45-19:50 release on user instruction: all three PRs merged (e8c4687, 0d8b192, 8433149); Hetzner make up rebuilt and recreated every container, all healthy, healthz 200, gateway source carries the LEFT JOIN. Details decisions.md 2026-09-21 19:50. Backlog: kgpt httpx2 2.10.0 → 2.12.0 (PYSEC-2026-3846/3848/3849, CI audit step red).
- Escalation for the human: account B has no Claude usage credits left (billing); account A is over its daily planner budget. Headless Planners cannot run until credits return; Codex executors are unaffected.
- After both PRs merge: human deploys kgpt (ghcr image on Hetzner) for the highlight field; Planner builds kgpt-ios onto the phone again (xcodebuild Debug + devicectl, as at 18:33). One background wait armed on both planners exiting.

## GOAL (user 19:58 "clean up the loose ends that you can", CLOSED 20:22): kgpt httpx2 2.10.0 → 2.12.0 so the CI dependency audit passes; tests.sh leaves worktrees clean
Complexity 2, clear localized change, zero scouts (facts grepped 19:59: uv.lock pins 2.10.0; kernel/pyproject.toml:21 floor httpx2>=0.28; ci.yml audit = uv export --frozen --no-dev --no-emit-project | pip-audit). Route: one Codex execute task on the kgpt bus, manual daemon from this session (goal start is dead: account B credits). pyproject.toml and uv.lock are kgpt [review] security_paths → one review on sonnet; account A still serves reviews (T-0020 review of T-0019 ran on A/sonnet at 19:37 and approved; the daily budget only gates the planner role), so gate → review → merge → PR can complete tonight. Planner-side: .orchestrator/tests.sh v2 (removes the node_modules symlink on exit) committed on kgpt main before the goal branch is cut.
Outcome: T-0022 (Codex terra, 2b76fe9) httpx2 floor >=2.12, lock 2.13.0 (httpx2, httpcore2), pip-audit clean, gate green; security review T-0023 on account B/sonnet approve (B serves reviews; only its Fable planner launches fail on credits); merged into goal/T-0021 20:19; PR 39 https://github.com/K3NTAW/kgpt/pull/39 open for the human.
Loose ends NOT cleanable by the Planner: orchestrator local main behind origin (planner-mode blocks merging origin/main in the root; human pulls), kgpt-ios local main 2 commits ahead (push main blocked by guardrails), phone install (user away), Claude credits (billing).

## PROGRAM (user 20:30): KGPT Adaptive AI Harness Optimization P0–P24 — lives in the kgpt repo
Source of truth: kgpt/.orchestrator/roadmap-harness.md (verbatim brief) and kgpt/.orchestrator/plan.md (classification c10, phase plan, three named uncertainties U1–U3). GOAL T-0024 on the kgpt bus; P0 scouts T-0025 (architecture map + 22 dimensions), T-0026 (orchestrator prior art adopt/adapt/skip), T-0027 (telemetry + eval gaps for P1) launched 20:48 via spawn.run_worker from this session (nohup, logs in scratchpad scout-T-00xx.log). P0 synthesis done 20:40 (kgpt plan.md ee07b36). Filed T-0028 P0 doc (c3, Codex astra running) and T-0029 P1a telemetry (c6): spec review T-0030 request_changes (unguarded provider.stats, error-row model source, ChatRequest.modality, gateway test) → v2 T-0031 filed 20:46 with those changes binding, T-0029 and the daemon's auto re-spec round T-0033 marked failed/superseded; spec review T-0032 running on sonnet. kgpt daemon now under a detached supervisor loop (scratchpad kgpt-daemon-supervisor.sh; stop by creating scratchpad/STOP-kgpt-daemon) so the 10-minute background-shell cap no longer kills executors. P1a merged 21:25 via fix round T-0046 (review T-0038 caught the role column being a constant; the kgpt daemon's autonomous planner-decision path wrote the re-spec T-0045, which the Planner re-filed as T-0046 under fix_round_for because depends_on on a held root never becomes ready). Dependents T-0039 P1b, T-0040 P1c, T-0041 P20 ready; T-0042 P21 behind P20. 2026-09-22 10:58-11:22: user merged PR 42 (9161840); deploy_flags.py set the two shadow flags and ran make up; gateway crash-looped (jev.py imports httpx, kernel ships httpx2); rollback via make restart at 57932e5 (make up failed at migrate, DB ahead); hotfix GOAL T-0106 → T-0107+T-0108 merged, PR 43 https://github.com/K3NTAW/kgpt/pull/43 open. 14:45 kgpt Phase 4 COMPLETE: PR 44 https://github.com/K3NTAW/kgpt/pull/44 (P4a tool gate, P17a tool economy, P6a voice + first-token latency, P19a failure taxonomy, completions; migration 0029); kgpt-ios PR 18 (voice modality) still open; kgpt daemon supervisor and daemons stopped. 12:10 user: do the next phase → kgpt Phase 4 GOAL T-0109 (branch from 0185178): P4a T-0110 (c6, Jev tool-call gate + migration 0029 with all phase-4 columns) in spec review; P17a/P6a/P19a filed after its spec approval; kgpt-ios voice-modality client task done: T-0012 cd0a48e merged into goal/T-0010 after review approve (T-0011 had merged as an empty diff when Codex stopped to ask about scope; gotcha recorded); PR 18 https://github.com/K3NTAW/kgpt-ios/pull/18 open for the human; ios supervisor stopped. P4a spec rounds: T-0111, T-0113 request_changes; v3 T-0114 in review T-0115. Plan detail in kgpt plan.md. 11:27 user merged PR 43 (0185178); deploy_main.py deployed it: all healthy, healthz 200, no tracebacks, shadow flags + key live. Incident closed 11:35. Earlier note: after the human merges: git checkout main + git pull + make up on Hetzner (via a python ssh runner), verify healthz, restart status, Traceback grep; the shadow flags are live in .env. 2026-09-22 09:40-10:55: overnight review hold cleared (accounts reset), scorecard tests merged via T-0094/T-0098, compose passthrough T-0099+T-0102 merged (review caught the empty-env override; env_ignore_empty), TYPESAFE_API_KEY pushed to the Hetzner sops env from the token store (push_secret.py, value never shown), main redeployed 10:30 (13 healthy, tables model_calls/harness_decisions present), Phase 3 PR opened, daemon supervisor stopped. Next after the human merges: make up, .env shadow flags, make restart. 00:32 user: merge and continue → PR 40 merged fa6e042, PR 41 retargeted to main and merged 57932e5 (its red CI was the pre-bump audit on the stacked base). U4: shared Jev endpoint; U5: luna stays for fast/classification. Phase 3 GOAL T-0073 cut from 57932e5: T-0074 P3a (c6), T-0075 P15a (c4), T-0076 P16a (c5, depends on both). Daemon supervisor restarted. Checkpoint 2 done 2026-09-22 00:05: Phase 2 GOAL T-0062 (branch from goal/T-0024): P2a T-0069+T-0070 (classifier + router + model_routing off/shadow/active, three spec-review rounds then waived), P22-lite T-0072 (routing scorecard, promotion evidence) merged; PR 41 https://github.com/K3NTAW/kgpt/pull/41 stacked on PR 40. kgpt daemon supervisor stopped; nothing running. Checkpoint 1 done 23:12: P1b T-0039, P1c T-0040, P20 T-0041 (rounds T-0053/T-0057 after reviews T-0048/T-0054/T-0056 and a rebase_changed_diff hold cleared by hand), P21 T-0042 all merged; kgpt PR 40 https://github.com/K3NTAW/kgpt/pull/40 open for the human. Named uncertainty U4 for the human in kgpt plan.md: which Jev endpoint kgpt may call before P3. Earlier note: read the three results once, synthesize into kgpt plan.md, write P0 architecture-map doc task + P1 telemetry/eval specs (spec review ≥6), run the kgpt daemon from here. kgpt PR 39 (httpx2) merged c44c941 on user instruction 20:28; kgpt local main is 2 commits ahead of origin (retrospective + program files) and cannot ff, human pulls or the next PR carries them.

## PROGRAM (user 14:10): Jev Context Intelligence & Harness Economy — this repo. Brief verbatim in .orchestrator/roadmap-context.md; GOAL T-0760.
Classification: complexity 10, architectural, cross-cutting (packets, retrieval, Jev call sites, tool exposure, prompts, routing, scorecards). Route: high-risk → P0 audit by three read-only scouts (this repo, spawn_scout), then P1 telemetry with spec review, then shadow-first features. Zero behaviour change before the baseline. Named uncertainties: U-C1 which packet sections are byte-identical across roles for one task and how large (Scout C-A); U-C2 every Jev call site's outbound payload and existing reuse/dedup mechanisms (Scout C-B); U-C3 measured tokens per role and per accepted task from the run logs, and where handoffs rebuild context (Scout C-C).
Prior art to reuse, not reimplement (recall 14:15): P1b role-specific review/spec-review/scout packets (T-0462), scout_evidence reuse_check, jev_gate (E3), jev_route/jev_planner shadow routing, decision_log KINDS, promotion FEATURES, attribution/scorecard, Phase H/I efficiency work (T-0353, T-0445).
Goal branch: goal/T-0760 cut from origin/main (local main is behind origin; worktrees base on the goal branch). Phase order in the GOAL spec.
P0 partial synthesis 14:30 (C-A T-0761 summary, C-C T-0763 findings; C-B T-0762 re-spawned after an rc=1 exit):
- Packets (C-A): execute = packet body capped 4800 chars (~1200 tok, spawn.py:195-366) + execute.md instructions (~380 tok) + spec/acceptance/scope DUPLICATED inside the same prompt (once in the packet spawn.py:298-305, again via execute.md's literal Spec/Acceptance/Scope lines; call sites spawn.py:788, daemon.py:574, daemon.py:870). review = spec/acceptance/scope/diff/gate with diff ≤8000 chars (~2000 tok, spawn.py:426-505) + review.md ~150 tok. spec_review uncapped spec/acceptance/scope/depends_on/tests (spawn.py:508-524). scout = question/expected output/scope tree/memory titles (spawn.py:527-546). headless planner = planner.md 4307 B (~1077 tok) as --append-system-prompt (goals.py:342-345) + decision packet ≤6000 chars (planner_packet.py:225-250). Spec/acceptance/scope are re-pulled from task fields by every role packet with no shared object: the P1 amplification to measure. Tools: workers get .mcp.worker.json (bus only), scout/review add read-only github, planner adds github + orchestrator MCP; --strict-mcp-config; non-execute roles get --disallowedTools Edit,Write,NotebookEdit (spawn.py:605-606). Only the headless Planner has a system prompt; every other role's instructions ride in the -p prompt.
- Baseline (C-C, runs 2026-09-20..22, fields role/input_tokens/output_tokens/cache_read_input_tokens/usd/task, bus.py:278-335): execute n=155 mean_in 964k p90 1.78M (mean cache_read 909k), out 7.5k, usd 6.91 where logged; review n=127 mean cache_read 658k, out 14.3k, usd 0.46; spec_review n=60 cache_read 859k, out 15.4k, usd 0.57; scout n=9 cache_read 1.29M, out 13.5k, usd 0.81. Per accepted execute task (77 roots, lineage walked by hand): mean 3.73M tokens, median 2.44M, max 32.3M (confidence 0.55). Fix rounds = 19.8% of tokens (undercounts review re-passes). memory/jev_route/scout_decision rows log no tokens (unmeasured, not free). Jev: 750k/956k/385k input tokens per day 09-19..21. Planner launch/escalation counts unverifiable (no planner rows, planner_runs.json absent: scorecard.py:434-439).
- Handoffs (C-C): resume = ['resume', thread, delta] (executor.py:416-417, cheapest); fresh fix round resends the full packet + repair delta (executor.py:185-197, 420-428; MAX_ROUNDS 5); Opus→Fable escalation packet is a filtered slice capped 6000 chars (planner_packet.py:253-284); handover rewrites plan.md's snapshot every ~15 min capped 120 lines (handover.py:12-23).
- Retrieval and Jev (C-B T-0762): memory_recall runs independently per role packet (spawn.py:276 execute/review, :537 scout; notes+bus layers, title+scope query, no shared cache); scout_evidence.py:29 duplicates the recall loader; repomap is CLI/merge-time only (cli.py:282, merge.py:85), not in packets; the graph layer reaches only reuse_check (scout_evidence.py:80), never execute/review packets; reuse_check (scout_evidence.py:74-106, keys title+objective, freshness 14 days + _bus_fresh base-vs-moved-paths) is the only cross-task dedup and runs once per scout spawn (mcp.py:85); jev_gate has its own session-scoped repeat dedup (jev_gate.py:172-199). Jev call sites and payloads (P20 input): jev_gate.py:221 (title, spec[:1500], acceptance, scope, role, 20 redacted recent tool calls, proposed input[:300]; needed/redundant/destructive; fail-open allow), jev_route.py:97 (redacted spec[:1500]/acceptance/scope/complexity/task_class/memory titles[:10]; 7 signals; neutral on None), jev_planner.py:126 (state() capped 4000 chars; 5 signals; jev_unavailable on None), jev_sched.py:194 (pair titles/scope/spec[:400] for ≤8 pairs), jev_points.py:148 (title[:300]/spec[:600]/scope[:300]/evidence with content stripped), jev_rank.py:41 (goal_text UNREDACTED by rank itself, caller's job; no mode key; 40-item batches). decision_log KINDS: 13 kinds (routing, wave, capacity, concurrency, merge_pressure, stale, jev_sched, allocation, strategy, speculation, scout, review_plan, planner_route); outcome() only at executor.py:313 for allocation. P26 needs context_selection, evidence_reuse, retrieval, tool_disclosure, instruction_loading, action_gate (rename of gate), workflow_strategy (map to strategy).
- P0 COMPLETE 14:40. Named decisions for phase 1: P1 telemetry attributes tokens per packet SECTION at build time (deterministic chars/4 with a fixed divisor, plus a sha256 per section so repeats across roles are counted) and per run via bus.log_run; candidate = every section built before caps, presented = after caps; amplification = sum(section tokens across all runs of a lineage) / unique-by-hash. First deterministic win after the baseline: drop the duplicated spec/acceptance/scope from the execute prompt (execute.md vs packet).
- Implication for P1: the biggest lever is the cache_read mass (packets are near-fully cached across calls) and the per-role re-pull of the same task fields; attribution needs per-section token splits at packet build time (spawn.packet_meta already carries chars/est_tokens/hash/version/sources: extend it per section) and a lineage_root-based per-task total (scorecard.by_task).
15:00: v2 T-0767 → spec review T-0768 (daemon dispatch paths carry instruction tokens, log_run derives context from packet_meta, fallback boundary at the repair-delta marker) → v3 T-0769 filed; a fourth round is waived per the three-round precedent. T-0765 P0 doc done (Codex terra a6b4a78, docs/context-economy/00-audit.md), gate green, merging. 14:55: T-0764 P1 v1 → spec review T-0766 request_changes (amplification shape, instruction tokens at the call site, executor/mcp fallback parsing full prompts or bare deltas, grouping by goal not lineage) → v2 T-0767 filed with decisions A/B/C; T-0765 doc released. Originally filed 14:50: T-0764 P1 context telemetry (c6, spec review first: section_meta per packet, candidate vs presented, instruction tokens, bus.log_run context field, orchestrator/context_scorecard.py with amplification per lineage, `orchestrator scorecard --context`, docs/context-economy/01-telemetry.md, six named tests); T-0765 P0 doc docs/context-economy/00-audit.md (c3, held until T-0764 has its worktree, then requeued). Both under the MCP-hosted daemon of this session. After P1 merges: baseline snapshot recorded in decisions.md, then P26 kinds + P23/24/25 modes (adapt promotion FEATURES), then the deterministic duplication fix (execute prompt double spec) as the first measured win. Status 14:35: scouts T-0761 (packets), T-0762 (retrieval/Jev/decision plumbing), T-0763 (routing economics + run-log baseline) running on account A, launched one at a time. Next: read each result once, synthesize into this plan, then file P1 context telemetry (spec review) and the P0 docs note.

15:30: BASE GATE RED for every task: tests/_harness.py copies live pool.toml (autonomous = true since 18:30 yesterday), so daemon.auto_fix_round calls planner_runs.build_ctx → bus.get(parent 'T-0043') → KeyError in two test_daemon tests. T-0765 (P0 doc, a6b4a78) held gate_red for that reason only. Fix T-0771 (c2, Codex sol): tolerate a missing goal in auto_fix_round + harness forces autonomous=false. Queued behind it: T-0772 P26 kinds + P23/24/25 modes (config/promotion only, depends_on T-0771); T-0773 execute-prompt dedup (packet only; depends_on T-0769, T-0771). After T-0771 merges: re-gate T-0765 (clear gated_at). 15:10: T-0771 branch (f601396 + fix 4ba6bb0, harness rewrites autonomous=false and strips planner from B) external gate green 1027 after one RunGuards flake; review T-0776 of fix round T-0775 saw an empty scoped diff (fix landed outside the fix task's scope) → T-0775 and auto round T-0777 superseded, security review T-0778 on the root's full scope running; merge T-0771 on approve, then sync wt/T-0769 (sync_wt.py in scratchpad) and release T-0769 (held awaiting_base_fix) for its gate. 15:20: T-0771 merged into goal/T-0760 (4ba6bb0) after review T-0778 approve + external gate green; wt/T-0769 and wt/T-0765 synced onto the goal head and released (status done, gate cleared). Phase 2 task graph: T-0772 modes+kinds (running) → T-0773 execute-prompt dedup (also needs T-0769), T-0774 Jev boundary (c4, security), T-0779 evidence objects + deterministic context router library (c6, spec review) → T-0780 shadow wiring into execute/review packets + scorecard columns (c6, spec review; needs T-0779 + T-0773). Checkpoint PR goal/T-0760 → main after T-0769 + T-0765 + T-0772 merge (P0 audit, P1 telemetry, modes). 15:45: P1 T-0769 review T-0782 request_changes (tests bypass real dispatch/run_worker/review_packet paths; unbounded _PACKET_BUILD_META) → fix round T-0785 (resume, wt/T-0769) running. Spec review T-0784 of the router library asked for: EvidencePool (name clash with pool.Pool), decision_row carrying kind+subject, checkable acceptance, and noted .orchestrator/roadmap-context.md is not on goal/T-0760 (committed on local main only) → v2 T-0786, wiring re-filed as T-0787 (depends T-0786 + T-0773); T-0779/T-0780 superseded. TODO when the merge queue is quiet: commit roadmap-context.md onto goal/T-0760 via a temp worktree (never while a merge is in flight: goal-branch commit loss gotcha). 16:00: T-0774 Jev boundary done in one round (6ae7183, 1036 green). P1 fix round T-0785 done (2a58adb, real dispatch/run_worker/review_packet paths exercised, _PACKET_BUILD_META capped 512). Spec review T-0788 (round 2) on the router library: failing ids live in resume_hint.failures (reuse failures.test_ids), scope|write_scope alias, evidence.fresh module-level → v3 T-0789 (final round), wiring re-filed T-0790. 16:10: P1 merged (3879094) after fix round T-0785 review T-0792 approve; T-0774 review T-0791 approve, merging. Spec review round 3 T-0793 on the router library asked only for wording (jev.redact exempt from 'never call Jev') and live relevance recomputation → v4 T-0794 with spec_review_verdict set to approve by the Planner (waiver recorded), wiring re-filed T-0795. 16:35: T-0773 never launched: its spec quoted literal double-brace placeholders, spawn.render raised unfilled_placeholder inside the main dispatch loop and the task sat queued with dispatched_at re-stamped (gotcha recorded). Re-filed as T-0799 (tokens described in words), wiring re-filed T-0800 (depends T-0794 + T-0799, Planner-approved spec), daemon fix T-0801 (hold render errors visibly; handle every executor.start status). T-0794 fix round T-0797 done (ccfd59d, 1059 green), review T-0798 running. 17:35: Root cause of silent review deaths: root checkout and the 22h-old in-process daemon (MCP pid 96058) ran pre-PR-18 code (old spawn.render). Committed Planner state (5499b63), replayed local main onto origin/main (951e700, sync_root.py), killed both MCP daemons, started the standalone daemon under scratchpad orch-daemon-supervisor.sh (STOP-orch-daemon stops it). Orchestrator MCP tools are gone for this session: use python (bus, spawn.run_worker, merge.merge, executor.start) instead. Reviews T-0804/T-0805 approved; T-0799 merged; T-0801 hit a rebase conflict with T-0799 in daemon.py dispatch (render kwargs) → fix round T-0806. Checkpoint PR 1 (goal/T-0760 → main) after T-0801 merges: body in scratchpad/pr-context1.md. Side job 17:05-17:20: Isaac added to kgpt (Access policy + kernel.users member); recorded in kgpt decisions. 17:50: PR 19 opened (checkpoint 1, 12 commits). The human merges; after merge the standalone daemon keeps running on the root checkout, which then needs `git pull --ff-only` (root is on main at 951e700, one Planner commit ahead of origin). 18:05: full program graph filed. Phase 3 (checkpoint 2): T-0800 wiring (done, in review T-0807) → T-0802 tool disclosure (c5), T-0814 conditional instructions v2 (c6; T-0812 asked for security signals passed to render, scorecard column test, size fact), T-0831 handoff economics v3 (Planner-approved after T-0813/T-0818), T-0809 read/search economy (fix round 2 T-0827 after review T-0825 caught a disabled Jev block path) → T-0832 evals + promotion evidence + --economy view (c6, spec review; depends on all four). Known merge-conflict hotspots: cli.py (T-0808/T-0809/T-0810 all add scorecard flags) and spawn.py (T-0802/T-0803): expect rebase fix rounds, keep them mechanical. 19:20: T-0802 tool disclosure merged (fix round T-0821). T-0809 round 1 (T-0822) silently disabled Jev blocking for reads and softened the regression test; review T-0825 caught it; round 2 T-0827 (3420810) restored it, in review. Account B quota cooldown (17:48-18:06) held every review/spec review: re-spawned by hand (T-0819/T-0820/T-0817/T-0818); the daemon does not retry held workers on its own after a cooldown → gotcha candidate. 20:30: PR 19 MERGED (32343f7) = checkpoint 1 only (12 pushed commits). 20:45: checkpoint 2 (T-0800, T-0802, T-0809, T-0814, T-0831; 9 commits) pushed and opened as PR 20 https://github.com/K3NTAW/orchestrator/pull/20, awaiting the human. Root main replayed onto origin/main (d779890) and the daemon restarted (pid 69144); the root still lacks checkpoint-2 modules until PR 20 merges, so the daemon's shadow features stay at checkpoint-1 level for now. A second Planner (headless autonomous, account B) is active on this bus: it superseded T-0827 and filed T-0837; its plan commits (845b250) are on main. Remaining: evals + promotion evidence + --economy: T-0832 → v2 T-0839 (T-0838) → v3 T-0841 Planner-approved after T-0840 (isolated STATE during eval, context_eval.json persisted for promotion, review_packet cfg override) → PR 21. Then the program is complete in shadow; P5 (Jev on the ambiguous bucket) and P7 (query-time summaries) are follow-ons on evidence.
20:50 headless decision (packet: T-0832 held_task, spec_review request_changes, expected output fix-round spec): STALE, no task filed. T-0832 was already marked failed at 20:40 (reason superseded by T-0839) by the previous Planner run; T-0839 is the v2 spec (queued, depends_on T-0802/T-0814/T-0831/T-0809 all merged) and binds every T-0838 risk: evaluate() caps at stay with reason shadow_quality_unmeasured when any quality delta is None; spawn.packet(task, worktree, *, cfg=None) with spawn.py + tests/test_spawn.py in scope (test_packet_accepts_cfg_override); context_scorecard.build() per-(role, task_class) via attribution.task_class; P28 figures from context_scorecard tool_tokens_* (tool_catalog.py out of scope); tests/test_context_scorecard.py extended in place. Facts re-verified on goal/T-0760 (0f550d7): tool_catalog.minimal_set:96, handoff_scorecard.by_start:140, attribution.task_class:128, spawn.packet:544, context_scorecard tool_tokens_minimal:58. A fix round with depends_on=[T-0832] would never dispatch (depends_on needs merged; T-0832 never executed, no worktree), so the packet's required output is wrong for this state; respec via T-0839 is the correct route (same pattern as T-0786/T-0789/T-0794). Spec review T-0840 of T-0839 running on claude:A (sonnet, pid 70505, since 20:41); standalone daemon alive (pid 69143, started 20:47) and dispatches T-0839 on approve, then gate → security review (spawn.py/promotion.py/cli.py are security paths) → merge into goal/T-0760 → PR 20 update or PR 21. Checkpoint 2 is https://github.com/K3NTAW/orchestrator/pull/20 (OPEN): PR 19 merged only checkpoint 1 (head 6ca204e); origin/main..goal/T-0760 is 9 commits. 21:40: T-0841 merged (review T-0842 caught two join bugs → T-0843). PROGRAM COMPLETE IN SHADOW. PR 20 now carries checkpoints 2+3 (11 commits) and awaits the human; after its merge: git pull --ff-only in root (sync_root.py) and restart the daemon (kill the daemon pid; the supervisor restarts it). Deferred by design: P5 Jev ambiguous-bucket classification, P7 query-time summaries. Goal T-0760 stays open until PR 20 merges; then retrospective + record.sh + close. 22:05: user said 'do the merges': PR 20 (41be3d8), kgpt PR 44 (071601c), kgpt-ios PR 18 (c99047c) merged; root synced (6020e26) and daemon restarted (pid 42349); kgpt deployed with make up. Remaining for T-0760: retrospective (record.sh draft/add) and close the GOAL task. 22:55: user 'do that': kgpt flags active on Hetzner (5 features, healthy), orchestrator [instructions] active; goal T-0760 reopened for the four active paths: router active T-0845 → v2 (T-0850: keep symbols, never inject in-scope file bodies, routed-findings section for reviews, jev_gate recovery wiring, header marker, atomic trim) → next id; tool_disclosure active T-0849 → re-filed on the v2; handoff routing T-0847 (spec review pending); read suppression T-0848 → v2 T-0853 (T-0852: range-aware, gate_mode independent, call index, summary total/since). All go to PR 21. 01:50: router active spec T-0855 Planner-approved after round 1 (T-0850) folded into v2 and two review attempts (T-0857) returned no verdict within budget. Discovery T-0884 in fix round T-0886 (inspection gate on every path into testing). 03:10: active paths merged on goal/T-0760: read suppression T-0860 (+T-0868), handoffs T-0858 (+T-0900: calendar-day cap, per-group active_share), router active T-0855 (+T-0903: review T-0901 caught unredacted evidence reaching the active packet; fixed with a runtime-fixture test); tool disclosure T-0856 → v2 T-0910 → v3 T-0918 Planner-approved (T-0912: NEEDS_TOOL_PREFIX result key, summed usage release, total_cost_usd, acceptance tests); specialists re-filed T-0919 (depends T-0914 + T-0918). Skills: telemetry T-0894 fix round T-0906 (gate rows use tool_target, not input). PR 21 OPEN https://github.com/K3NTAW/orchestrator/pull/21 (9 commits, all four active paths + hygiene); awaits the human. 06:05: tool disclosure T-0918: three hand-run reviews posted no parseable verdict (26-27 turns each) while the daemon's own retry T-0924 posted one valid medium comment (respawn cap must persist via pipeline.tool_escalation_used) → daemon auto fix round T-0928 done, in review. Gotcha candidate: sonnet reviews of 11-file diffs exhaust their turns without posting; the daemon's retry with the standard packet worked. 10:10: skills merged so far on goal/T-0861: registry T-0863, compaction T-0867, discovery T-0884(+T-0886), telemetry T-0894(+T-0906), routing shadow T-0913(+T-0926), scorecard T-0938(+T-0946), active routing T-0939(+T-0948 → rebase round T-0956), learned skills T-0954. Left: specialists T-0940 → v2 T-0961 → v3 T-0964 Planner-approved (T-0963: tool-disclosure integration shadow-only until goal/T-0861 carries PR 21, worker roles only, real catalog keys, reviewed-task id for rule 3), Jev routing T-0942 → v2 T-0967 → v3 Planner-approved (T-0969: ask resolved at call time, lazy imports against the executor↔skill_router cycle, batch truncation order, headroom under max_selected; id in the next line), promotion + P36 suite re-pointed; specialists T-0964 merged (+T-0970). Jev routing v3 = T-0971 (executing), Stage 10 = T-0972. Checkpoint PR for the skills program after T-0955 merges. 10:55: CROSS-GOAL BRANCH FACT (spec review T-0963): the context program's active paths (T-0918 tool disclosure etc.) live on goal/T-0760 (PR 21), not on goal/T-0861, although the daemon treats T-0918 as a satisfied dependency (merged_into is set). Specialists v3 therefore keeps tool-disclosure integration shadow-only; FOLLOW-UP after PR 21 merges: rebase goal/T-0861 onto main (temp worktree, guarded update-ref) and file the active hand-over. Rule: a depends_on across goals is satisfied only when the dependency is on main or on this goal's branch. 09:20: PR 21 (5646e4b) and PR 22 (860c1cf) merged; merge-resolution T-0984 (fast-forwarded by hand) carried main into the skills branch; Skill matcher added to the jev-gate hook and shipped via PR 22; root synced and daemon restarted. Remaining follow-up: file the specialists' tool-disclosure hand-over (now that both programs share main) as a small task under a new goal when the user wants it; watch the kgpt harness scorecards for the five active features. Next after phase 2: P2 evidence objects + P3/P4 context router shadow (spec review), P10 tool catalog (Level 0/1/2) shadow, P12/P13 instruction modules audit, P20 Jev privacy audit tests.

21:10 headless decision (packet: T-0839 held_task, spec_review request_changes, expected output fix-round spec): STALE, no task filed. T-0839 was already failed at 21:00 (reason superseded by T-0841) and T-0841 is the v3 spec, running on codex in wt/T-0841 with spec_review_verdict approve (Planner waiver after two rounds T-0838/T-0840). Planner grep confirmed all three T-0840 risks against the code (evidence.EvidencePool writes STATE/evidence/<goal>.jsonl; promotion.collect/report take root=STATE with no eval artefact; spawn.review_packet reads Pool().cfg at 497 and 533 on main) and confirmed v3 binds each: context_eval.isolated_state(root) + test_eval_writes_nothing_into_production_state; STATE/context_eval.json with suite_passed threaded into promotion.collect/evaluate + two tests; review_packet(task, reviewed, *, cfg=None) + test_review_packet_accepts_cfg_override. A fix round on T-0839 is wrong by construction: no worktree, no codex thread, and depends_on=[failed task] never dispatches (gotcha 2026-09-21 re-spec duplicates). Set constraints.respec_for=T-0839 on T-0841 so reconcile scores this decision as respec. Next: daemon carries T-0841 through gate, one security review on security_review_tier (spawn.py/promotion.py are security paths), merge into goal/T-0760. If T-0841 goes held gate_red or review request_changes it HAS a worktree, so the daemon's routine fix round (or a Planner fix-round spec with depends_on=[T-0841]) is the right shape.

23:20 headless decision (packet: T-0847 held_task, spec_review request_changes, expected output fix-round spec): STALE, no task filed. T-0847 was already failed at 23:05 (superseded) and T-0858 is the v2 spec (queued, depends_on=[], complexity 6, spec review T-0859 held: no account with headroom). Planner grep confirmed all five spec-review risks against the code (8 post_result failed sites in daemon.py; by_start reads only task/run rows; pool state mutates go through Pool._reservation_file under flock; the allocation-active override precedes the handoff block at executor.py:315) and confirmed v2 binds each: pipeline.handoff stamp at dispatch under bus.locked() with the day counter in the same block, reasons expected_route_cost / insufficient_evidence / gain_below_threshold / cap_reached / allocation_active, handoff yields to active allocation, daemon.py out of scope, test_active_share_and_outcomes_from_task_stamps + test_handoff_yields_to_active_allocation. A fix round with depends_on=[T-0847] never dispatches (bus.ready needs a merged dependency; T-0847 never executed). Set constraints.respec_for=T-0847 on T-0858 so reconcile scores this as respec. Next: the daemon re-spawns T-0859 when an account has headroom; on approve it dispatches T-0858 → gate → security review (executor.py is a security path) → merge into goal/T-0760 → PR 21.

## Needs the human
- Plan commit 9fde958 (T-1038 respec T-1078, decisions entry) sits on local main: guardrails block a direct push to main; push it or include it in the next PR.
- Review and merge checkpoint 2: https://github.com/K3NTAW/orchestrator/pull/20 (OPEN). After merge: git pull --ff-only on the root checkout (the standalone daemon runs on it); restart the daemon if the code changed.
- Nothing blocks the four active-path tasks (T-0853, T-0855, T-0856, T-0858): spec reviews T-0854/T-0857/T-0859 wait only on account headroom; the daemon carries them through gate, security review and merge into goal/T-0760; no human action until PR 21 opens.

## PROGRAM (user 23:20): Skill Intelligence & Dynamic Specialist Agents — this repo. Brief verbatim in .orchestrator/roadmap-skills.md; GOAL T-0861.
Classification: complexity 10, architectural, cross-cutting (skills, prompts, spawn packets, tool exposure, scorecards, promotion, Jev). Route: high-risk → P0 by Planner grep (zero scouts), then staged shadow-first tasks per P37 with spec review from c6. Goal branch: goal/T-0861 cut from main at 7e3eade (brief + plan committed). Runs alongside the T-0760 active-path tasks (T-0855/T-0856/T-0858/T-0860).
P0 facts (grep 23:25, provenance repo): 12 skills under skills/: planner (memory 3859 B, orchestrate 3568, write-spec 1771, resume 938), scout (repo-map 736, test-gap 624, trace-callers 498), review (adversarial-review 659, challenge 480), triage (classify 453, compact-memory 454), executor (implement-spec 719). 11 are symlinked into .claude/skills (all but implement-spec), and worktrees carry .claude/skills, so EVERY Claude worker of every role sees all 11 descriptions (Level 0, ~2077 chars ≈ 520 tokens, always exposed) and can load any body on demand (Level 2 on invocation, Claude Code's Skill tool). Codex executors get only implement-spec through AGENTS.md ("load skill implement-spec"); results report "Skill used: implement-spec". Worker prompts (scout/review/execute/challenge/spec-review.md) never name skills; review_packet adds the adversarial-review security checklist pointer. Skills with scripts: implement-spec, memory, repo-map, test-gap, trace-callers; adversarial-review and memory carry references. No registry, no versioning beyond git, no per-skill tests except tests/test_memory_skill.py (+ hook tests), no skill telemetry in runs, scorecard, attribution or decision_log; no skill trigger logic anywhere; task classes come from attribution.task_class (security, architectural, debugging, mechanical, unfamiliar or explicit constraints.task_class); workflow strategies in strategy.STRATEGIES (7). Named uncertainty: none blocking; the transcript format for detecting Skill-tool invocations by Claude workers (needed for usage telemetry) is verifiable from .orchestrator/runs transcripts by the executor.
Stage plan (P37): S1a registry + inventory doc T-0863 (c5), S1b skill telemetry T-0866 → v2 T-0888 → v3 T-0894 Planner-approved (T-0893: exposure = all active builtins, Skill-tool logging descoped because .claude/settings.json is protected, skill_selection in CONTEXT_KINDS), S2 compaction T-0867 merged (Level 2 down 49.8%), S3 routing shadow T-0895 → v2 T-0913 (T-0909: existing [skills] table, mandatory never capped, trigger strength, collect() excludes static rows), S4 scorecard + marginal value T-0915 → v2 T-0932 → v3 Planner-approved (T-0934: single-skill-run overlap proxy, execute-root lineage via strategy._root_id, ap.error for flags without --skills), S3 routing shadow T-0913 merged (+T-0926), S5 active routing T-0914 → v2 T-0935 → v3 T-0939 Planner-approved (T-0937: select once before packet build, role on all four record sites, active presentation for execute/review only, windowed recovery rate), S6 specialists T-0940 (c7; depends T-0939 + T-0918), S7 discovery T-0884 merged; ids cascaded twice because depends_on is immutable. HUMAN ITEM: add "Skill" to the jev-gate PreToolUse matcher in .claude/settings.json so Skill-tool invocations become observable, S7 external discovery/quarantine/inspection T-0879 → v2 T-0882 after spec review T-0881 (provenance_info on SkillRecord, findings.json gating in transition(), deterministic trusted-repo match); S4 scorecard T-0938 merged (+T-0946: real gate-row tool names), S5 active routing T-0939 in fix round T-0948 (top-level role on decision rows), S8 learned skills T-0941 → v2 T-0951 → v3 T-0954 Planner-approved (T-0953: seed state.json before the transition, keep discovery/registry suites green), S9 Jev skill routing T-0942 (depends T-0939 + T-0938), S10 promotion/demotion + freshness + version scores + P36 suite T-0955 (depends S4-S9) — full graph filed; S1b skill telemetry in runs/scorecard/decision log (shadow) → S2 compaction to the P6 structure + static triggers + Level 0/1/2 renderings → S3 dynamic skill disclosure shadow (skill_selection decisions per spawn, minimal set computed, exposure unchanged) → S4 skill scorecard + marginal value + redundancy → S5 active routing for builtin skills (per-worktree .claude/skills view limited to the selected set; Codex AGENTS.md pointer) → S6 temporary specialists (role + skills + context + tools packet, conflict detection) → S7 external quarantine/inspection/sandbox (metadata-first discovery, static checks, no execution) → S8 learned skill proposals (evidence-derived drafts into quarantine) → S9 Jev skill routing for the ambiguous bucket (boundary declared) → S10 promotion/demotion + freshness + versioning + P36 suite. Checkpoint PRs after S1-S2, S3-S4, S5-S6, S7-S10.

## GOAL (user 09:40 "do the tool handover"): specialists' tool-disclosure hand-over — T-0985
Complexity 4, localized (spawn.run_worker allowlist branch + specialist tools already computed). Route: brief spec, one executor, deterministic gate, security review (spawn.py), human PR. Zero scouts (facts from grep: _shadow_tool_disclosure returns tool_allowlist; specialist.compose returns tools/tools_added/tools_unavailable; selection runs before the packet build). Goal branch goal/T-0985 cut from origin/main (860c1cf).
Task T-0986 merged (c5618f8) after the goal auto-close gotcha; PR 23 OPEN https://github.com/K3NTAW/orchestrator/pull/23, awaits the human. Goal closed.

## GOAL (user 09:58 "do that"): goal containers never dispatched; goals close only when every execute child is merged — T-0988
Complexity 3, localized (daemon.stale/dispatch/gate/merge filters + executor refusal + planner_runs closable test). Route: brief spec, one executor, gate, security review (daemon.py/executor.py/spawn.py), human PR. Root cause (grep 09:55): the daemon dispatched GOAL T-0985 to Codex because stale() treats any parentless queued execute task as work. Goal branch goal/T-0988 from origin/main. Task T-0989 filed 10:00. The daemon still grabbed the goal container T-0988 in the 16 s before it was set running and started Codex terra on it; the run was stopped (SIGTERM) and the goal restored to running with no result. This is the live reproduction of the bug T-0989 fixes. T-0989 merged (d0611d7, gate + security review green); PR 24 OPEN https://github.com/K3NTAW/orchestrator/pull/24 after GitHub recovered (pushed on retry). Goal closed.

## CROSS-REPO GOAL (user 11:40): docs.kentawaibel.com/orchestrator as ONE self-contained artifact — docs-kentawaibel goal T-0012 (its own bus)
Complexity 6 (content + inline SVG diagrams + tests; no security paths). Route: brief spec with a facts digest, spec review, one executor, gate (vitest + tsc + eslint), human PR from goal/T-0012 → main; Vercel deploys main. Facts (grep 11:30): the live page is public/orchestrator/index.html (33 KB static HTML with inline SVG, PR 2 of 2026-09-20, "Orchestrator system map"); content/orchestrator.md is a second doc that links to it; the page generator scripts never reached main; PR 3 (goal/T-0012 identity/routes/budgets…) was closed unmerged. Stale worktrees/branches T-0013..T-0017 in that repo were moved aside (renamed old-*) because the bus counter reused their ids. Digest committed on goal/T-0012 at .orchestrator/notes/orchestrator-facts-2026-09-23.md (2ff767f). Task T-0013 (c6, spec review). Docs daemon under scratchpad docs-daemon-supervisor.sh (STOP-docs-daemon stops it).

## History (closed)
- GOAL T-0674 Adaptive Planner Routing: closed 17:55, PR 17 merged into main 545fd12 at 18:00, local main = origin/main 363942a. Details: decisions.md 2026-09-21. Backlog still open after this goal: tests/test_scorecard.py:858 live-bus leak (c2); compact-memory (decisions.md and gotchas.md over 300 lines); rebase_changed_diff review respawn.
- GOAL T-0561 roadmap completion: PR 16 merged 15:35. Earlier goals: decisions.md 2026-09-18 to 2026-09-21.

## Session rules (carry forward)
- Spec-writing: acceptance names tests as tests/test_<module>.py::test_name, unittest.TestCase methods; create a dependent task only after its parent's spec review approves (depends_on immutable); read every finished review.
- Planner commits from main checkout: stage only .orchestrator/ paths; `git commit -F <scratchpad file>`; guardrails block pushing main and ~/.config/f; planner-mode scans Bash text (no backticks, `<`, `>`, cp/rm/mv/install/touch, tail pipes, git rebase/checkout/reset/revert, .env).
- Tool contracts: codex(task_id, prompt) dispatches directly; codex_reply(task_id, delta) resumes; merge(task_id) after gate + approve only; recall via `uv run python .claude/skills/memory/scripts/recall.py` (recall.sh picks a python without tomllib).
- Prompt-template edits: Planner edits .orchestrator/prompts directly and commits on main before the goal branch is cut; never through a reviewed Codex diff.
- Session restart kills the in-process daemon; check pgrep for codex exec and claude -p first. Worktrees base on goal/<parent> when that branch exists, else origin/main.

## Prior art (ids for recall)
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt, home `ssh k3ntaw@192.168.1.167` (fish), kgpt home side /opt/kgpt-home. docs.kentawaibel.com: repo /Users/k3ntaw/code/docs-kentawaibel, Vercel project docs-kentawaibel.

## Auto-handover 2026-09-23T20:22:32+02:00 — daemon tick

[planner].handover_context_tokens is the configured handover threshold.
Open goals: none

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 584 more

Last events:
- 2026-09-23T20:07:02+02:00 T-1326 update {"status": "done", "merged_into": "goal/T-0991", "sha": "ccf4b88afa2dbc9fd0e217b
- 2026-09-23T20:07:03+02:00 T-0991 update {"pipeline": {"notification_transition": "infra:B:quota", "last_merge": {"status
- 2026-09-23T20:07:03+02:00 T-1326 update {"pipeline": {"first_ready_at": 1790185908.542434, "dispatched_at": 1790185908.5
- 2026-09-23T20:07:22+02:00 T-1326 update {"pipeline": {"first_ready_at": 1790185908.542434, "dispatched_at": 1790185908.5
- 2026-09-23T20:12:59+02:00 T-0991 update {"status": "done", "reason": "Planner 2026-09-24: all 26 execute tasks merged in

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.

## Cross-repo goal (docs-kentawaibel T-0019, user 2026-09-24 'update the docs'): the single-artifact /orchestrator page gains the Hermes-inspired program
Complexity 6, route: one execute task T-0020 with spec review, gate (vitest, tsc, eslint), one review, human PR from goal/T-0019. Facts digest committed at .orchestrator/notes/orchestrator-facts-2026-09-24-hermes.md in the docs repo (built from main after PR 26: 15 new modules, 86 modules, 1,494 tests, 28 kinds, 22 features, 22 hermes-eval cases, modes as of 2026-09-24, scorecard --hermes baseline). Docs daemon supervisor pid 83512 dispatches.
