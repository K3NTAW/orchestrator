# plan.md — Planner checkpoint (updated 2026-09-20 17:55; Phase H closed, PR 12 open; open goal: T-0445 Phase I, owned by the launcher session)

## OWNER 2026-09-20 17:55 — GOAL T-0445 Phase I (efficiency and Jev decision layer) is driven by the launcher session (planner_session.json pid 7181, process 7155). Session 6410 closed Phase H and has nothing left: the docs goal T-0007 is fully closed by the launcher session (PR 2, preview verified, PR comment, artifact https://claude.ai/artifact/WVgUMFJ1eWxFGTqVTxdUtt); production deploys after the human merges PRs 1 and 2, not from a worktree. Session 6410: do not create tasks, deploy, or rewrite this file; close.
Brief: .orchestrator/phase-i-program.md (user text 17:30). Complexity 9. Priority order P0 telemetry -> P1 packets -> P2 fix-round economics -> P3 Jev shadow routing -> P4 evaluation -> P5 active (evidence only) -> P6 review packets -> P7 independent experiments. Goal branch goal/T-0445 cut from goal/T-0353 head 0d7bf09 at 17:52; PR 13 will list PR 12 as base. Scouts done (read once): T-0446 telemetry, T-0447 packets and fix rounds, T-0448 routing and Jev.
Scout matrix (verified where marked): run rows (bus.log_run, bus.py:269-284) from Claude workers carry task, role, tier, account, usd, turns, executor, complexity, packet_meta and raw usage (spawn.py:409-439); Codex rows carry tokens but never usd (executor.py:107-140); planner_decision rows are the richest (planner_runs.py:570-609); Jev usage lives only in runs/jev/*.jsonl and gate.jsonl (jev.py:7-20, jev_gate.py:16); memory recall is unrecorded; created_at is not stored (daemon.py:576-581 derives it ad hoc); pipeline stamps dispatched_at, gated_at, review_held_at, merged_at are epoch floats (daemon.py:268-292, 534-538, 801-842); fix-round lineage via constraints.fix_round_for and auto_round (daemon.py:150-198); scorecard.build is per executor from execute rows (scorecard.py:186-249), scores() = merged/(merged+failed) min 5 runs else bench priors (scorecard.py:257-301), task_class from constraints or heuristics (scorecard.py:65-128). VERIFIED by Planner grep: bus.normalize_usage builds input_uncached/cache_read/total_tokens (bus.py:241-243) but no run row on disk carries them; daemon.dispatch renders execute without packet= (daemon.py:536) while prompts/execute.md line 1 is {{packet}}, so Codex receives the literal placeholder and no compact packet (Codex execute input median about 315k tokens vs Claude roles' small uncached inputs). Review prompt lacks spec and gate result (spawn.py:514-520); executor packet lacks depends_on (spawn.py:258-267); auto_fix_round re-dispatches a new task instead of resuming the thread (daemon.py:139-201); executor.reply resumes when compatible, MAX_ROUNDS 5 (executor.py:17, 309-339). Routing: pick_executor(role, complexity, scores, task) filters then ranks by scorecard cost when class_success >= floor else weight*score (pool.py:522-554); dispatch chain daemon.dispatch -> _dispatch_worker -> executor.start -> pick_executor (daemon.py:492-538, executor.py:243-256); shadow seam = daemon.dispatch loop before render, active seam = pick_executor ranking block; jev.ask fails open on every error path (jev.py:181-256), budget in jev_state.json (jev.py:125-148), redaction jev.py:34-60, cache in jev_gate (jev_gate.py:145-199).
P0 MERGED on goal/T-0445 (head d7b942a, 18:50): P0a T-0449 attribution fields and normalized tokens on every run row, Codex usd from tokens (orchestrator/attribution.py); P0b T-0450 created_at, first_green_at, gate_attempts, gate_reds, lineage_fix_rounds, accepted_at (Codex shipped without its six tests; held, one codex_reply, merged by hand at 6f3938e); P0c v2 T-0454 scorecard.efficiency and scorecard --efficiency with read-time attribution for legacy rows, fixed by T-0458 after review T-0457 (phantom goals from pr_url on non-goal tasks; malformed-lines footer clobbered); P0d v2 T-0455 orchestrator/baseline.py, baseline save|show|list|compare, README section. Superseded: T-0451, T-0452 (depends_on is immutable, so re-pointing meant new tasks).
BASELINE phase-h-code saved 18:52 from the merged code over all history (.orchestrator/baselines/phase-h-code.json): 127 accepted tasks, 152.7M tokens, 230.7 USD API-equivalent; median 308k tokens per accepted task, max 19.1M; fix_round_rate 0.315, avg 0.68 fix rounds; first_pass_rate undefined (no stamps on old tasks, defined from now on); amplification 1.93 with Execution 79.2M, Fix rounds 56.7M (72 percent of execution), Code review 10.9M, Spec review 2.2M, Scout 2.2M, Jev 1.5M, Planner 0 (interactive Planner usage is in planner_usage.json, not run rows; headless rows absent in the window), unknown group 6.7M. The live server still runs Phase F code, so the after-measurement for P1 comes from tests, packet_meta on new rows once the server runs goal/T-0445, and a second snapshot.
P1 in flight (18:55): P1a T-0461 packet reaches Codex plus dependencies section and packet_meta on Codex rows (running); P1b T-0462 review, spec-review and scout packets (after P1a); P1c T-0463 progressive recall with memory run rows (after P1b). Then baseline save p1-code and compare; then P2 (fix-round economics: executor-level first-pass and cost-to-accepted, auto_fix_round resumes the Codex thread when compatible instead of a fresh task), then P3 Jev shadow classification at the daemon.dispatch seam with rows joined by task, P4 evaluation, P5 only on evidence, P6, P7 one at a time.
External gate on the merged head d7b942a: merge queue green, my rerun red on two test_planner_runs tests (RunGuards concurrent claim, TickAutonomous one launch per tick); checking alone and rerunning before any fix round (Phase H rule).

## State in one paragraph
Phase H (T-0353, efficiency handover, complexity 8) closed at 17:36: ten increments merged on goal/T-0353 (head 0d7bf09, 45 commits and 34 files over goal/T-0260 at 2ca1517), tests-green exit 0 externally on wt/goal-T-0353, PR 12 https://github.com/K3NTAW/orchestrator/pull/12 open for the human after PR 11, retrospective in decisions.md 2026-09-20, GOAL result posted. The main checkout stays on goal/T-0240 (the running MCP servers load Phase F code); switch to goal/T-0353 after PRs 11 and 12 merge so routes, reservations, the acceptance-test gate and failure signatures act, then measure tokens per accepted goal on the next goal against 5.96M. Two Claude Code sessions were attached after the 16:36 restart; the launcher session (planner_session.json pid 7181) ran the docs.kentawaibel.com goal T-0007 in the docs repo (done 17:06, PR 2 there) while this session finished Phase H. No open goal remains on this bus.

## Phase H results (measured unless marked)
- Cost: 21.50 USD API-equivalent, 417 calls, 611 turns; review 84.4 percent, spec review 8.0, scouts 7.6; Codex execute 0 percent because Codex rows carry no usd (backlog: scorecard derives it from tokens as the H4 ledger does). 91 tasks: 49 execute (33 fix rounds and rebases: 14 reliability, 10 budget, 4 context, 3 rebases, 1 accounting, 1 routing), 36 reviews (13 approve, 23 request_changes), 4 spec reviews, 2 scouts.
- Baseline Phase G: 22.38 USD, 82 tasks, 5.96M tokens per accepted goal over 9 goals; interactive Fable 35.2M tokens on 2026-09-19, zero headless decision runs.
- After-results for the new policies: estimate only until the server runs goal/T-0353 code. Counted Planner interventions the new code removes: nine codex_reply rounds for tests Codex never wrote (H6 makes that a gate_red), two hand re-spawns of dead reviews (H5b), three slot-counter resets (H5a), every chain marked merged by hand (G7 v4 already merged).
- Delivered: H1 accounting fields and undefined ratio; H2 launch attribution and scorecard --planner; H3a decision routes; H3b routing wired (failures, gitutil, notify modules; per-goal launch guard; evidence-checked idempotent close, PR left to the Planner unless auto_open_pr); H4 reservations owned by the run that starts, ledger behind its flock, notify once per transition; H5a counts from bus, codex tools post results; H5b worktree reuse, review re-spawn, snapshot hash; H6 acceptance-test gate; H7 failure kinds and signatures, bounded validated flaky rerun, resume compatibility; H8 packet header, acceptance never dropped, bus_events filters; H9 docs.
- Deferred with reasons (matrix in git history at 9111d99): Jev changes, read and test result caches, memory expiry, Codex sandbox scope enforcement, learned routing, expansion metrics.

## Lessons (recorded in gotchas.md and decisions.md)
- Codex shipped code without its acceptance-named tests nine times; check `git diff --stat HEAD~1` for tests/ before trusting a result until H6 is live.
- A full-suite failure that passes alone needs an ordered-module run before a fix round (tests.test_cli autostart thread leak).
- Reservation hand-offs between dispatcher and worker breed defects; the run that starts owns its reservation.
- Pool.save must never serialize state that a locked helper owns (running counters, reservation ledger).
- Two Planner sessions on one bus yield to each other symmetrically; plan.md carries an OWNER header, and the session whose spec got reviewed continues.

## docs.kentawaibel.com
- Site repo /Users/k3ntaw/code/docs-kentawaibel (private GitHub K3NTAW/docs-kentawaibel; Vercel project docs-kentawaibel linked to it, production branch main). Live since 2026-09-20 00:30 from goal/T-0001 (PR 1 open). Goal T-0007 (orchestrator system map page public/orchestrator/index.html, six SVG diagrams, twelve systems, glossary; entry content/orchestrator.md rewritten) done on goal/T-0007, PR 2 https://github.com/K3NTAW/docs-kentawaibel/pull/2 (base PR 1). This session verifies gate and build on wt/deploy7, deploys production from that branch and publishes the page as a shareable artifact. Human: merge PR 1 then PR 2 so main matches production.

## Read first (next session)
1. `bus_read(status_not="done")` then skill `resume`; no goal is open on this bus. Leftovers are superseded or failed tasks.
2. Planner commits from the main checkout (goal/T-0240): stage only .orchestrator/ paths; commit with `git commit -F <scratchpad file>`. planner-mode.sh scans the whole command text: keep backticks, `>`, `<`, and the words it treats as writes (cp, rm, mv, install, touch, tail with a pipe, git rebase/checkout/reset/revert, .env) out of Bash text. Use absolute paths. macOS has no `timeout`.
3. Tool contracts: `spawn_scout(<scout id>)` may answer spawned while ensure_worktree failed on a stray task branch (check the task claimed); `spawn_review(<review id>)`; `spawn_spec_review(<spec_review id>)`; `merge(task_id, target)` skips the review policy. `codex` and `codex_reply` return the result dict and do NOT post it on the Phase F server. Specs are immutable: a fix round is a new task with constraints.fix_round_for; mark chains merged by hand until goal/T-0353 code runs.
4. Slot counters in pool_state.json leak on restart-killed processes on the Phase F server; reset only when pgrep shows no codex exec and no claude -p worker.
5. A review or spec review requeued after a dead worker never runs again on the Phase F server: spawn it by hand.
6. Session restart kills the in-process daemon and its posting threads; workers that survive lose their result. Check pgrep before restarting.
7. Two sessions attached: read the OWNER header before creating tasks.

## Human items outstanding
- Merge PRs 6 to 12 in order in the orchestrator repo; PR 1 then PR 2 in the docs repo.
- Decisions: latency target, security_paths width, daemon as a process (Phase F review); review budget (Phases G and H: every review found a real defect); [limits].reservations on or off after the first production goal; [planner.routes].auto_open_pr stays false.

## Backlog (c2-c3, after PR 12)
- Scorecard usd for Codex rows from tokens (reuse pool.usd_of); [daemon].respawn_after_s documented in pool.toml; consistent total_tokens for pre-G1a rows; recall ranking relevance.
- kgpt side of the orchestrator module (C-K1..C-K4) on the kgpt bus.

## Phase C architecture (decided)
Two processes. (1) kgpt `modules/orchestrator`, thin MCP module on the shared image, runs_on home, owns per-user `orchestrator_connections`, tools list_goals/goal_status READ and start_goal/cancel_goal WRITE_EXTERNAL, forwards to the user's endpoint with X-KGPT-User-Id. (2) `orchestrator serve` in its own container on kenta-server, one ORCH_ROOT per repo under /work, `goal start` = install+commit scaffold + GOAL task + headless Planner launch; bearer per endpoint; per-slug locks.

## Phase D architecture (decided)
The daemon owns the loop; the Planner is a function of decision points (planner_runs.py): task held, scout fan-out done, goal closable, now routed routine/investigate/escalate (H3). Each launch is a fresh headless `claude -p` with a compact decision packet, recorded with route, reason, evidence and cheaper steps, reconciled when dead, gave_up after two real failures (infra failures cool the account instead).

## Prior art (ids for recall.sh get)
- Phase C, D, F, G, H retrospectives: decisions.md 2026-09-18 to 2026-09-20 (goals T-0073, T-0109, T-0240, T-0260, T-0353). Gotchas of 2026-09-19 and 2026-09-20 in gotchas.md.
- bus:T-0354, T-0355 Phase H scouts; docs bus T-0008, T-0009 system-map scouts.
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt, home `ssh k3ntaw@192.168.1.167` (fish shell), kgpt home side /opt/kgpt-home.

## Auto-handover 2026-09-20T18:34:56+02:00 — daemon tick

### T-0445 GOAL Phase I: Orchestrator efficiency and Jev decision layer — P0 current-version token/cost/outcome attribution and efficiency metrics (tokens per accepted task/goal, tokens to first green, first-pass and fix-round rates, pipeline amplification, preserved baselines), then in order P1 role-specific context packets and dedup, P2 fix-round economics in the scorecard, P3 Jev executor classification in shadow mode, P4 shadow evaluation, P5 active routing only on evidence, P6 review packet and complementary-review optimization, P7 independent Jev decision-point experiments; safety invariants deterministic
- running: T-0455 P0d v2: baseline snapshots (supersedes T-0452, re-pointed at P0c v2) — orchestrator/baseline.py saves the efficiency metrics plus a configuration fingerprint to .orchestrator/baselines/<label>.json, compares two snapshots with the non-inferiority metrics flagged, CLI baseline save|show|list|compare, README section Efficiency telemetry (executor=astra, started=2026-09-20T18:30:24+02:00)
- failed: T-0451 P0c: scorecard efficiency metrics — tokens and cost per accepted task and goal (fix-round lineage collapsed), tokens and time to first green, time to accepted, first-pass success rate, fix-round rate and average, fix-round tokens, calls and turns per accepted task, model distribution, pipeline amplification with the nine-bucket breakdown, grouping by goal, executor, complexity band, task class and role, unknown bucket; CLI scorecard --efficiency (reason=superseded by P0c v2 T-0454 (adds read-time attribution for legacy rows so the baseline covers history), resume_hint_keys=[]), T-0452 P0d: baseline snapshots — orchestrator/baseline.py saves the efficiency metrics plus a configuration fingerprint to .orchestrator/baselines/<label>.json, compares two snapshots with the non-inferiority metrics flagged, CLI baseline save|show|compare, README section Efficiency telemetry (reason=superseded by P0d v2 (next P0d task) because depends_on is immutable and its dependency T-0451 was superseded by T-0454, resume_hint_keys=[])
- done (not merged): T-0446 Scout I-S1 (telemetry inventory at Phase H code, READ wt/goal-T-0353): every field where tokens, usd, calls, turns, timestamps and outcomes are recorded per role, what scorecard computes today, and the field-by-field gap against the P0 metric list, T-0447 Scout I-S2 (context construction and fix-round paths at Phase H code, READ wt/goal-T-0353): what each role's prompt and packet contains today, where unrelated state leaks in, how fix rounds resume, and measured packet sizes from recent runs, T-0448 Scout I-S3 (routing and Jev integration at Phase H code, READ wt/goal-T-0353): pick_executor inputs and order, scorecard and bench feeds, the Jev client API, budget, cache, redaction and fail-open patterns, the jev_gate flow, and the exact seam where a pre-dispatch classification would attach in shadow mode, T-0453 review: P0a: attribution on every run row — bus.log_run enriches each row with bucket (planner, scout, execute, fix_round, spec_review, review, challenge, jev, memory, other), fix-round lineage, executor and model, complexity band and task class; normalized token fields actually written for Claude and Codex rows; Codex usd estimated from tokens with usd_source; Jev usage rows carry task, goal and bucket, T-0456 review: P0b: lifecycle stamps for time and token metrics — bus.create_task writes created_at; the gate stamps first_green_at once and counts gate attempts and reds; report_merge stamps accepted_at on the merged task and on its fix-round lineage root, T-0457 review: P0c v2: scorecard efficiency metrics (supersedes T-0451; adds read-time attribution for legacy rows) — tokens and cost per accepted task and goal with fix-round lineage collapsed, tokens and time to first green, time to accepted, first-pass and fix-round rates, fix-round tokens, calls and turns, model distribution, pipeline amplification with the nine-bucket breakdown, grouping by goal, executor, band, class and role, unknown bucket; CLI scorecard --efficiency, T-0459 review: P0c fix round 1: accepted_goals counts only GOAL tasks (role triage or goal, parent None) so an execute task carrying pr_url is never a phantom goal; tokens_per_accepted_goal and usd_per_accepted_goal stop re-scanning through efficiency() and efficiency() no longer rewrites the shared malformed-lines footer state (review T-0457)
- merged: T-0449 P0a: attribution on every run row — bus.log_run enriches each row with bucket (planner, scout, execute, fix_round, spec_review, review, challenge, jev, memory, other), fix-round lineage, executor and model, complexity band and task class; normalized token fields actually written for Claude and Codex rows; Codex usd estimated from tokens with usd_source; Jev usage rows carry task, goal and bucket (sha=06dec5da), T-0450 P0b: lifecycle stamps for time and token metrics — bus.create_task writes created_at; the gate stamps first_green_at once and counts gate attempts and reds; report_merge stamps accepted_at on the merged task and on its fix-round lineage root (sha=6f3938ef), T-0454 P0c v2: scorecard efficiency metrics (supersedes T-0451; adds read-time attribution for legacy rows) — tokens and cost per accepted task and goal with fix-round lineage collapsed, tokens and time to first green, time to accepted, first-pass and fix-round rates, fix-round tokens, calls and turns, model distribution, pipeline amplification with the nine-bucket breakdown, grouping by goal, executor, band, class and role, unknown bucket; CLI scorecard --efficiency (sha=?), T-0458 P0c fix round 1: accepted_goals counts only GOAL tasks (role triage or goal, parent None) so an execute task carrying pr_url is never a phantom goal; tokens_per_accepted_goal and usd_per_accepted_goal stop re-scanning through efficiency() and efficiency() no longer rewrites the shared malformed-lines footer state (review T-0457) (sha=60e6ce3f)

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 193 more

Last events:
- 2026-09-20T18:29:53+02:00 T-0458 update {"status": "done", "merged_into": "goal/T-0445", "sha": "60e6ce3fd685f5e618c9ffd
- 2026-09-20T18:30:10+02:00 T-0454 update {"status": "done", "merged_into": "goal/T-0445", "merged_via": "fix round T-0458
- 2026-09-20T18:30:23+02:00 T-0455 update {"pipeline": {"dispatched_at": 1789921823.5597088}}
- 2026-09-20T18:30:24+02:00 T-0455 update {"status": "running", "assigned_to": "codex", "worktree": "/Users/k3ntaw/code/or
- 2026-09-20T18:30:24+02:00 T-0455 update {"rounds": 0, "executor": "astra", "tier": "astra"}

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.
