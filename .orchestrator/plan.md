# plan.md — Planner checkpoint (updated 2026-09-21 17:55; GOAL T-0674 Adaptive Planner Routing CLOSED, PR goal/T-0674 -> main open for the human)

## CLOSED 17:55 — GOAL T-0674 Adaptive Planner Routing: goal/T-0674 head 0f7856d (14 commits over main 43bda00, 33 files, +3518), external gate 1017 tests green (main 961), baseline planner-fable-only saved (0 headless Planner tokens, as expected), PR https://github.com/K3NTAW/orchestrator/pull/17 opened. Cost 156 USD API-equivalent, 26.5M worker tokens, 81 bus tasks, 26 spec-review rounds (9 waived after three rounds, dispatched directly), 16:00-17:50. Retrospective: decisions.md 2026-09-21 "GOAL T-0674 closed"; three gotchas recorded (spec-review rounds are worth it, double-brace placeholders in specs kill render, rebase_changed_diff needs a Planner re-review). Live smoke on the goal code: `scorecard --planner-routing` read-side empty, `promotion` lists planner_routing stay shadow n=0, `pick planner --model` says hold because account A is over its daily planner budget (interactive Fable usage).
HUMAN NEXT: review and merge the PR; `git pull`; restart with `f orch`; run one ordinary goal with [planner].autonomous = true (shadow) so headless decisions and Opus shadows produce evidence; then `scorecard --planner-routing`, `promotion`, `scorecard --planner`; consider `f orch` reading `orchestrator pick planner --model`. Push main afterwards (Planner commits 43bda00 + this checkpoint are unpushed).
Backlog opened: daemon respawn of reviews on rebase_changed_diff and on render errors (R22); spec text must never contain the double-brace placeholder syntax (render validation); tests/test_scorecard.py:858 live-bus leak (c2); compact-memory (decisions.md and gotchas.md well over 300 lines).

## (history) GOAL T-0674 — Adaptive Planner Routing (user "do this", 16:00)
Complexity 9, route: high-risk/architectural → Planner grep audit (zero scouts, all facts below have provenance repo), detailed plan with interface contracts, atomic specs (spec review ≥6 on sonnet), Codex execution in waves, gate + security review (every orchestrator/*.py and .mcp*.json is a security path), serial merge into goal/T-0674, human PR. Routing modes stay shadow/off; nothing flips without evidence. Base: main 6014a42 (PR 16 code live since the 15:26 restart).

### Audit (P0) — done 16:10, Planner grep
- Where Fable is selected or assumed: pool.toml [models].planner = claude-fable-5-1 (line 219); goals.launch_planner uses cfg["models"]["planner"] (goals.py:322-353); planner_runs.launch_routed resolves tier fable → models.planner, other tiers → models.<tier> (planner_runs.py:1090-1110); decision.py escalate_tier default "fable" ([planner.routes].escalate_tier, pool.toml:42); prompts/planner.md line 1 and CLAUDE.md line 1 say "Planner (Fable 5.1)"; the interactive launcher `f orch` lives in ~/.config/f (guardrails hard floor, human-owned) and picks the interactive model.
- Launch events: planner_runs.decision_points (scouts_done | held | closable per goal) → tick → run_group (grouped per goal, decision.route: routine/investigate(sonnet)/escalate(fable), same-state_version guard, one launch per goal) or legacy run(); goals.start (`orchestrator goal start`, headless goal intake, always models.planner); daemon.auto_fix_round consults decision.route for routine holds when [planner].autonomous. jev_points.planner_relaunch exists (T-0586) but has no caller.
- Context sent: decision_packet (≤6000 chars: kind, goal title, held task title/hold reason/failures ≤1500/review comments + full spawn.packet inventory; scouts_done: 300-char scout summaries; closable: merged task list) or grouped_packet; system prompt prompts/planner.md; template prompts/planner-decision.md. No delta since the previous decision, no memory, no structured findings, no required-output section.
- Which launches need high-level reasoning: decision.py already sends routine (auto fix rounds, routine_close incl. retrospective and optional PR) and investigate (complexity ≤4, sonnet) away from Fable; everything else escalates to Fable regardless of purpose. Missing: a purpose taxonomy and an Opus tier between investigate and escalate.
- Attribution today: headless runs → bus.log_run role planner_decision (route, reason, evidence, prompt_hash, tokens, usd) in runs/<date>.jsonl plus runs/planner_runs.json; interactive → Pool.tally_planner reads Claude Code transcripts into planner_usage.json per account (pool-wide, no per-goal share; scorecard.planner_footer); premium_summary (`scorecard --planner`) lists both.
- KEY FINDING: runs/planner_runs.json does not exist and `scorecard --planner` shows 0 headless invocations; [planner].autonomous = false here and in kgpt-ios. 100 percent of Fable spend is the interactive Planner session on account A: 2.0-2.5M output tokens and 0.3-1.2B cache-read tokens per day (17-21 Sep), 45M pool-formula tokens today. The router built by this goal governs headless launches; its Fable savings only materialise once goals run with [planner].autonomous = true (shadow first) and the interactive session shrinks to goal intake + escalations. Recorded as the goal's central caveat; the interactive model choice stays with the human's launcher (`orchestrator pick planner --model` will print the router's recommendation).
- Pool/account abstractions: accounts A/B with planner budgets, tier aliases via [models], executors table is Codex-only; a Planner tier is an alias resolved through [models] (fable → planner, opus → opus), so multiple Planner models need no pool changes. promotion.FEATURES/decision_log.KINDS/schedlog/baseline are the reuse points.
- Existing tests: test_decision 14, test_planner_runs 68, test_goals 35, test_jev_points 5, test_promotion 7, test_attribution 9; suite 961 green.

### Decomposition (ids assigned)
Wave A, independent pure modules (parallel; no daemon.py/planner_runs.py edits):
- WAVE A COMPLETE 17:47, all seven merged into goal/T-0674: Q1 T-0675 planner_taxonomy.py · Q4 v3 T-0700 planner_router.py + decision_log KINDS planner_route · Q12 v4 T-0706 jev_planner.py · Q2 v5 T-0708 planner_telemetry.py · Q3 v4 T-0719 planner_packet.py (two security-review rounds T-0725/T-0731 fixed via codex_reply 65be3c7, ca522eb; T-0736 approve) · Q6 v4 T-0729 planner_shadow.py + .mcp.planner-shadow.json · Q5 v5 T-0730 planner_scorecard.py + `scorecard --planner-routing` (read-side until B1). Spec-review rounds per module: Q1 0, Q4 2, Q12 3 (waived), Q2 4 (waived), Q3 3 (waived), Q6 3 (waived; v1 T-0684 never dispatched: spec quoted the double-brace placeholder, spawn.render died in the daemon thread, gotcha R20/T-0660 again), Q5 3 (waived). Waived tasks dispatched directly via scratchpad dispatch_direct.py (daemon._dispatch_worker path); the daemon's auto spec review ran anyway and its nits were folded via codex_reply. Wave B filed 17:30: B1 T-0732 (c7, deps Q1 Q2 Q4 Q12 Q6) and B4 T-0733 (c4, deps Q2 Q5 Q6, running 17:46). Superseded (all risks folded into the next version): T-0676, T-0678, T-0680, T-0682, T-0684, T-0685, T-0689, T-0690, T-0692, T-0693, T-0694, T-0698, T-0702, T-0704, T-0710, T-0715, T-0722, T-0724, T-0727; common reviewer findings: name the producer of every consumed shape (planner_taxonomy, planner_telemetry) as depends_on, dict access everywhere, plain notify.notify (notify_once needs a real task id), literal CRITERIA mapping, explicit state_version parameter. Prompt prompts/planner-shadow.md written by the Planner (prompt diffs cannot be reviewed under render, gotcha 2026-09-21) and committed on main before goal/T-0674 is cut.
Wave B, integration (create after Wave A spec reviews approve; depends_on immutable):
- B1 c7 planner_runs.py + decision.py + goals.py: taxonomy → router in run_group/tick; mode off byte-identical argv; shadow launch via planner_shadow after the production launch; active tier choice; hold with visible reason (record status held_for_fable + notify_once) when a hard decision has no Fable; telemetry rows (launch in run_group, usage in _record_decision_usage, materiality in reconcile from snapshots at launch and exit); planner_skipped rows for every eliminated point (same state_version, routine, none, fix round in flight, session attached, no headroom); decision.py gains route none "unchanged_state" and routine "retrospective_deterministic"; goals.launch_planner(model=None); re-escalation (P15): a goal whose last invocation was opus and whose new point classifies architectural_replan or hits repeated_spec_rejection/lineage_cap forces fable with reescalation=True and an escalation packet carrying the opus decision summary. depends_on Q1, Q2, Q4, Q6, Q12.
- B2 c6 packets: decision_packet/grouped_packet delegate to planner_packet.build with previous record (delta from bus events after cursor_at_launch), memory hits via scout_evidence.memory_recall (titles only), packet_chars into telemetry; escalation_packet on re-escalation. depends_on Q3, B1.
- B3 c5 CLI + attribution + docs: `orchestrator pick planner --model` (router recommendation for the interactive launcher), `scorecard --planner` gains per-goal interactive attribution and Fable share (planner_telemetry.interactive_by_goal, accepted_goal_summary), CLAUDE.md line 1 wording. depends_on Q2, Q4, Q5, B1.
- B4 c4 promotion.FEATURES planner_routing (table planner.routing, key mode, default shadow, evidence from planner_scorecard.class_evidence aggregated + shadow agreement), baseline._metrics gains fable_tokens_per_accepted_goal, planner_tokens_per_accepted_goal, fable_calls_per_accepted_goal, fable_escalation_rate, planner_tokens_per_material_decision (PRIMARY lower-better, never a non-inferiority flag), pool.toml [planner.routing] documented defaults + [limits].max_budget_usd.planner_shadow. depends_on Q2, Q5.
After merge: Planner saves baseline `planner-fable-only` (baseline.save), runs the gate on goal/T-0674, retrospective, PR → main.

### Interface contracts (workers follow these; details in each task's constraints.interface_contract)
- Config [planner.routing]: mode shadow|off|active (default shadow; off = today's behaviour), default_tier opus, escalation_tier fable, hard_complexity_min 9, hard_security true, hard_spec_review_request_changes_min 2, hard_ambiguous true, min_samples 20, shadow_sample_rate 1.0, fable_reserve_share 0.2, jev_mode shadow, soft_threshold 0.5. planner_router.load_cfg(pool_cfg) is the only reader; unknown value → default + one notify.
- Tiers are aliases: fable → [models].planner, opus → [models].opus, sonnet → [models].sonnet; the router never returns model ids.
- Taxonomy: classify(point, ctx, goal, task) → {decision_type ∈ 9 names, architectural, ambiguous, risk_class none|low|high, signals, band, task_class}.
- Router: decide(classification, ctx, route, cfg, availability, evidence, jev_signal, sample) → RouterDecision(tier, mode, hard, hold, hold_reason, reasons, soft_score, shadow_tier, candidates, evidence); hard decisions never downgrade; hold when Fable unavailable; decision_log kind planner_route.
- Telemetry: runs/sched/planner_invocations.jsonl phases launch|usage|materiality merged by launch_id; planner_skips.jsonl; planner_shadow.jsonl; planner_jev.jsonl; decision rows in runs/sched/decisions.jsonl. No prompts, transcripts or chain-of-thought anywhere.
- Scorecard: group key model|decision_type|band|task_class|arch; class_evidence → {n, noninferior True|False|None, deltas, reescalation_rate}; tokens/usd/latency never enter non-inferiority.
- Shadow: argv uses .mcp.planner-shadow.json (empty mcpServers) + --disallowedTools Edit,Write,MultiEdit,NotebookEdit,Bash,Task,WebFetch,WebSearch; prompt template planner-shadow.md returns one JSON object {proposed_action, summary, tasks_proposed, confidence, needs_fable, unresolved}.
- Tests: unittest.TestCase methods in tests/test_<module>.py; acceptance names tests/test_<module>.py::test_name.

### Needs the human
- After the PR merges: flip [planner].autonomous = true in shadow for one ordinary goal so headless decisions (and Opus shadows) start producing evidence; read `scorecard --planner-routing`, `promotion`, `scorecard --planner`. Consider having `f orch` read `orchestrator pick planner --model` once B3 lands. Push main after Planner commits (guardrails block the Planner).

## History (closed)
- GOAL T-0561 roadmap completion closed 15:00, PR 16 merged 15:35 at a819bfe, servers restarted 15:26, main pushed 16:00 (6014a42). Retrospective decisions.md 2026-09-21; baseline sched-shadow-1. Backlog opened: R22 spawn.render placeholder validation + dispatch hold on render errors; review respawn retries; hooks tests namespace session ids; scout result cap keeps findings; tests/test_scorecard.py:858 leaks tasks onto the live bus (c2); compact-memory (decisions.md and gotchas.md 351 lines, budget 300).
- Earlier goals: decisions.md 2026-09-18 to 2026-09-21 (T-0073, T-0109, T-0240, T-0260, T-0353, T-0445, T-0489, T-0499, T-0503, T-0561).

## Session rules (carry forward)
- Spec-writing: acceptance names tests as tests/test_<module>.py::test_name, unittest.TestCase methods; create a dependent task only after its parent's spec review approves (depends_on immutable); read every finished review.
- Planner commits from main checkout: stage only .orchestrator/ paths; `git commit -F <scratchpad file>`; guardrails block pushing main and ~/.config/f; planner-mode scans Bash text (no backticks, `<`, `>`, cp/rm/mv/install/touch, tail pipes, git rebase/checkout/reset/revert, .env).
- Tool contracts: codex(task_id, prompt) dispatches directly; codex_reply(task_id, delta) resumes; merge(task_id) after gate + approve only; recall via `uv run python .claude/skills/memory/scripts/recall.py` (recall.sh picks a python without tomllib).
- Prompt-template edits: Planner edits .orchestrator/prompts directly and commits on main before the goal branch is cut; never through a reviewed Codex diff.
- Session restart kills the in-process daemon; check pgrep for codex exec and claude -p first. Worktrees base on goal/<parent> when that branch exists, else origin/main.

## Prior art (ids for recall)
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt, home `ssh k3ntaw@192.168.1.167` (fish), kgpt home side /opt/kgpt-home. docs.kentawaibel.com: repo /Users/k3ntaw/code/docs-kentawaibel, Vercel project docs-kentawaibel.

## Auto-handover 2026-09-21T17:48:28+02:00 — daemon tick

[planner].handover_context_tokens is the configured handover threshold.
### T-0674 GOAL: Adaptive Planner Routing — evidence-driven Planner model routing (Opus default candidate, Fable escalation) with telemetry, taxonomy, deterministic elimination, compact packets, shadow mode, Planner scorecard, promotion, usage protection; brief .orchestrator/planner-routing-program.md
- failed: T-0676 Q2: orchestrator/planner_telemetry.py — per-invocation Planner rows (launch, usage, materiality, skips), per-goal and per-accepted-goal Fable/Opus aggregation, interactive transcript attribution (P1) (reason=?, resume_hint_keys=[]), T-0678 Q3: orchestrator/planner_packet.py — compact, delta-oriented Planner decision packets and the Opus→Fable escalation packet, size-measured (P4, P10) (reason=?, resume_hint_keys=[]), T-0680 Q4: orchestrator/planner_router.py — Planner tier router: modes off|shadow|active, hard vs soft Fable escalation, evidence gate, Fable reserve, hold instead of silent downgrade; decision_log kind planner_route (P5, P9, P11, P17, P18) (reason=?, resume_hint_keys=[]), T-0682 Q5: orchestrator/planner_scorecard.py + `scorecard --planner-routing` — Planner outcomes by model × decision type × band × class × architectural, non-inferiority evidence per class, economics (P7, P13, P16) (reason=?, resume_hint_keys=[]), T-0684 Q6: orchestrator/planner_shadow.py + .mcp.planner-shadow.json — non-mutating Opus shadow Planner launches, structured result parsing, agreement scoring (P6) (reason=?, resume_hint_keys=[]), T-0685 Q12: orchestrator/jev_planner.py — Jev as a shadow routing signal for Planner tier choice: typed batched questions over redacted structured state, fail open, never bypasses hard escalation (P12) (reason=?, resume_hint_keys=[]), T-0686 spec review: Q6: orchestrator/planner_shadow.py + .mcp.planner-shadow.json — non-mutating Opus shadow Planner launches, structured result parsing, agreement scoring (P6) (reason=?, resume_hint_keys=[]), T-0689 Q3 v2: orchestrator/planner_packet.py — compact, delta-oriented Planner decision packets and the Opus→Fable escalation packet, size-measured (P4, P10) (reason=?, resume_hint_keys=[]), … and 20 more
- done (not merged): T-0677 spec review: Q2: orchestrator/planner_telemetry.py — per-invocation Planner rows (launch, usage, materiality, skips), per-goal and per-accepted-goal Fable/Opus aggregation, interactive transcript attribution (P1), T-0679 spec review: Q3: orchestrator/planner_packet.py — compact, delta-oriented Planner decision packets and the Opus→Fable escalation packet, size-measured (P4, P10), T-0681 spec review: Q4: orchestrator/planner_router.py — Planner tier router: modes off|shadow|active, hard vs soft Fable escalation, evidence gate, Fable reserve, hold instead of silent downgrade; decision_log kind planner_route (P5, P9, P11, P17, P18), T-0683 spec review: Q5: orchestrator/planner_scorecard.py + `scorecard --planner-routing` — Planner outcomes by model × decision type × band × class × architectural, non-inferiority evidence per class, economics (P7, P13, P16), T-0687 spec review: Q12: orchestrator/jev_planner.py — Jev as a shadow routing signal for Planner tier choice: typed batched questions over redacted structured state, fail open, never bypasses hard escalation (P12), T-0688 review: Q1: orchestrator/planner_taxonomy.py — decision taxonomy for Planner invocations (purpose over complexity), pure and testable (P2), T-0691 spec review: Q2 v2: orchestrator/planner_telemetry.py — per-invocation Planner rows (launch, usage, materiality, skips), per-goal and per-accepted-goal Fable/Opus aggregation, interactive transcript attribution by day overlap (P1), T-0696 spec review: Q4 v2: orchestrator/planner_router.py — Planner tier router: modes off|shadow|active, hard vs soft Fable escalation, evidence gate, Fable reserve, hold instead of silent downgrade; decision_log kind planner_route (P5, P9, P11, P17, P18), … and 32 more
- merged: T-0675 Q1: orchestrator/planner_taxonomy.py — decision taxonomy for Planner invocations (purpose over complexity), pure and testable (P2) (sha=d81ad317), T-0700 Q4 v3: orchestrator/planner_router.py — Planner tier router: modes off|shadow|active, hard vs soft Fable escalation, evidence gate, Fable reserve, hold instead of silent downgrade; decision_log kind planner_route (P5, P9, P11, P17, P18) (sha=4d4f30a1), T-0706 Q12 v4: orchestrator/jev_planner.py — Jev as a shadow routing signal for Planner tier choice: typed batched questions over redacted structured state, fail open, never bypasses hard escalation (P12) (sha=3de3635e), T-0708 Q2 v5: orchestrator/planner_telemetry.py — per-invocation Planner rows (launch, usage, materiality, skips), per-goal and per-accepted-goal Fable/Opus aggregation, interactive transcript attribution by day overlap (P1) (sha=ed43042d), T-0719 Q3 v4: orchestrator/planner_packet.py — compact, delta-oriented Planner decision packets and the Opus→Fable escalation packet, size-measured (P4, P10) (sha=e4eddd2c), T-0729 Q6 v4: orchestrator/planner_shadow.py + .mcp.planner-shadow.json — non-mutating Opus shadow Planner launches, structured result parsing, agreement scoring (P6) (sha=c2261172), T-0730 Q5 v5: orchestrator/planner_scorecard.py + `scorecard --planner-routing` — Planner outcomes by model × decision type × band × class × architectural, non-inferiority evidence per class, economics (P7, P13, P16) (sha=fd98e1dd), T-0733 B4: promotion.FEATURES planner_routing, baseline Planner metrics, pool.toml [planner.routing] documented defaults and planner_shadow budget (P14, P18, P20) (sha=d4361b25), … and 4 more

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 351 more

Last events:
- 2026-09-21T17:45:24+02:00 T-0754 update {"status": "done", "result": {"summary": "Diff matches the spec closely: _packet
- 2026-09-21T17:45:24+02:00 T-0754 update {"review_verdict": "approve"}
- 2026-09-21T17:45:24+02:00 T-0754 update {"review_facts": {"verdict": "approve", "findings_count": 0, "findings_by_severi
- 2026-09-21T17:45:24+02:00 T-0751 update {"review_verdict": "approve"}
- 2026-09-21T17:47:27+02:00 T-0751 update {"status": "done", "merged_into": "goal/T-0674", "sha": "0f7856d9c994d66b7a7866e

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.
