# plan.md — Planner checkpoint (updated 2026-09-19 23:02, session 5, Phase G closing out)

## State in one paragraph
Open goal: T-0260 Phase G token economy (complexity 8), branch goal/T-0260 from goal/T-0240 head 4c92cac. Session 5 started 22:50 on a fresh MCP server (Phase F code live: the daemon posts Codex results it dispatched itself, holds red merges, and reconciles dead workers that carry a pid). The restart orphaned two Codex runs dispatched by the old server: T-0326 G7 v4 (resumed with codex_reply, committed 8262cd7, result posted by hand because codex_reply returns the result but does not post it) and T-0331 G10 fix round 1 (fresh codex thread started 22:53, running). Merged on goal/T-0260 so far: G0, G1a+fix, G1b+fix, G2+2 fixes, G2b chain (T-0267, T-0302, T-0307, T-0329 at 644285b), G4+fix (T-0274, T-0280), G6 v4 (T-0290), G11+fix (T-0324, T-0328 at b12190e). Not merged, contrary to the session 4 plan: G3 T-0271 is held on review T-0309 (duplicated hunk headers in bounded_diff, render re-bounds, tests missing); fix round T-0336 queued 23:00.

## Phase G task map (23:20)
- Merged: G0 T-0270; G1a T-0264+T-0283; G1b T-0265+T-0291; G2 T-0266+T-0284+T-0298; G2b T-0267+T-0302+T-0307+T-0329; G4 T-0274+T-0280; G6 v4 T-0290 (T-0268, T-0281, T-0286 superseded); G11 T-0324+T-0328; G10 T-0276+T-0331 (5e21878); G7 v4 T-0326+T-0337 (3767871; T-0272, T-0296, T-0320 marked).
- In review: G9 fix round 3 T-0327 (d0ba3a7; review T-0332 re-spawned by hand 23:18 after the restart killed it and the daemon never re-dispatches a requeued review). When it merges: mark T-0314, T-0308, T-0275. G5 fix round 1 T-0341 (fix_round_for T-0273, review T-0343 running; T-0273 held on review T-0338, its tests added by codex_reply at f9747a8). When it merges: mark T-0273.
- Queued: G3 fix round 2 T-0344 (fix_round_for T-0336; T-0336 held on review T-0339, T-0271 held on T-0309). When it merges: mark T-0336, T-0271. G8 T-0277 ready since 23:19 (complexity 5, no spec review).
- Then: retrospective, PR 11.
- Closed leftovers: T-0301 feat-none smoke task marked failed 22:53; T-0299, T-0311 failed spec reviews stay as they are.
- Backlog after Phase G (c2 each): consistent total_tokens for legacy scorecard rows; recall ranking relevance; review-budget decision (T-0260 at about 12 USD at 22:00, every review found a defect); codex_reply and codex MCP tools should post the result to the bus like the daemon's executor thread does (see gotcha 2026-09-19 23:00).

## Phase G measurements and cost (22:30)
- scorecard --by goal T-0260: 9.60 USD at 21:55 (44.9 percent review, 39.1 percent spec review, 16 percent scouts), above the 3.0 USD review budget; every one of the ten code reviews and five spec reviews found a real defect. Decision for the human: keep spec review from 6 and security_paths as is (it paid), or narrow.
- tokens per accepted goal (G1b, on this repo): 5,963,247 over 9 goals (usd 20.27); legacy rows mix with normalised ones.
- G2 packet for T-0256: 3,335 chars, 21 symbols, 15 relevant tests. repomap --stdout: 3,966 chars.

## Read first
1. `bus_read(status_not="done")` then skill `resume`. Live: T-0260 and the children above; the rest are superseded/failed leftovers.
2. Planner commits from the main checkout (goal/T-0240): `git status --short` first; stage only .orchestrator/ paths; commit with `git commit -F <scratchpad file>`. planner-mode.sh scans the whole command text: keep backticks, `>`, and the words it treats as writes (cp, rm, mv, install, tail with a pipe, git rebase/checkout/reset/revert even inside quoted prose) out of Bash text; put long text in a scratchpad file with the Write tool. Use absolute paths: a `cd wt/...` inside a command moves the session cwd.
3. Tool contracts: `spawn_spec_review(<spec_review task id>)` (given an execute id it RUNS AN EXECUTE); `spawn_scout(<execute id>)` runs the fallback executor; `spawn_review(<review task id>)`; `merge(task_id, target)` rebases, tests and fast-forwards but skips the review policy. `codex(task_id, prompt)` and `codex_reply(task_id, delta)` return the result dict and do NOT post it: post with bus.post_result(tid, {summary, commit, executed_by, provenance:['repo'], usage}, 'done') by hand. Specs, scope and depends_on are immutable on the bus: a fix round is a new task with constraints.fix_round_for (its worktree is cut from the held task's branch); mark the chain merged by hand (bus.update(original, status='done', merged_into='goal/T-0260', merged_via='fix round <id> <sha>', hold_reason=None)) until G7 v4 runs in the server. A rebase is a Codex task (T-0258 pattern).
4. Review policy in force: code review only when the merged diff touches pool.toml [review] security_paths (orchestrator/*.py and .orchestrator/prompts/** match almost everything); spec review from complexity 6 on sonnet, waived by the Planner after three rounds; tests-green is the merge bar otherwise.
5. Restart rule: hand over at ~150k context or when an account's daily budget is reached, and only when no codex exec or claude -p worker is alive (pgrep). Tasks dispatched by the old server carry no pid, so the new daemon never reconciles them: resume each by hand (codex_reply on the thread if one exists, else a fresh codex thread).
6. The daemon appends an "Auto-handover" section to this file every 15 min while a goal is open; drop it when rewriting.
7. Account state at 22:51: A planner_day_tokens 33.4M over its 30M budget, B at 4.7M; `orchestrator pick planner` said "hold: no account with headroom" (rate_limit reason on both). Session 5 runs anyway; keep Planner turns short.

## Human items outstanding
- Merge PRs 6, 7, 8, 9, 10 in order: https://github.com/K3NTAW/orchestrator/pull/10 is Phase F. PR 11 (Phase G) comes after Phase G closes.
- Decisions from the Phase F review (latency target, security_paths width, daemon as a process) and the Phase G review-budget question above.

## Next work after Phase G
1. Retrospective for T-0260 (skill memory record), open PR 11 goal/T-0260 → main.
2. Switch the checkout to goal/T-0260 after PR 11 opens so the daemon runs the lease sweep, auto fix rounds and packets on itself; measure tokens per accepted goal on the next goal against 5.96M.
3. kgpt side of the orchestrator module (C-K1..C-K4) on the kgpt bus.

## Phase C architecture (decided)
Two processes. (1) kgpt `modules/orchestrator`, thin MCP module on the shared image, runs_on home, owns per-user `orchestrator_connections` (endpoint_url, label, encrypted bearer), tools list_goals/goal_status READ and start_goal/cancel_goal WRITE_EXTERNAL (proposals), forwards to the user's endpoint with X-KGPT-User-Id applied by UserContextMiddleware. (2) `orchestrator serve` in its own container on kenta-server (ubuntu:24.04 image from this repo: claude native installer, codex release binary, uv, git, gh), one ORCH_ROOT per repo under /work, `goal start` = install+commit scaffold + GOAL task + headless Planner launch; bearer per endpoint; per-slug locks.

## Phase D architecture (decided)
The daemon owns the loop; the Planner becomes a function of decision points (planner_runs.py): task held, scout fan-out done, goal closable. Each decision is a fresh headless `claude -p` launched by goals.launch_planner with an ids-only prompt, recorded in planner_runs.json, reconciled when dead, terminal gave_up after two failures. Never while an interactive session is registered in planner_session.json. Review cost is bounded by pool.toml [review]; scout cost by pool.toml scout limits; Planner session cost by the 150k handover rule.

## Prior art (ids for recall.sh get)
- Phase C retrospective and D3/Phase D retrospective: decisions.md 2026-09-18/19 entries (goal T-0073, T-0109). Gotchas of 2026-09-18/19 in gotchas.md.
- bus:T-0066..T-0069 kgpt scouts; T-0074..T-0077 Phase C scouts; T-0261..T-0263 Phase G scouts.
- Hosts: Hetzner `ssh kgpt@46.62.167.12` dir /home/kgpt/kgpt (compose project), home `ssh k3ntaw@192.168.1.167` (fish shell, pipe scripts via `bash -s`), kgpt home side /opt/kgpt-home.

## Auto-handover 2026-09-19T23:19:02+02:00 — daemon tick

### T-0260 GOAL Phase G: token economy — tokens per accepted goal, straightforward-goal route with zero scouts, compact worker packets and bounded tool outputs, sampled Jev, short Planner decision sessions, automatic routine fix tasks, stage-stamp crash safety, semantic review triggers
- queued: T-0277 G8: semantic review triggers (authorization, migrations, public interfaces, dependencies) next to security_paths, and review approval bound to the reviewed commit and re-assessed after a rebase that changes the diff (depends_on=['T-0272', 'T-0267']), T-0344 G3 fix round 2: test_review_prompt_diff_is_bounded uses the worktree short-circuit and persists the reviewed worktree via bus.update; the review branch fetches the reviewed task once (review T-0339) (depends_on=[])
- running: T-0332 review: G9 fix round 3: pool.codex_available takes the task too, so availability and dispatch routing agree (review T-0325) (executor=?, started=2026-09-19T23:17:57+02:00), T-0343 review: G5 fix round 1: per-session Jev state file writes take the shared state lock, _target_mtime stats the real path and only the logged target is redacted; the seven acceptance tests at f9747a8 stay green (review T-0338) (executor=?, started=2026-09-19T23:16:07+02:00)
- held: T-0271 G3: bounded tool outputs with an expansion path — tests-green prints one line on pass and keeps the log on fail, review and spec-review prompts get a summary-first bounded diff, recall shows provenance and freshness (hold_reason=review request_changes: T-0309 (request_changes), resume_hint_keys=[]), T-0273 G5: Jev gate in sampled mode by default — deterministic sampling, local exact-repeat detection without a network call, tool target and input hash in gate rows, and an offline diagnosis command with a labelling export (hold_reason=review request_changes: T-0338 (request_changes), resume_hint_keys=[]), T-0275 G9: executors routed by expected completion cost per task class with a success floor, falling back to weight times score below the sample size (hold_reason=review request_changes: T-0304 (request_changes), resume_hint_keys=[]), T-0308 G9 fix round 1: pick_executor falls back to weight times score when no measured executor clears the floor, and the success floor is computed per task class (review T-0304) (hold_reason=review request_changes: T-0310 (request_changes), resume_hint_keys=[]), T-0314 G9 fix round 2: executor.start passes the task to pick_executor so cost-based routing runs in production (review T-0310) (hold_reason=review request_changes: T-0325 (request_changes), resume_hint_keys=[]), T-0336 G3 fix round 1: bounded_diff emits each hunk header once, render() stops re-bounding an already bounded diff, and the three acceptance tests in tests/test_spawn.py exist (review T-0309) (hold_reason=review request_changes: T-0339 (request_changes), resume_hint_keys=[])
- failed: T-0299 spec review: G7 v2: the daemon writes routine fix rounds itself and reconciles merged_into (spec review T-0293 folded in: coordinated with the autonomous held decision point, escalation notifies once) (reason=review returned no parseable verdict, resume_hint_keys=['raw']), T-0311 spec review: G7 v2 automatic fix rounds (T-0296) (reason=superseded by T-0312 (daemon-shaped spec review task), resume_hint_keys=[])
- done (not merged): T-0261 Scout G-S1 (token accounting inventory): every place tokens or usd are recorded per role, what scorecard by_goal/by_task aggregate, and what tokens-per-accepted-goal would still miss, T-0262 Scout G-S2 (worker packet and tool-output surfaces): what a worker receives at start, what it fetches in its first turns, where outputs are capped, and what a compact packet generator can build from, T-0263 Scout G-S3 (daemon stage stamps, automatic fix rounds, review triggers): crash windows between stamp and action, what an automatic fix task needs and already exists, and where semantic review triggers and commit-bound approval would go, T-0269 spec review: G6: stage stamps become claimed/completed pairs with a lease — a crash between a stamp and its action is re-run on a later tick, reconciled against process and git state, T-0278 review: G1a: every run row carries goal_id and normalised token buckets (uncached input, cache read, cache write, output, reasoning) through one bus.normalize_usage(); Jev usage rows carry task and goal ids, T-0279 review: G2: compact worker packet — spawn.packet(task) rendered into execute and fix-round prompts at dispatch (objective, acceptance, base commit, write and read scope, symbols and tests per scope file, matching gotchas, verification command, evidence pointers), capped at about 1,200 tokens, T-0282 spec review: G6 v2: stage stamps get a lease and a done marker with an explicit field mapping; sweep_leases only reconciles tasks in queued, running or done, never a held task; merge retries counted (spec review T-0269 folded in), T-0285 review: G4 fix round 1: mirror the rewritten CLAUDE.md Never/Always lists into .orchestrator/prompts/planner.md (gate red: test_planner_md_matches_claude_md_lists), … and 27 more
- merged: T-0264 G1a: every run row carries goal_id and normalised token buckets (uncached input, cache read, cache write, output, reasoning) through one bus.normalize_usage(); Jev usage rows carry task and goal ids (sha=?), T-0265 G1b: scorecard reports tokens per accepted goal — by-role and cached/uncached splits per goal, Jev and Planner buckets, failed and superseded tasks included, accepted goals counted by PR (sha=?), T-0266 G2: compact worker packet — spawn.packet(task) rendered into execute and fix-round prompts at dispatch (objective, acceptance, base commit, write and read scope, symbols and tests per scope file, matching gotchas, verification command, evidence pointers), capped at about 1,200 tokens (sha=?), T-0267 G2b: repo map generator — orchestrator repomap writes memory/architecture.md (module, public symbols, one-line purpose, commit sha) under about 1,000 tokens, refreshed after every merge (sha=?), T-0268 G6: stage stamps become claimed/completed pairs with a lease — a crash between a stamp and its action is re-run on a later tick, reconciled against process and git state (sha=?), T-0270 G0: scout and triage worktrees base on the goal branch, not origin/main (Phase G scouts T-0262/T-0263 read code from PR 5 and reported stale findings) (sha=443feaab), T-0272 G7: the daemon writes routine fix rounds itself (gate red with failing test ids, review request_changes with concrete comments) and reconciles the original task's merged_into when the fix round merges; design or scope changes still escalate to the Planner (sha=?), T-0274 G4: straightforward-goal route, zero scouts by default, five-file limit as a warning, read scope and interface contracts in specs, one-line Planner reminder (sha=?), … and 19 more

Worktrees: wt/T-0002, wt/T-0006, wt/T-0007, wt/T-0008, wt/T-0009, wt/T-0010, wt/T-0012, wt/T-0013, wt/T-0014, wt/T-0016, wt/T-0018, wt/T-0021, wt/T-0022, wt/T-0023, wt/T-0026, … and 145 more

Last events:
- 2026-09-19T23:18:09+02:00 T-0344 created {}
- 2026-09-19T23:18:52+02:00 T-0326 update {"status": "done", "merged_into": "goal/T-0260", "merged_via": "fix round T-0337
- 2026-09-19T23:18:52+02:00 T-0272 update {"status": "done", "merged_into": "goal/T-0260", "merged_via": "fix round T-0337
- 2026-09-19T23:18:52+02:00 T-0296 update {"status": "done", "merged_into": "goal/T-0260", "merged_via": "fix round T-0337
- 2026-09-19T23:18:52+02:00 T-0320 update {"status": "done", "merged_into": "goal/T-0260", "merged_via": "fix round T-0337

Resume: skill resume; re-spawn held spec reviews; dispatch ready execute tasks by hand while Codex cools.
