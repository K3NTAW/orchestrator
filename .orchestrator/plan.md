# plan.md — Planner checkpoint

## Goal T-0065 (opened 2026-09-18 11:20): kgpt cowork with GitHub repos
Complexity 7 overall. Two phases. Scouts T-0066..T-0069 done 2026-09-18 ~13:15; findings folded in below (all ≥0.8 unless noted).

### Phase A — orchestrator repo (this bus): make the orchestrator installable into kgpt
Why: ROOT = ORCH_ROOT or this repo (orchestrator/__init__.py:5); worktrees, hooks, prompts, bus, merge hang off it and worktrees carry only committed files. kgpt has no .claude/settings.json, hooks, skills or .mcp.json.
- T-0070 execute (complexity 4) `orchestrator install <target>` — dispatched by hand to claude:sonnet at 13:20 (see gotcha below). Review: other account, other model; label same-family-review.
- After merge: `uv run orchestrator install /Users/k3ntaw/code/kgpt`; commit the scaffold in kgpt (Planner may commit; message + decisions.md entry with revert path); hand the human one line: start the Planner with `ORCH_ROOT=/Users/k3ntaw/code/kgpt` from the kgpt root (`f orch` lives under ~/.config/f, protected). Then copy "Phase B" below into kgpt/.orchestrator/plan.md and create the tasks there.
- Gotcha to record at retrospective: daemon.free_slots (daemon.py:84) counts only non-cooling Codex executors, so with every Codex row cooling it returns 0 and dispatch() breaks before executor.start → the fallback_claude policy never fires from the daemon. Manual route: spawn_scout(task) → run_worker execute branch (spawn.py:208). Fix candidate: count Claude capacity (limits.max_parallel_claude_workers) when on_exhausted=fallback_claude.

### Phase B — kgpt bus (after restart): the feature
Gate on kgpt (S4, measured): lint 0.3 s, mypy 1.6 s (171 files), pytest 32 s = 620 passed / 418 skipped without DB+Redis, web tsc 1.9 s. `.orchestrator/tests.sh` = `set -euo pipefail; make lint; make typecheck; make test; (cd clients/web && npm run typecheck)`. It does NOT cover the 418 DB-backed tests (fixtures skip; conftest refuses non-"test" DB names; never point it at `make dev`). CI on the PR runs them with Postgres+Redis and fails on any skip. So every spec below must carry fake-backed unit tests that run without a DB, and the human merges only on green CI.
Facts the specs rely on:
- Tables: kernel.projects (0016), project_folders (0017), working_set (0016), github_connections (0018: unique (user_id, installation_id)), attachments (filename, media_type, size_bytes, sha256, direction, attribution, project_id, folder_id, user_id). Next migration 0026. New owned tables get their own RLS in their own migration and go in OWNED_AFTER_0012, never OWNED_TABLES (ADR 0014).
- Import (kernel/github.py:173-249, modules/github/server.py:115-180): tree → blob per kept file → store.save(body, filename, attribution="imported from owner/repo@ref") → put_in_project → folder mirrors the repo dir. Blob sha, tree sha and commit sha are dropped. Caps 400 files / 40 MB / 2 MB.
- commit_files (api.py:326-398): ref → commit → blobs → tree → commit → POST refs (new branch only; existing branch = GitHubSaidNo). App perms today: Contents RW, Metadata R. Pull requests needs a new App permission.
- Newer-version rule (workingset.py:93-102): newest attachment with same filename in same project and later created_at. Nothing is deleted, so the imported attachment stays as the base.
- Tiers (tools/tiers.py:49-96): suggest ceiling WRITE_LOCAL; untrusted content in the turn caps at WRITE_LOCAL and turns WRITE_EXTERNAL into PROPOSED (not denied). github module autonomy=suggest. So a commit or PR is always a proposal. Good.
- Proposals: kernel proposals.create(module, title, rationale, actions=[PlannedToolCall(tool, args)], evidence, risk, from_untrusted) (proposals/__init__.py:46); approve executes via registry (135). Only the agent loop creates them today (loop.py:672); routes exist for approve/reject only. A gateway route can create one directly → UI buttons need no chat turn.
- Web (S3): Cowork.tsx (tree, FolderRow/FileRow, NewProjectDialog modal), Projects.tsx ProjectPanel, api.ts `api` object + react-query keys ['projects'], ['tree', id], ['project-files', id]; Preview.tsx is the only viewer; no diff renderer anywhere in the client. Build: `npm run typecheck`, types from `make api-types`.
- Handbook drift tests (kernel/tests/test_handbook.py:109-131): every manifest tool name must appear in 08-modules.md; every Tier/Autonomy value in 07-permissions.md; index < 3000 chars.
Decisions (ADR 0019, task B0): one repo per project (columns-on-a-binding-table, unique project_id); the base for diffs is the imported attachment itself (nothing new stored); refresh never merges — a file changed on both sides is reported as a conflict and left alone; remote deletions are reported, never applied; commits and PRs are proposals by construction (untrusted ceiling), never on the base branch; the App gains "Pull requests: read and write".

Atomic specs (create in kgpt bus with parent = GOAL; tier per pool; complexity as given; ≥5 gets spec review):
B0 (c2, no deps) ADR 0019 "A project can be bound to one repository" — docs/decisions/0019-*.md with context, decision list above, alternatives (join table for many repos; storing base blobs separately; auto-merge on refresh). Acceptance: file exists, numbering continues 0018, no code.
B1 (c6, no deps) Schema + kernel binding. Files: kernel/migrations/versions/0026_project_repos.py, kernel/kgpt_kernel/repos.py, kernel/kgpt_kernel/github.py, modules/github/server.py, kernel/tests/test_repos.py. Tables: kernel.project_repos(id, project_id unique FK, installation_id, owner, repo, base_branch, base_commit_sha, refreshed_at, user_id + RLS) and kernel.repo_files(attachment_id unique FK, project_id FK, repo_path, blob_sha, user_id + RLS). repos.py: git_blob_sha(bytes) (sha1 of b"blob %d\0"+body), bind(), binding_of(project_id), record_file(), files_of(), changes(project_id) → [{repo_path, status: modified|added, base_attachment_id, current_attachment_id}] using the newer-version rule scoped to filename+folder, "modified" only when git_blob_sha(current) != stored blob_sha; attachments filed in the project with no repo_files row = added. import_into_project takes (path, body, sha) and commit sha, binds on first import, refuses a second import of a different repo into a bound project (error names the bound repo), same repo = allowed (records new files). import_repo passes tree shas and the ref's commit sha (api ref lookup already exists in commit_files; factor a small helper only if inside api.py scope — otherwise fetch via existing tree call's sha and note it). Acceptance: unit tests for git_blob_sha against a known git value, changes() classification with in-memory fakes, DB tests under the db fixture for bind/refuse/record; alembic upgrade+downgrade round-trip test pattern copied from 0016 tests; `make lint typecheck test` green.
B2a (c3, no deps) GitHub API additions. Files: modules/github/api.py, modules/github/tests/test_github_api.py. Add ref_sha(owner, repo, ref) (GET git/ref/heads/{ref}), branches(owner, repo) (GET /repos/{o}/{r}/branches, paginated to 100), create_pull_request(owner, repo, head, base, title, body) (POST /repos/{o}/{r}/pulls). Same _call pattern, check_owner/check_repo/check_ref on every interpolated segment, source-invariant tests like the existing ones. No behaviour change to existing functions.
B2b (c6, deps B1, B2a) Module tools refresh_repo, repo_changes, read_repo_file, list_branches. Files: modules/github/server.py, modules/github/module.yaml, modules/github/tests/test_refresh.py, docs/handbook/08-modules.md, docs/handbook/15-github.md. refresh_repo(project_id) tier WRITE_LOCAL untrusted: fetch tree at base_branch head; per repo_files row compare remote sha vs blob_sha; remote-changed + locally-unchanged → fetch blob, store.save as new attachment (same filename, same folder), record_file; changed both sides → conflicts[]; new remote paths (worth_importing) → import; remote-missing → removed_remotely[]; update base_commit_sha/refreshed_at; return counts + lists + the honest "contents_not_read" line. repo_changes(project_id) tier READ → repos.changes. read_repo_file(owner, repo, path, ref=None) READ_EXTERNAL untrusted, 2 MB cap, says when truncated. list_branches READ_EXTERNAL untrusted. Tests with the FakeGitHub monkeypatch of api._call (test_commit.py:94 pattern). Handbook: tool names in 08-modules github section; 15-github "Using it" gains refresh, changes, branches.
B3 (c5, deps B2b) Commit from project + pull request. Files: modules/github/server.py, modules/github/module.yaml, modules/github/tests/test_commit.py, docs/handbook/08-modules.md, docs/handbook/15-github.md. propose_commit gains optional project_id: with it and no files, owner/repo/base come from the binding and files = repo_changes contents (store.read_blob on current attachments), refusing when there are none ("nothing has changed"). propose_pull_request(project_id or owner/repo, branch, title, body, base=None) WRITE_EXTERNAL → api.create_pull_request; refuses branch == base. Handbook: setup step adds Pull requests permission; using-it examples; "kgpt cannot merge" line.
B4 (c5, deps B3) Gateway routes. Files: gateway/kgpt_gateway/routes/repos.py, the router registration file (find where routes/projects.py is included), gateway/tests/test_repo_routes.py, packages/api-types/** (regenerated via make api-types), docs/handbook/14-projects.md. Routes: GET /projects/{id}/repo (binding or 404), GET /projects/{id}/repo/changes, GET /projects/{id}/repo/diff?path= (difflib.unified_diff of base vs current attachment text; 415 for binary; 413 over 2 MB), POST /projects/{id}/repo/refresh (executes the refresh_repo tool through the registry the way proposals.approve does), POST /projects/{id}/repo/commit {branch, message} → proposals.create(module="github", actions=[PlannedToolCall("propose_commit", {project_id, branch, message})], evidence = changed paths, from_untrusted=True) → {proposal_id}, POST /projects/{id}/repo/pull-request {branch, title, body} likewise. Owner scoping via the session like routes/projects.py. Tests with the app fixture pattern from test_projects_routes.py; DB-free tests for the diff helper.
B5 (c5, deps B4) Web RepoPanel + Diff. Files: clients/web/src/api.ts, clients/web/src/components/RepoPanel.tsx, clients/web/src/components/Diff.tsx, clients/web/src/components/Cowork.tsx, docs/handbook/14-projects.md. RepoPanel under the project select when GET repo is 200: owner/repo@branch, refreshed_at, Refresh (mutation → invalidate ['repo', id], ['tree', id], ['project-files', id]), changed files list with modified/added badges styled like FileRow, click → Diff.tsx (unified diff, +/- colouring with existing tokens, monospace <pre> like Preview text view), Commit and Open PR buttons open a NewProjectDialog-style modal (branch, message | branch, title, body) → POST → toast "proposal filed, approve it in the thread". Gate: `npm run typecheck`; add the acceptance that `npm run build` succeeds.
Order: B0 ∥ B1 ∥ B2a → B2b → B3 → B4 → B5. Scopes of concurrent tasks are disjoint (B1 touches server.py only for passing shas; B2a touches api.py only).
Open question for the human (not blocking): should the GitHub App permission change (Pull requests RW) be done before B3 lands, since propose_pull_request cannot be exercised live without it.

## Prior art
- mem:gotchas.md:91 kgpt hosts (Hetzner kgpt@46.62.167.12, dir /home/kgpt/kgpt; home 192.168.1.167 fish shell).
- bus:T-0035 tests-green.sh runs under uv in the target dir when pyproject.toml exists; honours .orchestrator/tests.sh first.
- Both boxes healthy 2026-09-18 11:00; gateway saw zero chat requests in 7 days.

## Handover if the session restarts
Read this file, `bus_read(status_not="done")`, then skill `resume`. If T-0070 is merged: run the install, commit in kgpt, hand the human the ORCH_ROOT line, and carry "Phase B" into kgpt/.orchestrator/plan.md, then create B0, B1, B2a first.
