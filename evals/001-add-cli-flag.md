Goal: add `orchestrator status --plain` (one line per account).
Expected: 1 scout (repo-map), 1 execute task with scope ["orchestrator/cli.py","tests/**"], tests green, merged into goal/<id>, PR opened, memory updated with a dated entry.
